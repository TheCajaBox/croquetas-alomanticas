import { codigo, pista } from '../comun.js'

export default {
  id: "ferro-03-referencias-de-plantilla",
  mundo: "ferrocarril",
  entorno: "vue3",
  tipo: "cazar-linea",
  titulo: "La caja que todavía no existe",
  enunciado: codigo(
    "A veces hace falta el elemento de verdad: para poner el foco, para medirlo, para",
    "hacer scroll. Eso se pide con una **referencia de plantilla**, y tiene un momento",
    "exacto a partir del cual se puede usar.",
    "",
    "Este componente quiere poner el foco en el campo y revienta. Pulsa la línea culpable.",
  ),
  codigoMostrado: codigo(
    "const { ref, onMounted } = Vue",
    "",
    "const componente = {",
    "  setup() {",
    "    const caja = ref(null)",
    "",
    "    caja.value.focus()",
    "",
    "    return { caja }",
    "  },",
    "  template: `<input ref=\"caja\" />`,",
    "}",
    "",
    "montar(componente)",
  ),
  errorMostrado: codigo(
    "TypeError: Cannot read properties of null (reading 'focus')",
    "",
    "[Vue warn]: Unhandled error during execution of setup function",
  ),
  lineaCulpable: 7,
  explicaciones: {
    7: codigo(
      "Esta. `setup` se ejecuta **antes de que se pinte nada**, así que en ese momento la",
      "referencia todavía vale `null`: el `<input>` no existe. Vue la rellena justo después",
      "de montar.",
      "",
      "El sitio donde ya se puede usar es `onMounted`:",
      "",
      "```js",
      "onMounted(() => caja.value.focus())",
      "```",
      "",
      "Es la misma idea que en la mansión: lo que toca el DOM va después del primer",
      "pintado, nunca antes.",
    ),
    5: codigo(
      "`const caja = ref(null)` está perfecta, y ese `null` es lo correcto: es el valor que",
      "tiene la referencia hasta que Vue la rellena. Declararla es justo lo que hay que",
      "hacer; el problema es cuándo se usa lo que hay dentro.",
    ),
    11: codigo(
      "La plantilla está bien: `ref=\"caja\"` es la forma de decir «guárdame este elemento",
      "en la variable que se llama así». El nombre coincide con el de la variable, que es",
      "el otro fallo típico y aquí no está.",
    ),
    9: codigo(
      "Devolver `caja` es obligatorio para que la plantilla la vea y pueda rellenarla. Si",
      "faltara, la referencia se quedaría en `null` para siempre — pero en silencio, sin",
      "ningún error.",
    ),
  },
  pistas: [
    pista("El error dice que algo vale `null` y le están pidiendo `focus`. Busca qué vale `null` en ese momento y quién debía haberlo rellenado.", 0),
    pista("Vue rellena las referencias de plantilla **después** de pintar. Pregúntate en qué momento se ejecuta el cuerpo de `setup`.", 1),
    pista("La línea que declara la referencia está bien y la plantilla también. Lo que está fuera de sitio es el momento en que se usa lo que hay dentro.", 2),
  ],
  recompensa: { croquetas: 13 },
}
