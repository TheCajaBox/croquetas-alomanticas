import { codigo, pista } from '../comun.js'

export default {
  id: "inspeccion-09-el-filtro-que-se-esquiva",
  mundo: "inspeccion",
  entorno: "worker",
  tipo: "trazar",
  titulo: "El filtro que se esquiva",
  enunciado: codigo(
    "`limpiar` intenta protegerte **borrando** lo peligroso: quita todas las apariciones de",
    "`script` y de `javascript:`. Es la lista de prohibidos otra vez, y aquí se ve por qué no",
    "funciona.",
    "",
    "Rellena la tabla con lo que devuelve `limpiar` para cada entrada, y con si el resultado",
    "es peligroso o no.",
  ),
  codigoMostrado: codigo(
    "function limpiar(texto) {",
    "  return texto.replaceAll('script', '').replaceAll('javascript:', '')",
    "}",
    "",
    "limpiar('<script>robar()')             // (1)",
    "limpiar('<SCRIPT>robar()')             // (2)",
    "limpiar('<scscriptript>robar()')       // (3)",
    "limpiar('<img src=x onerror=robar()>') // (4)",
  ),
  variables: ["devuelve", "¿peligroso?"],
  pasos: [
    { etiqueta: "(1) la etiqueta directa", valores: { devuelve: "'<>robar()'", "¿peligroso?": "no" } },
    { etiqueta: "(2) en mayúsculas", valores: { devuelve: "'<SCRIPT>robar()'", "¿peligroso?": "sí" } },
    { etiqueta: "(3) partida por la mitad", valores: { devuelve: "'<script>robar()'", "¿peligroso?": "sí" } },
    { etiqueta: "(4) otra etiqueta", valores: { devuelve: "'<img src=x onerror=robar()>'", "¿peligroso?": "sí" } },
  ],
  valoresPosibles: [
    "'<>robar()'",
    "'<SCRIPT>robar()'",
    "'<script>robar()'",
    "'<img src=x onerror=robar()>'",
    "''",
    "'robar()'",
    "sí",
    "no",
  ],
  porque: codigo(
    "Tres de cuatro pasan. Y cada una pasa por un motivo distinto, que es lo que hace",
    "imposible arreglar este filtro:",
    "",
    "**(2) Mayúsculas.** `replaceAll` distingue mayúsculas de minúsculas; el HTML no. Se",
    "arregla con una expresión regular insensible… y entonces vienen `<ScRiPt>`, los espacios",
    "raros entre el nombre y el mayor, y el tabulador.",
    "",
    "**(3) La joya.** La entrada es `<sc` + `script` + `ript>`. El filtro borra el `script` de",
    "en medio y al borrarlo **junta los trozos de los lados**: queda `<script>`. El filtro ha",
    "construido el ataque él solo, y a partir de un texto que no contenía ninguna etiqueta.",
    "Cualquier filtro que borre en una sola pasada tiene este problema, y repetir hasta que",
    "no cambie nada abre otro: un texto que crezca en cada pasada y no termine nunca.",
    "",
    "**(4) Lo que no estaba en la lista.** No hay ningún `script` ni ningún `javascript:`, y",
    "ejecuta código igual. Hay decenas de formas: `<img onerror>`, `<svg onload>`,",
    "`<body onpageshow>`, `<iframe srcdoc>`. La lista de prohibidos no puede tenerlas todas,",
    "entre otras cosas porque cada versión nueva de los navegadores añade alguna.",
    "",
    "## Lo que se hace en vez de esto",
    "",
    "**Escapar, no borrar.** No intentes decidir si el texto es peligroso: escríbelo de forma",
    "que no pueda significar nada. `escaparHtml` no tiene lista de nada; convierte cinco",
    "caracteres y ya está, y funciona con los cuatro casos de la tabla —y con los que se",
    "inventen mañana—.",
    "",
    "Cuando de verdad hace falta admitir HTML del usuario -un editor de texto con negritas-",
    "no se filtra a mano: se usa una biblioteca de saneado que **reconstruye** el documento",
    "a partir de una lista de etiquetas y atributos permitidos, en vez de tachar cosas del",
    "que llegó.",
  ),
  tests: [
    {
      nombre: "el filtro se junta a sí mismo los trozos",
      codigo: codigo(
        "function limpiar(texto) {",
        "  return texto.replaceAll('script', '').replaceAll('javascript:', '')",
        "}",
        "esperar(limpiar('<scscriptript>robar()'), 'el filtro se junta el ataque').igualA('<script>robar()')",
        "esperar(limpiar('<SCRIPT>robar()'), 'en mayúsculas').igualA('<SCRIPT>robar()')",
        "esperar(limpiar('<img src=x onerror=robar()>'), 'otra etiqueta').igualA('<img src=x onerror=robar()>')",
        "esperar(limpiar('<script>robar()'), 'la directa sí la para').igualA('<>robar()')",
      ),
    },
  ],
  pistas: [
    pista("Tres de las cuatro salen peligrosas. La primera es la única que el filtro para.", 0),
    pista(
      "En (2), fíjate en que `replaceAll` compara letra por letra: `SCRIPT` y `script` no son el mismo texto para JavaScript, y sí lo son para el navegador.",
      1,
    ),
    pista(
      "En (3), tapa con el dedo la palabra prohibida que hay en medio y lee lo que queda a los lados, pegado. Mira qué se forma.",
      2,
    ),
  ],
  recompensa: { croquetas: 7 },
}
