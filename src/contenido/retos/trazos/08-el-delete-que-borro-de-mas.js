import { codigo, pista } from '../comun.js'
import { TRAZOS } from '../tablas-de-elantris.js'

export default {
  id: "trazos-08-el-delete-que-borro-de-mas",
  mundo: "trazos",
  entorno: "sql",
  tipo: "cazar-linea",
  titulo: "El borrado que se llevó de más",
  enunciado: codigo(
    "El consejo pidió limpiar los apuntes del tenderete, que no está en ningún gremio y no",
    "paga cuota. Es **una** venta: la del lunes, de 165 monedas.",
    "",
    "El programa borró **tres**. Pulsa la línea que tiene la culpa.",
  ),
  esquema: TRAZOS.esquema,
  datos: TRAZOS.datos,
  codigoMostrado: codigo(
    "BEGIN;",
    "",
    "DELETE FROM ventas",
    "WHERE puesto_id IN (",
    "  SELECT id",
    "  FROM puestos",
    "  WHERE gremio_id IS NULL OR abierto = 0 OR gremio_id = 3",
    ");",
    "",
    "COMMIT;",
  ),
  errorMostrado: codigo(
    "3 filas cambiadas.",
    "Se esperaba 1: la única venta del tenderete.",
    "Han desaparecido también las dos de El caldero.",
  ),
  lineaCulpable: 7,
  explicaciones: {
    7: codigo(
      "Esta. La condición tiene **tres** trozos unidos con `OR` y solo el primero era lo que",
      "se pedía:",
      "",
      "- `gremio_id IS NULL` — el tenderete. Correcto.",
      "- `abierto = 0` — Aon Ien, que está cerrado. No tiene ninguna venta, así que no borra",
      "  nada, y estaba de más igual.",
      "- `gremio_id = 3` — **El caldero**, que sí tiene dos ventas. Ahí están las dos que",
      "  faltan.",
      "",
      "Un `OR` de más en un `DELETE` no da error y no se nota: tres filas borradas es un",
      "número perfectamente creíble. Se nota tres semanas después, cuando alguien echa de",
      "menos los apuntes de un puesto.",
      "",
      "Y hay una cosa peor que el `OR`: **nadie miró antes**. La misma condición en un",
      "`SELECT` habría enseñado los tres puestos afectados en un segundo:",
      "",
      "```sql",
      "SELECT id, nombre FROM puestos",
      "WHERE gremio_id IS NULL OR abierto = 0 OR gremio_id = 3;",
      "```",
      "",
      "Eso es la costumbre del mundo, y es la que separa un borrado de un accidente.",
    ),
    1: codigo(
      "El `BEGIN` está bien puesto y es lo único que se hizo bien aquí. Dentro de una",
      "transacción, un `ROLLBACK` en vez del `COMMIT` habría dejado la base como estaba.",
      "",
      "Y fíjate en la lección incómoda: la transacción no evita el fallo, **da tiempo a",
      "verlo**. Sirve si alguien mira las filas cambiadas antes de confirmar. Con el `COMMIT`",
      "escrito de antemano en el mismo envío, no sirve de nada.",
    ),
    3: codigo(
      "Borrar de `ventas` es lo que se pedía: los apuntes, no el puesto. Si el `DELETE` fuera",
      "sobre `puestos`, además, la clave ajena lo habría parado -ese puesto tiene ventas- y",
      "eso habría sido una suerte.",
    ),
    4: codigo(
      "Un `IN` con subconsulta es una forma razonable de escribir «las ventas de estos",
      "puestos». Ojo: la versión negada, `NOT IN`, con una subconsulta que pueda devolver",
      "nulos no devolvería nada, y eso ya lo cazaste en El Dor. Aquí es afirmativa, así que",
      "no hay problema.",
    ),
    10: codigo(
      "El `COMMIT` confirma lo que haya pasado, bueno o malo. No es el culpable: es el",
      "momento en que ya no hay marcha atrás. Con un `ROLLBACK` aquí, nada de esto habría",
      "quedado escrito.",
    ),
  },
  pistas: [
    pista("Ejecuta la subconsulta sola, como un `SELECT`, y mira qué puestos devuelve. Ahí está todo.", 0),
    pista("La condición tiene tres partes unidas con `OR`. El enunciado pedía una.", 1),
    pista("Y de las tres partes, dos sobran. Una de las que sobran no borra nada -su puesto no tiene ventas- y la otra se lleva dos filas. Busca cuál.", 2),
  ],
  recompensa: { croquetas: 10 },
}
