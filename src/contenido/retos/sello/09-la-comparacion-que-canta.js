import { codigo, pista } from '../comun.js'

export default {
  id: "sello-09-la-comparacion-que-canta",
  mundo: "sello",
  entorno: "worker",
  tipo: "trazar",
  titulo: "La comparación que canta",
  enunciado: codigo(
    "`iguales` compara dos textos letra a letra y se va en cuanto encuentra una diferencia. Es",
    "lo razonable: para qué seguir mirando si ya sabes que no son iguales.",
    "",
    "Rellena la tabla con **cuántas letras compara** en cada caso —el valor de `vueltas` al",
    "terminar— y con lo que devuelve.",
    "",
    "La huella guardada es `abcdef`.",
  ),
  codigoMostrado: codigo(
    "function iguales(a, b) {",
    "  if (a.length !== b.length) return false",
    "  let vueltas = 0",
    "  for (let i = 0; i < a.length; i += 1) {",
    "    vueltas += 1",
    "    if (a[i] !== b[i]) return false",
    "  }",
    "  return true",
    "}",
    "",
    "iguales('abcdef', 'zzzzzz')   // (1)",
    "iguales('abcdef', 'abzzzz')   // (2)",
    "iguales('abcdef', 'abcdez')   // (3)",
    "iguales('abcdef', 'abcdef')   // (4)",
  ),
  variables: ["vueltas", "devuelve"],
  pasos: [
    { etiqueta: "(1) falla en la 1.ª", valores: { vueltas: "1", devuelve: "false" } },
    { etiqueta: "(2) falla en la 3.ª", valores: { vueltas: "3", devuelve: "false" } },
    { etiqueta: "(3) falla en la 6.ª", valores: { vueltas: "6", devuelve: "false" } },
    { etiqueta: "(4) no falla", valores: { vueltas: "6", devuelve: "true" } },
  ],
  valoresPosibles: ["0", "1", "2", "3", "5", "6", "7", "true", "false", "undefined"],
  porque: codigo(
    "Mira la columna de `vueltas`: **1, 3, 6, 6**. Cuanto más se parece lo que envías a la",
    "huella guardada, más trabajo hace la función antes de decirte que no. Y hacer más trabajo",
    "es tardar más.",
    "",
    "Eso es un **canal lateral**: la función no te dice cuál es la huella, pero su tiempo te",
    "dice cuántas letras has acertado. Con eso se adivina letra a letra: pruebas las treinta y",
    "seis posibles en la primera posición, te quedas con la que tardó un poco más, y repites.",
    "En vez de probar 36⁶ combinaciones —dos mil millones— pruebas 36×6, que son doscientas",
    "dieciséis.",
    "",
    "Y sí: un décimo de milisegundo se mide. No a la primera, pero repitiendo la medición mil",
    "veces y quedándose con la media, sí. Se han robado claves así.",
    "",
    "La fila (1) enseña además otra fuga: la primera línea de la función devuelve `false` sin",
    "comparar nada cuando las longitudes no coinciden, así que el tiempo también cuenta si has",
    "acertado el **largo**.",
    "",
    "La solución no es esconder el tiempo: es que no dependa del dato. Se comparan **todas** las",
    "letras siempre, y se responde al final. Eso es el reto siguiente.",
  ),
  tests: [
    {
      nombre: "compara bien, que eso no está en discusión",
      codigo: codigo(
        "function iguales(a, b) {",
        "  if (a.length !== b.length) return false",
        "  for (let i = 0; i < a.length; i += 1) if (a[i] !== b[i]) return false",
        "  return true",
        "}",
        "esperar(iguales('abcdef', 'abcdef'), 'dos iguales').esVerdadero()",
        "esperar(iguales('abcdef', 'abcdez'), 'una letra distinta').esFalso()",
        "esperar(iguales('abcdef', 'abcde'), 'largos distintos').esFalso()",
      ),
    },
  ],
  pistas: [
    pista("`vueltas` sube una vez por letra comparada, y la comparación se para en la primera que falla.", 0),
    pista(
      "En (1) la primera letra ya falla: `a` contra `z`. Así que compara una y se va.",
      1,
    ),
    pista(
      "En (3) las cinco primeras coinciden y falla la sexta, así que compara las seis. En (4) coinciden todas y también compara las seis: por eso las dos últimas filas valen lo mismo en `vueltas` y distinto en `devuelve`.",
      2,
    ),
  ],
  recompensa: { croquetas: 7 },
}
