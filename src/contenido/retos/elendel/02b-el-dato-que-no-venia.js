import { codigo, pista } from '../comun.js'

export default {
  id: "elendel-02b-el-dato-que-no-venia",
  mundo: "elendel",
  entorno: "worker",
  tipo: "cazar-linea",
  titulo: "El dato que no venía",
  enunciado: codigo(
    "Ya sabes convertir el texto de fuera en datos. Lo que todavía no sabes es que el",
    "formato puede estar impecable y **el contenido no ser el que esperabas**: una lista",
    "vacía, un campo que ese día no venía, un objeto con una clave menos.",
    "",
    "Aquí el JSON es válido, `JSON.parse` no protesta y el programa revienta de todas",
    "formas. Pulsa la línea culpable.",
  ),
  codigoMostrado: codigo(
    "const respuesta = '{ \"ciudad\": \"Elendel\", \"agentes\": [] }'",
    "",
    "const parte = JSON.parse(respuesta)",
    "",
    "const alMando = parte.agentes[0]",
    "",
    "console.log(`Al mando: ${alMando.nombre}`)",
    "",
    "console.log(`${parte.ciudad}: ${parte.agentes.length} agentes de servicio`)",
  ),
  errorMostrado: codigo(
    "TypeError: Cannot read properties of undefined (reading 'nombre')",
    "",
    "    en la línea 7",
  ),
  lineaCulpable: 7,
  explicaciones: {
    7: codigo(
      "Esta. `alMando` vale `undefined` -no había ni un agente en la lista- y a un `undefined`",
      "no se le puede pedir `.nombre`. Ahí sí se para el programa.",
      "",
      "Lo que falla no es la lectura del dato: es **dar por hecho que el dato está**. Y para eso",
      "hay dos herramientas que van juntas:",
      "",
      "```js",
      "alMando?.nombre                    // undefined en vez de reventar",
      "alMando?.nombre ?? 'sin asignar'   // y un valor de repuesto",
      "```",
      "",
      "El `?.` corta la cadena en cuanto encuentra un vacío, y el `??` pone algo en su lugar. La",
      "regla de la casa: **todo lo que venga de fuera se lee con `?.`**, aunque hoy funcione.",
      "Hoy funciona porque hoy había datos.",
    ),
    1: codigo(
      "El texto es JSON perfectamente válido: llaves cerradas, comillas dobles, dos claves con",
      "sus valores. Que la lista esté vacía no lo hace inválido -una comisaría puede no tener",
      "nadie de servicio esta noche- y esa es justamente la trampa del reto.",
    ),
    3: codigo(
      "`JSON.parse` ha hecho su trabajo y lo ha hecho bien. Devuelve un objeto con `ciudad` y",
      "con `agentes`, y `agentes` es una lista de verdad: vacía, pero lista. Envolver esto en un",
      "`try` no habría cambiado nada, porque aquí no se lanza ningún error.",
    ),
    5: codigo(
      "Aquí está el origen del vacío, y sin embargo esta línea es legal. Pedir la posición 0 de",
      "una lista vacía devuelve `undefined` y no se queja: en JavaScript salirse de una lista no",
      "es un error, es un `undefined` más.",
      "",
      "Eso la convierte en la línea **donde empieza el silencio**, no en la culpable. Si el",
      "programa se limitara a guardar ese valor y no tocarlo, no pasaría nada. El problema",
      "aparece cuando alguien lo trata como si fuera un agente.",
    ),
    9: codigo(
      "Esta línea está bien y además nunca llegó a ejecutarse: cuando un programa revienta, se",
      "detiene en el sitio. Habría impreso `Elendel: 0 agentes de servicio`, que es exactamente",
      "lo que había que decir esta noche.",
    ),
  },
  pistas: [
    pista("El error da un nombre de propiedad y un número de línea. Antes de mirar el código, pregúntate qué tiene que valer una cosa para que pedirle una propiedad reviente.", 0),
    pista("Recorre el camino del dato hacia atrás desde el error: quién puso el vacío y en qué línea se usa como si no lo fuera. Son dos líneas distintas.", 1),
    pista("Una lista vacía preguntada por su primer elemento no da error: da un vacío, y calladamente. Lo que no se puede hacer con un vacío es pedirle nada de dentro.", 2),
  ],
  recompensa: { croquetas: 12 },
}
