import { codigo, pista } from '../comun.js'
import { MERCADO } from '../tablas-de-elantris.js'

export default {
  id: "mercado-04-las-partes-de-un-resumen",
  mundo: "mercado",
  entorno: "sql",
  tipo: "etiquetar",
  titulo: "Las partes de un resumen",
  enunciado: codigo(
    "Agrupar añade dos cláusulas a las cinco que ya conoces, y una de ellas es la que más se",
    "confunde de todo SQL.",
    "",
    "Elige un nombre de la lista y pulsa el trozo al que le corresponde. Sobran nombres a",
    "propósito.",
  ),
  esquema: MERCADO.esquema,
  fragmentos: [
    { texto: "SELECT " },
    { texto: "puesto_id", etiqueta: "la columna por la que se agrupa" },
    { texto: ", " },
    { texto: "SUM(monedas) AS total", etiqueta: "el agregado, que resume el montón" },
    { texto: "\nFROM ventas\nWHERE " },
    { texto: "dia <> 'miércoles'", etiqueta: "qué filas entran en los montones" },
    { texto: "\nGROUP BY " },
    { texto: "puesto_id", etiqueta: "en qué montones se parte" },
    { texto: "\nHAVING " },
    { texto: "SUM(monedas) > 150", etiqueta: "qué montones salen" },
    { texto: "\nORDER BY total DESC;" },
  ],
  etiquetas: [
    "la columna por la que se agrupa",
    "el agregado, que resume el montón",
    "qué filas entran en los montones",
    "en qué montones se parte",
    "qué montones salen",
    "la clave ajena",
    "cuántos montones como mucho",
    "el nombre de la tabla",
  ],
  porque: codigo(
    "La lista de cláusulas pasa de cinco a siete, y el orden sigue sin negociarse:",
    "",
    "```sql",
    "SELECT · FROM · WHERE · GROUP BY · HAVING · ORDER BY · LIMIT",
    "```",
    "",
    "Y el orden en que **trabaja** la base explica todo lo demás:",
    "",
    "1. `FROM` — de dónde.",
    "2. `WHERE` — tira filas. **Antes de agrupar**, así que las que tira no entran en ningún",
    "   montón.",
    "3. `GROUP BY` — hace los montones.",
    "4. `HAVING` — tira **montones**. Aquí ya existen las sumas, así que puede hablar de ellas.",
    "5. `SELECT` — calcula los agregados y pone los alias.",
    "6. `ORDER BY` — ordena.",
    "7. `LIMIT` — corta.",
    "",
    "De ahí salen las dos reglas del mundo, y no hay que memorizarlas aparte:",
    "",
    "- **Un `WHERE` no puede hablar de una suma.** Cuando él trabaja, no hay sumas todavía.",
    "  Para eso está el `HAVING`.",
    "- **Un `HAVING` no debería hacer el trabajo del `WHERE`.** Funciona -filtrar filas después",
    "  de agrupar da el mismo resultado en muchos casos-, pero hace que la base agrupe filas",
    "  que iba a tirar, y sobre todo se lee peor: mezcla dos ideas distintas en la misma línea.",
    "",
    "Y una que sorprende: el `SELECT` trabaja **después** del `HAVING`, así que un `HAVING` no",
    "puede usar el alias `total` -en SQLite sí, por cortesía, y en otras bases no-. Escribir el",
    "agregado entero en el `HAVING` funciona en todas.",
  ),
  pistas: [
    pista("Dos de los trozos marcables son la misma columna escrita dos veces: en el `SELECT` y en el `GROUP BY`. No es una redundancia, y el apunte explica por qué.", 0),
    pista("El `WHERE` y el `HAVING` filtran los dos, y no filtran lo mismo. Uno mira filas y el otro montones.", 1),
    pista("Fíjate en que el `HAVING` habla de `SUM(monedas)`: eso solo existe **después** de agrupar. Un `WHERE` no podría escribir esa condición.", 2),
  ],
  recompensa: { croquetas: 7 },
}
