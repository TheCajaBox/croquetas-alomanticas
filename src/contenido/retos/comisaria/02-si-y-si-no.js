import { codigo, pista } from '../comun.js'

export default {
  id: "com-02-si-y-si-no",
  mundo: "comisaria",
  entorno: "worker",
  tipo: "completar",
  titulo: "Si pasa esto, haz aquello",
  enunciado: codigo(
    "Un programa que hace siempre lo mismo sirve de poco. Este tiene que decidir.",
    "",
    "La comisaría clasifica los avisos por la recompensa: a partir de 500 son **grandes**,",
    "a partir de 100 **medianos**, y por debajo de eso **menudencias**.",
    "",
    "Coloca las fichas. Sobran algunas.",
  ),
  apunte: codigo(
    "`if` es la primera bifurcación del camino:",
    "",
    "```js",
    "if (recompensa >= 500) {",
    "  console.log('Este lo cogemos')",
    "}",
    "```",
    "",
    "Entre paréntesis va algo que da verdadero o falso. Si es verdadero, se ejecuta lo",
    "de las llaves; si no, se salta entero.",
    "",
    "Con `else` se dice qué hacer en el otro caso:",
    "",
    "```js",
    "if (recompensa >= 500) {",
    "  console.log('Este lo cogemos')",
    "} else {",
    "  console.log('Que lo coja otro')",
    "}",
    "```",
    "",
    "Y con `else if` se encadenan tantos casos como haga falta:",
    "",
    "```js",
    "if (recompensa >= 500) {",
    "  categoria = 'grande'",
    "} else if (recompensa >= 100) {",
    "  categoria = 'mediano'",
    "} else {",
    "  categoria = 'menudencia'",
    "}",
    "```",
    "",
    "## El orden importa, y mucho",
    "",
    "Se comprueban **de arriba abajo** y en cuanto uno acierta, se dejan de mirar los",
    "demás. Por eso la cadena va de más exigente a menos.",
    "",
    "Si la pusieras al revés —primero `>= 100`— una recompensa de 800 entraría por ahí",
    "y se quedaría en `'mediano'`, porque el `>= 500` de abajo ya no se llega a mirar",
    "nunca. El programa no da error: da un resultado mal, que es peor.",
    "",
    "## Un atajo que verás mucho",
    "",
    "Cuando lo único que haces es elegir entre dos valores, hay una forma corta:",
    "",
    "```js",
    "const trato = recompensa >= 500 ? 'lo cogemos' : 'que lo coja otro'",
    "```",
    "",
    "Se lee: condición, `?`, lo de si es verdad, `:`, lo de si no. Se llama **ternario**",
    "y está bien para casos cortos. Para tres o más casos, `if` y a otra cosa.",
  ),
  plantilla: codigo(
    "function clasificar(recompensa) {",
    "  if (recompensa ___ 500) {",
    "    return 'grande'",
    "  } ___ (recompensa >= 100) {",
    "    return 'mediano'",
    "  } ___ {",
    "    return 'menudencia'",
    "  }",
    "}",
  ),
  solucion: codigo(
    "function clasificar(recompensa) {",
    "  if (recompensa >= 500) {",
    "    return 'grande'",
    "  } else if (recompensa >= 100) {",
    "    return 'mediano'",
    "  } else {",
    "    return 'menudencia'",
    "  }",
    "}",
  ),
  fichas: [">=", "else if", "else", "<=", "if", "==", "then"],
  tests: [
    { nombre: "500 justos ya son grandes", codigo: "esperar(clasificar(500)).igualA('grande')" },
    { nombre: "800 es grande", codigo: "esperar(clasificar(800)).igualA('grande')" },
    { nombre: "100 justos son medianos", codigo: "esperar(clasificar(100)).igualA('mediano')" },
    { nombre: "250 es mediano", codigo: "esperar(clasificar(250)).igualA('mediano')" },
    { nombre: "40 es una menudencia", codigo: "esperar(clasificar(40)).igualA('menudencia')" },
    { nombre: "0 también es una menudencia", codigo: "esperar(clasificar(0)).igualA('menudencia')" },
  ],
  pistas: [
    pista("«A partir de 500» incluye el 500. Hay un operador que dice justo eso: mayor **o igual**.", 0),
    pista("El segundo hueco encadena otra condición detrás de la primera. El tercero es el «en cualquier otro caso», y ese no lleva condición ninguna.", 1),
    pista("`>=`, `else if` y `else`.", 2),
  ],
  recompensa: { croquetas: 6 },
}
