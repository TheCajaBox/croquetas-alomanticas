import { codigo, pista } from '../comun.js'
import { MERCADO } from '../tablas-de-elantris.js'

export default {
  id: "linea-06-el-informe-que-duplica",
  mundo: "linea",
  entorno: "sql",
  tipo: "cazar-linea",
  titulo: "El informe que suma de más",
  enunciado: codigo(
    "Un informe de lo que ha recaudado cada gremio. El mercado hizo 1.275 monedas y las 165 del",
    "tenderete no son de ningún gremio, así que el informe debería sumar 1.110.",
    "",
    "Suma **1.815**. Pulsa la línea que tiene la culpa.",
  ),
  esquema: MERCADO.esquema,
  datos: MERCADO.datos,
  codigoMostrado: codigo(
    "SELECT",
    "  g.nombre AS gremio,",
    "  COUNT(DISTINCT p.id) AS puestos,",
    "  SUM(v.monedas) AS total",
    "FROM gremios AS g",
    "JOIN puestos AS p ON p.gremio_id = g.id",
    "JOIN ventas AS v ON v.puesto_id = p.id",
    "JOIN puestos AS p2 ON p2.gremio_id = g.id",
    "GROUP BY g.id",
    "ORDER BY total DESC;",
  ),
  errorMostrado: codigo(
    "Los totales suman 1.815.",
    "El mercado hizo 1.275, y 165 no son de ningún gremio: tenían que ser 1.110.",
    "La columna `puestos` sí sale bien.",
  ),
  lineaCulpable: 8,
  explicaciones: {
    8: codigo(
      "Esta. La tabla `puestos` está unida **dos veces** -como `p` y como `p2`- y la segunda",
      "no se usa para nada: no aparece en ninguna columna del `SELECT` ni en ninguna",
      "condición.",
      "",
      "No es inofensiva. Cada fila del resultado intermedio se **multiplica** por el número de",
      "puestos que tenga su gremio, y `SUM` suma esas copias:",
      "",
      "```sql",
      "-- Los escribas tienen 3 puestos, así que cada venta suya sale 3 veces:",
      "-- 305 monedas de verdad -> 915 en el informe.",
      "-- Los canteros tienen 2, así que sus 95 salen 190.",
      "-- Y los gremios de un solo puesto salen bien, que es lo que más despista.",
      "```",
      "",
      "Y fíjate en lo cruel del detalle: la columna `puestos` **sí sale bien**, porque lleva",
      "`DISTINCT` y las copias son idénticas. Así que el informe tiene una columna correcta al",
      "lado de otra inflada, y eso hace que parezca más de fiar de lo que es.",
      "",
      "Una unión que no se usa es basura que hay que quitar. Aparecen al copiar y pegar",
      "consultas y al arreglar una a medias, y no dan error nunca.",
      "",
      "La comprobación que las caza es la de siempre: **que los totales de los montones sumen",
      "el total de la tabla**. El número de filas no lo delata -seis gremios son seis gremios",
      "aunque dentro haya el triple de filas- y la suma sí.",
    ),
    4: codigo(
      "`SUM(v.monedas)` es lo que se pedía y está bien escrito. No es el culpable: es la",
      "víctima. Suma exactamente lo que tiene delante, y lo que tiene delante está duplicado.",
      "",
      "Ponerle un `DISTINCT` -`SUM(DISTINCT v.monedas)`- haría que el número cambiara y",
      "seguiría siendo falso: sumaría **una vez cada cantidad distinta**, así que dos ventas",
      "de 35 monedas contarían como una. Es el arreglo que parece funcionar y miente de otra",
      "manera.",
    ),
    3: codigo(
      "`COUNT(DISTINCT p.id)` está bien, y es la columna que sale correcta. El `DISTINCT` la",
      "salva de la duplicación, y de paso es lo que esconde el problema: si esta columna",
      "también estuviera inflada, alguien lo habría notado antes.",
    ),
    6: codigo(
      "La primera unión de `puestos` es necesaria: sin ella no se puede saber de qué gremio es",
      "cada venta. La condición es correcta y el alias también.",
    ),
    9: codigo(
      "Agrupar por `g.id` es lo correcto: el identificador y no el nombre. Y no tiene nada que",
      "ver con el problema, que ocurre antes de agrupar: cuando el `GROUP BY` llega, las filas",
      "ya están triplicadas.",
    ),
  },
  pistas: [
    pista("Cuenta las tablas del `FROM` y las uniones. ¿Cuántas veces aparece cada una?", 0),
    pista("Hay un alias que no se usa en ninguna otra línea de la consulta. Búscalo.", 1),
    pista("Y piensa qué le pasa a una fila cuando se une con una tabla por una condición que casan varias filas: sale una vez por cada una. Los escribas tienen tres puestos.", 2),
  ],
  recompensa: { croquetas: 10 },
}
