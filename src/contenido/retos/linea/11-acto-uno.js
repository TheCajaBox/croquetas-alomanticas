import { codigo } from '../comun.js'
import { MERCADO } from '../tablas-de-elantris.js'

export default {
  id: "linea-11-acto-uno",
  mundo: "linea",
  entorno: "sql",
  tipo: "cazar-linea",
  acto: 1,
  titulo: "Acto I: la línea que falta",
  enunciado: codigo(
    "Aquí no hay pistas, y en el acto II tampoco.",
    "",
    "El informe del cierre del mercado. Lo escribió alguien que sabe: la unión no pierde puestos,",
    "el `COUNT` cuenta la columna buena, el hueco está decidido. Y devuelve **una sola fila**:",
    "",
    "```",
    "puesto   | ventas | total",
    "Aon Aon  |   16   | 1275",
    "```",
    "",
    "Una fila que dice que Aon Aon hizo dieciséis ventas por 1.275 monedas. Aon Aon hizo tres,",
    "por 140. Los números no son inventados -son los del mercado entero- y el nombre tampoco.",
    "",
    "A esta consulta le falta **una línea**, y la base no ha dicho nada. Pulsa la línea a la que",
    "le falta lo que falta: la que debería tener algo debajo.",
  ),
  esquema: MERCADO.esquema,
  datos: MERCADO.datos,
  codigoMostrado: codigo(
    "SELECT",
    "  p.nombre AS puesto,",
    "  COUNT(v.id) AS ventas,",
    "  COALESCE(SUM(v.monedas), 0) AS total",
    "FROM puestos AS p",
    "LEFT JOIN ventas AS v",
    "  ON v.puesto_id = p.id",
    "ORDER BY total DESC;",
  ),
  errorMostrado: codigo(
    "1 fila devuelta.",
    "puesto: Aon Aon · ventas: 16 · total: 1275",
    "Se esperaban 9, una por puesto. Ningún error, ningún aviso.",
  ),
  lineaCulpable: 7,
  explicaciones: {
    7: codigo(
      "Esta. Debajo de aquí falta el `GROUP BY p.id`.",
      "",
      "Sin `GROUP BY`, los agregados resumen **la tabla entera en una sola fila**: dieciséis",
      "ventas y 1.275 monedas, que son los totales del mercado. Eso es correcto y es lo que se",
      "pidió.",
      "",
      "Lo que no es correcto es la columna `p.nombre`, y aquí está lo que hace este final:",
      "",
      "> En una consulta agrupada, el `SELECT` solo puede llevar columnas del `GROUP BY` y",
      "> agregados. `p.nombre` no es ninguna de las dos cosas.",
      "",
      "**Casi todas las bases dan error ahí. SQLite no.** Te devuelve el nombre de una fila",
      "cualquiera del montón -aquí, la primera- y se queda tan tranquila. Así que sale una",
      "fila con un nombre de verdad al lado de dos números de verdad, y no significan lo mismo:",
      "el nombre es de un puesto y los números son de nueve.",
      "",
      "Es el aon al que le falta una línea. No humea, no se rompe, no avisa. Hace otra cosa.",
      "",
      "Y fíjate en la crueldad del detalle: **el `ORDER BY total DESC` no chirría**. Ordenar",
      "una fila sale bien. Todo lo demás de la consulta está impecable, y por eso pasó la",
      "revisión.",
    ),
    6: codigo(
      "El `LEFT JOIN` es correcto y necesario: es lo que consigue que un puesto sin ventas",
      "aparezca en el informe. Con el `GROUP BY` puesto, esta línea es la que hace que `La",
      "muralla` salga con su cero.",
      "",
      "Y date cuenta de que aquí no se nota: con una sola fila de resultado, da igual si la",
      "unión salva a alguien o no.",
    ),
    3: codigo(
      "`COUNT(v.id)` está bien y es justamente lo que hay que escribir después de un `LEFT",
      "JOIN`: cuenta valores no nulos, así que un puesto sin ventas contaría cero y no uno.",
      "",
      "Aquí devuelve 16 porque el montón es la tabla entera, no porque esté mal escrito. Con",
      "el `GROUP BY` puesto, esta línea da los números correctos sin tocarla.",
    ),
    4: codigo(
      "El `COALESCE` también está bien: es la decisión de que un hueco se lea como cero.",
      "",
      "Aquí no hace nada -hay dieciséis ventas que sumar, así que no hay ningún nulo que",
      "tapar- y con el `GROUP BY` puesto es lo que hace que `La muralla` salga con un 0 en vez",
      "de con un blanco.",
    ),
    8: codigo(
      "El `ORDER BY` no tiene ninguna culpa, y es lo que hace este fallo tan difícil de ver:",
      "ordenar una sola fila funciona perfectamente. Si el resultado hubiera salido sin orden,",
      "o del revés, alguien habría mirado la consulta con otros ojos.",
    ),
  },
  recompensa: { croquetas: 13 },
}
