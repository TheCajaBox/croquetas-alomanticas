import { codigo, pista } from '../comun.js'

export default {
  id: "dia1-07-primera-funcion",
  mundo: "primer-dia",
  entorno: "worker",
  tipo: "codigo",
  titulo: "Tu primera función",
  jefe: true,
  enunciado: codigo(
    "Y ahora sí: a escribir. Una función, y pequeña.",
    "",
    "Escribe `saludar(nombre)`, que **devuelva** el texto:",
    "",
    "`Buenas, NOMBRE.`",
    "",
    "Por ejemplo, `saludar('Wayne')` tiene que devolver `Buenas, Wayne.` — con la coma,",
    "con el punto final y con esas mayúsculas.",
    "",
    "Te dejo el hueco preparado abajo. Solo hay que rellenar la línea del `return`.",
  ),
  apunte: codigo(
    "Una **función** es un trozo de programa al que le pones nombre para poder usarlo",
    "tantas veces como quieras:",
    "",
    "```js",
    "function saludar(nombre) {",
    "  return `Buenas, ${nombre}.`",
    "}",
    "",
    "saludar('Wayne')   // 'Buenas, Wayne.'",
    "saludar('Marasi')  // 'Buenas, Marasi.'",
    "```",
    "",
    "Las piezas:",
    "",
    "- `function` — «voy a declarar una función»",
    "- `saludar` — su nombre",
    "- `(nombre)` — el **parámetro**: un hueco que se rellena al llamarla",
    "- `{ ... }` — el cuerpo, lo que hace",
    "- `return` — **lo que devuelve** al que la llamó",
    "",
    "Lo de `return` es lo que más cuesta al principio. `console.log` **enseña** algo",
    "por pantalla y ya está; `return` **entrega** un valor para que quien llamó a la",
    "función pueda seguir usándolo. No son lo mismo, y aquí hace falta `return`.",
    "",
    "Una función sin `return` devuelve `undefined`, que es la forma que tiene",
    "JavaScript de decir «pues nada».",
  ),
  inicial: codigo(
    "function saludar(nombre) {",
    "  // Devuelve aquí el saludo. Acuérdate de las comillas invertidas.",
    "}",
  ),
  solucion: codigo(
    "function saludar(nombre) {",
    "  return `Buenas, ${nombre}.`",
    "}",
  ),
  requisitos: [
    { tipo: "declaraVariable", valor: "saludar" },
    { tipo: "usaPlantilla" },
    { tipo: "prohibeVar" },
  ],
  tests: [
    { nombre: "saluda a Wayne", codigo: "esperar(saludar('Wayne')).igualA('Buenas, Wayne.')" },
    { nombre: "saluda a Marasi", codigo: "esperar(saludar('Marasi')).igualA('Buenas, Marasi.')" },
    { nombre: "devuelve el saludo en vez de escribirlo", codigo: "esperar(saludar('Wax'), 'lo que devuelve la función').esDeTipo('string')" },
    { nombre: "aguanta hasta un nombre vacío", codigo: "esperar(saludar('')).igualA('Buenas, .')" },
  ],
  pistas: [
    pista("Dentro de la función, `nombre` ya vale lo que le hayan pasado al llamarla. Solo tienes que montar el texto con él.", 0),
    pista("Con comillas invertidas y `${nombre}` dentro. Y no te dejes ni la coma ni el punto final: los tests miran el texto entero.", 1),
    pista("`return `Buenas, ${nombre}.`` y ya está. Wayne dice que él la habría escrito igual pero tardando más.", 2),
  ],
  recompensa: { croquetas: 10 },
}
