/**
 * El apunte de Gaotona para «inspeccion-07-cada-destino-su-escape».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "El patrón de este reto es el que hay que llevarse: de los seis destinos, **tres se",
  "resuelven no escapando**. Y son los tres donde escapar a mano es lo que más caro ha",
  "salido históricamente.",
  "",
  "## Los tres donde se escapa",
  "",
  "**Texto de HTML.** Los cinco caracteres. Es `escaparHtml` y ya está.",
  "",
  "**Atributo de HTML.** La misma sustitución, **y** el atributo entre comillas. Las dos",
  "cosas, porque son dos defensas distintas: la sustitución impide cerrar el atributo, y las",
  "comillas impiden que un espacio abra otro. Sin comillas, `title=x onmouseover=robar()`",
  "son dos atributos aunque hayas escapado los cinco caracteres, porque el espacio no está",
  "en la lista de los cinco. Eso es el reto ocho.",
  "",
  "**URL, como parámetro.** `encodeURIComponent`, no `encodeURI`. La diferencia importa:",
  "`encodeURI` respeta `&`, `=`, `?` y `/` porque está pensada para direcciones enteras;",
  "`encodeURIComponent` los codifica porque está pensada para **un trozo** de dirección.",
  "Usar la primera para un parámetro deja añadir parámetros.",
  "",
  "```js",
  "'/buscar?q=' + encodeURIComponent(termino)    // bien",
  "'/buscar?q=' + encodeURI(termino)             // termino = 'a&admin=1' cuela un parámetro",
  "```",
  "",
  "## Los tres donde no se escapa: se manda aparte",
  "",
  "Y este es el patrón de verdad. En los tres, el problema es el mismo —estás **construyendo",
  "una frase** en un idioma que alguien va a interpretar— y la solución es la misma: no",
  "construir la frase. Mandar el dato por otro canal.",
  "",
  "```js",
  "// Base de datos: el dato va aparte, y llega como dato.",
  "base.consultar('SELECT * FROM puestos WHERE nombre = ?', [nombre])",
  "",
  "// Orden del sistema: argumentos sueltos, sin pasar por el intérprete de órdenes.",
  "ejecutarArchivo('convertir', ['-t', formato, nombreDeArchivo])",
  "",
  "// JavaScript: el dato en un atributo o en un JSON, y el código lo lee.",
  "'<div id=\"saludo\" data-nombre=\"' + escaparHtml(nombre) + '\"></div>'",
  "```",
  "",
  "Cuando el dato viaja por otro canal, **no existe la posibilidad** de que se lea como",
  "instrucción. No hay lista de caracteres que recordar, no hay orden de sustituciones, no",
  "hay una codificación nueva que se inventen dentro de tres años. El agujero no está",
  "tapado: no está.",
  "",
  "Escapar para estos tres destinos se puede hacer y casi nunca sale bien, porque las reglas",
  "son bizantinas: en SQL depende del motor y de la codificación de la conexión; en el",
  "intérprete de órdenes cambia entre `sh`, `bash` y Windows; y en JavaScript hay que pensar",
  "en el `</script>` que cierra el bloque desde dentro de una cadena.",
  "",
  "## La tabla que conviene tener en la cabeza",
  "",
  "```",
  "destino                    qué se hace",
  "texto HTML                 escapar cinco caracteres",
  "atributo HTML              escapar cinco + comillas alrededor",
  "parámetro de URL           encodeURIComponent",
  "consulta a base de datos   parámetro aparte",
  "orden del sistema          argumento aparte",
  "código JavaScript          dato aparte, y leerlo",
  "```",
  "",
  "## Cómo se plantea",
  "",
  "Empieza contando: tres de las seis descripciones dicen «no se escapa». Eso ya empareja",
  "medio reto. De los tres que quedan, dos son la misma sustitución y se diferencian por una",
  "cosa que no está en el texto sino alrededor de él.",
)
