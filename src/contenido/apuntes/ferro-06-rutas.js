/**
 * El apunte de Wax para «ferro-06-rutas».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Una aplicación de verdad tiene varias pantallas y una dirección para cada una. Eso permite",
  "compartir un enlace, volver atrás con el botón del navegador y recargar sin perder dónde",
  "estabas — tres cosas que se dan por sentadas y que hay que montar a propósito.",
  "",
  "De eso se encarga **vue-router**. Este juego lo usa: cada `#/reto/...` de la barra de",
  "direcciones es una ruta suya.",
  "",
  "## El mapa",
  "",
  "```js",
  "const rutas = [",
  "  { path: '/', component: Portada },",
  "  { path: '/reto/:retoId', component: Reto, props: true },",
  "  { path: '/mundo/:mundoId', component: Mundo, props: true },",
  "]",
  "",
  "const router = createRouter({",
  "  history: createWebHashHistory(),",
  "  routes: rutas,",
  "})",
  "```",
  "",
  "Una lista de pares dirección-componente, y poco más. Lo interesante son los detalles.",
  "",
  "## Los dos puntos marcan lo que varía",
  "",
  "`'/reto/:retoId'` encaja con `/reto/es6-01`, con `/reto/dia1-02` y con cualquier otro. El",
  "trozo variable queda disponible como `retoId`.",
  "",
  "Y entonces hay dos formas de recogerlo:",
  "",
  "```js",
  "// Con props: true, llega como una prop normal",
  "const props = defineProps(['retoId'])",
  "",
  "// Sin eso, hay que ir a buscarlo",
  "const ruta = useRoute()",
  "ruta.params.retoId",
  "```",
  "",
  "**La primera es mejor casi siempre**, y no por escribir menos: un componente que recibe el",
  "dato por prop se puede montar en una prueba, reutilizar en otro sitio o meter dentro de otro",
  "componente. Uno que llama a `useRoute()` solo funciona dentro de una aplicación con rutas y",
  "en la ruta correcta.",
  "",
  "Es la misma idea que con las props de siempre: cuanto menos sepa un componente de dónde está",
  "metido, más sitios hay donde puede estar.",
  "",
  "## Las dos piezas de la plantilla",
  "",
  "```html",
  "<RouterLink to=\"/colonia\">La colonia</RouterLink>",
  "<RouterView />",
  "```",
  "",
  "`<RouterLink>` es un enlace que **no recarga la página**: cambia la dirección y Vue pinta lo",
  "que toque. Con un `<a href>` normal se recargaría todo y se perdería el estado.",
  "",
  "`<RouterView />` es **el hueco donde se pinta la pantalla actual**. Sin él no se ve nada,",
  "aunque las rutas estén perfectas. Es el fallo de la primera vez: todo bien configurado y la",
  "pantalla en blanco, porque falta decir dónde va.",
  "",
  "## Las guardas",
  "",
  "```js",
  "router.beforeEach((hacia, desde) => {",
  "  if (!hayPartida) return '/'",
  "  return true",
  "})",
  "```",
  "",
  "Se ejecuta antes de cada navegación. Devolver `true` deja pasar; devolver otra ruta redirige;",
  "devolver `false` cancela.",
  "",
  "Es donde se comprueba si hay sesión, si hay permisos o —como en este juego— si el mundo está",
  "desbloqueado. Y conviene que la guarda sea la **única** que decide eso: si además cada",
  "componente lo comprueba por su cuenta, acabas con dos reglas que se contradicen el día que",
  "alguien cambia una.",
  "",
  "## Las dos historias",
  "",
  "```js",
  "createWebHashHistory()     // /#/reto/es6-01",
  "createWebHistory()         // /reto/es6-01",
  "```",
  "",
  "La segunda es más bonita y necesita que el servidor devuelva la misma página para cualquier",
  "dirección. Si no lo hace, recargar en `/reto/es6-01` da un 404.",
  "",
  "Este juego usa la primera precisamente por eso: está publicado en GitHub Pages, que sirve",
  "archivos y no sabe nada de rutas. Con la almohadilla, el servidor solo ve `/` y todo lo",
  "demás lo resuelve el navegador.",
  "",
  "## Lo que despista",
  "",
  "**Cambiar de parámetro no vuelve a crear el componente.** Ir de `/reto/a` a `/reto/b` usa la",
  "misma instancia: `onMounted` no se dispara otra vez. Si cargas datos ahí, la segunda pantalla",
  "se queda con los de la primera. Se arregla vigilando el parámetro con un `watch`, o poniendo",
  "`:key` en el `<RouterView />` para forzar que se recree — que es lo que hace este juego.",
  "",
  "**Las rutas se prueban en orden.** Una ruta comodín puesta antes que las demás se lo come",
  "todo.",
  "",
  "**`useRoute()` y `useRouter()` no son lo mismo.** El primero es dónde estás; el segundo es",
  "con lo que te mueves.",
  "",
  "**Carga perezosa casi gratis:** `component: () => import('./VistaReto.vue')` hace que esa",
  "pantalla no viaje en el paquete inicial. En una aplicación con veinte pantallas, es la",
  "optimización con mejor relación esfuerzo-resultado que existe.",
  "",
  "## Pruébalo tú",
  "",
  "Con estas rutas, ¿qué se pinta en `/reto/es6-01`?",
  "",
  "```js",
  "const rutas = [",
  "  { path: '/:lo(.*)', component: NoEncontrado },",
  "  { path: '/reto/:retoId', component: Reto },",
  "]",
  "```",
  "",
  "**`NoEncontrado`**. La ruta comodín está la primera y encaja con todo, así que gana ella y la",
  "de abajo no se prueba nunca.",
  "",
  "Es un fallo que despista mucho porque la ruta específica está escrita, se ve perfectamente en",
  "el archivo, y aun así no se usa jamás. El comodín va siempre el último.",
  "",
  "## Cómo se plantea",
  "",
  "1. **Una ruta por pantalla que quieras poder enlazar o recargar.** Si no tiene sentido",
  "   compartir su dirección, quizá no era una ruta sino un estado.",
  "2. **`props: true` siempre que puedas.** El componente queda independiente del router.",
  "3. **El comodín, el último.**",
  "4. **Las comprobaciones de acceso, en una guarda y solo ahí.**",
  "5. **Si el mismo componente sirve para varios parámetros**, decide qué pasa al cambiar de",
  "   uno a otro: o `watch`, o `key`.",
)
