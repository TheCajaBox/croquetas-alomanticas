/**
 * El apunte de Wax para «ferro-05-estado-compartido».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Dos componentes que no son padre e hijo no pueden pasarse datos por props. Para que la",
  "barra de arriba y el panel del fondo compartan el número de croquetas habría que subir el",
  "dato hasta el antepasado común de los dos y volver a bajarlo por todas las ramas",
  "intermedias, tocando componentes que no pintan nada con él.",
  "",
  "Eso tiene nombre: *prop drilling*. Y la solución es sacar el dato **fuera de los",
  "componentes**.",
  "",
  "## Un almacén, por dentro",
  "",
  "```js",
  "// colonia.js",
  "import { reactive } from 'vue'",
  "",
  "const estado = reactive({ croquetas: 0 })",
  "",
  "export function usarColonia() {",
  "  return {",
  "    estado,",
  "    sumar: () => { estado.croquetas += 1 },",
  "    gastar: () => { estado.croquetas = Math.max(0, estado.croquetas - 1) },",
  "  }",
  "}",
  "```",
  "",
  "Eso es un almacén. No hace falta ninguna librería, y por debajo Pinia no hace mucho más.",
  "",
  "La clave está en **dónde se declara el estado**: fuera de la función. Un módulo se ejecuta",
  "una sola vez por muchas veces que lo importen, así que ese `reactive` se crea una vez y",
  "todas las llamadas a `usarColonia()` devuelven el mismo. Si estuviera dentro, cada llamada",
  "crearía el suyo y no compartiría nada — que es justo lo que quieres en un composable normal",
  "y justo lo contrario aquí.",
  "",
  "Es la misma diferencia que en El taller entre un contador y otro: la variable de fuera se",
  "comparte, la de dentro no.",
  "",
  "## Cómo se usa",
  "",
  "```js",
  "// En cualquier componente, sin props de por medio",
  "const { estado, sumar } = usarColonia()",
  "```",
  "",
  "Y ya está: dos componentes en ramas distintas del árbol ven el mismo número, y al pulsar en",
  "uno se actualiza el otro.",
  "",
  "Fíjate en que se devuelve **`estado` entero**, no `estado.croquetas`. Desestructurar un",
  "`reactive` corta la reactividad, así que quien reciba el número suelto se quedaría con el",
  "valor de aquel momento.",
  "",
  "## `provide` / `inject` no es lo mismo",
  "",
  "Ya viste `provide` e `inject` en La Nueva Seran, y sirven para algo parecido pero no igual:",
  "",
  "- **`provide`/`inject`** reparte hacia abajo, a los descendientes de quien provee. Distintas",
  "  ramas pueden recibir cosas distintas. Es lo que quieres para un tema visual o para la",
  "  configuración de un formulario con muchos hijos.",
  "- **Un módulo compartido** es único para toda la aplicación. Es lo que quieres para la",
  "  sesión, el carrito o las croquetas.",
  "",
  "Y hay un caso donde la diferencia importa de verdad: si el mismo código corre en un servidor",
  "—renderizado en servidor—, un módulo compartido lo comparten **todas las peticiones a la",
  "vez**, o sea todos los usuarios. Ahí hay que crear el almacén por petición, y por eso Pinia",
  "lo hace así.",
  "",
  "## Qué añade Pinia",
  "",
  "Si el patrón cabe en diez líneas, ¿para qué una librería? Por cuatro cosas que no se ven en",
  "un ejemplo pequeño:",
  "",
  "- **Herramientas de desarrollo:** ver el estado, ver cada cambio y volver atrás.",
  "- **Una forma única de escribirlo**, en vez de que cada archivo invente la suya.",
  "- **Renderizado en servidor** resuelto.",
  "- **Módulos separados** que se cargan cuando hacen falta.",
  "",
  "Su forma es reconocible desde aquí:",
  "",
  "```js",
  "export const usarColonia = defineStore('colonia', {",
  "  state: () => ({ croquetas: 0 }),",
  "  getters: { hayDeSobra: (s) => s.croquetas > 100 },",
  "  actions: { sumar() { this.croquetas += 1 } },",
  "})",
  "```",
  "",
  "`state` es el `reactive`, `getters` son `computed` y `actions` son funciones. Nada nuevo.",
  "",
  "## Lo que despista",
  "",
  "**Compartir de más es peor que compartir de menos.** El estado global es cómodo y por eso",
  "crece: en cuanto un dato está ahí, cualquiera lo cambia desde cualquier sitio y ya no se",
  "sabe quién lo movió. Que un dato sea de un componente y viva en él es lo normal; el almacén",
  "es para lo que de verdad usan varios.",
  "",
  "**Sigue habiendo un solo hilo.** Que el estado sea compartido no significa que haya carreras",
  "como en otros lenguajes: dos componentes nunca lo tocan a la vez.",
  "",
  "**Devuelve funciones para modificar, no dejes que cambien el estado a mano.** Es la",
  "diferencia entre poder buscar `sumar()` y encontrar los cinco sitios que suben las",
  "croquetas, o tener que buscar `croquetas +=` por todo el proyecto.",
  "",
  "**Este juego lo hace así.** Los almacenes de `src/almacen/` son exactamente esto, con Pinia:",
  "la partida, la economía, los gatos y los sombreros viven fuera de los componentes porque los",
  "mira media aplicación.",
  "",
  "## Pruébalo tú",
  "",
  "¿Cuál de los dos comparte de verdad?",
  "",
  "```js",
  "// A",
  "export function usarColonia() {",
  "  const estado = reactive({ croquetas: 0 })",
  "  return { estado }",
  "}",
  "",
  "// B",
  "const estado = reactive({ croquetas: 0 })",
  "export function usarColonia() {",
  "  return { estado }",
  "}",
  "```",
  "",
  "El **B**. En el A el `reactive` está dentro, así que cada llamada crea uno nuevo: dos",
  "componentes tendrían dos contadores distintos, cada uno subiendo por su cuenta, y desde",
  "fuera parecería que la reactividad «no funciona».",
  "",
  "Un cambio de dos líneas de sitio y el comportamiento es el opuesto. Por eso conviene saber",
  "mirar exactamente eso al leer un composable ajeno: ¿el estado está dentro o fuera?",
  "",
  "## Cómo se plantea",
  "",
  "1. **¿Cuántos componentes miran este dato?** Uno: que viva en él. Un padre y sus hijos:",
  "   props, o `provide`. Media aplicación: almacén.",
  "2. **¿El estado va dentro o fuera de la función?** Fuera para compartir, dentro para que",
  "   cada uno tenga el suyo. Es toda la decisión.",
  "3. **Devuelve el objeto reactivo, no sus campos sueltos.**",
  "4. **Da funciones para cambiarlo**, y que sean las únicas que lo tocan.",
  "5. **¿Va a correr en un servidor?** Entonces el almacén se crea por petición, y ahí sí toca",
  "   Pinia.",
  "",
  "## Cómo lo hace este juego",
  "",
  "Los almacenes de `src/almacen/` son exactamente este patrón, con Pinia por encima. La",
  "partida, las croquetas, los gatos y los sombreros viven fuera de los componentes porque los",
  "mira media aplicación:",
  "",
  "```js",
  "export const usarEconomia = defineStore('economia', {",
  "  state: () => ({ croquetas: 12 }),",
  "  actions: {",
  "    ingresar(cantidad, concepto) { ... },",
  "    gastar(cantidad, concepto) { ... },",
  "  },",
  "})",
  "```",
  "",
  "Y fíjate en que **no hay ninguna forma de tocar `croquetas` desde fuera** que no sea",
  "`ingresar` o `gastar`. Eso es a propósito: buscar `gastar(` encuentra los cuatro sitios que",
  "restan croquetas en todo el juego. Si cada componente hiciera `croquetas -= 5`, habría que",
  "buscar `croquetas` por todo el proyecto y leer cada resultado.",
  "",
  "Esa es la ventaja de verdad del patrón, y no se nota hasta que el proyecto crece: **el estado",
  "compartido es el que más se estropea, así que es el que más conviene tener detrás de",
  "puertas con nombre**.",
  "",
  "La cabecera con las croquetas, el panel de la colonia y el puesto de pistas de Wayne miran el",
  "mismo número desde tres sitios distintos del árbol, sin una sola prop entre ellos. Eso es lo",
  "que estás montando en este reto.",
)
