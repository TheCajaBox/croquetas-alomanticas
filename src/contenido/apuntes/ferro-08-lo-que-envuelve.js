/**
 * El apunte de Wax para «ferro-08-lo-que-envuelve».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "`<Transition>`, `<Teleport>` y `<KeepAlive>` son componentes que no pintan nada propio.",
  "Envuelven a otro y cambian **cómo** aparece, **dónde** aparece o **si desaparece**.",
  "",
  "Son de lo último que se aprende y de lo primero que hace falta en una pantalla de verdad:",
  "en cuanto hay un diálogo, una lista que cambia o una pestaña con formulario a medio",
  "rellenar, aparecen los tres.",
  "",
  "## `<Transition>` no anima: pone clases",
  "",
  "Es lo que más sorprende. Vue no sabe cómo quieres que se vea la animación, así que no la",
  "hace: se limita a poner y quitar clases en los momentos exactos.",
  "",
  "```html",
  "<Transition name=\"desvanecer\">",
  "  <p v-if=\"visible\">Aquí estoy</p>",
  "</Transition>",
  "```",
  "",
  "```css",
  ".desvanecer-enter-active, .desvanecer-leave-active { transition: opacity 0.3s; }",
  ".desvanecer-enter-from, .desvanecer-leave-to { opacity: 0; }",
  "```",
  "",
  "Las seis clases que puede poner son `-enter-from`, `-enter-active`, `-enter-to`,",
  "`-leave-from`, `-leave-active` y `-leave-to`. Sin CSS que las use, no se ve absolutamente",
  "nada — y el componente «no funciona» aunque esté perfectamente puesto.",
  "",
  "Vue sí hace una cosa lista: **espera a que la animación de salida termine** antes de quitar",
  "el elemento del DOM. Eso, a mano, es la parte incómoda de animar algo que desaparece.",
  "",
  "**Solo un elemento a la vez**, o un componente de una sola raíz. Para varios,",
  "`<TransitionGroup>`, que además necesita una `key` en cada hijo para saber cuál se ha movido.",
  "",
  "Y funciona igual con `v-if` que con `v-show`. La diferencia sigue siendo la de siempre:",
  "`v-if` crea y destruye; `v-show` solo esconde con CSS, y es más barato para algo que se",
  "enseña y se oculta muchas veces.",
  "",
  "## `<Teleport>` cambia el sitio, no la familia",
  "",
  "```html",
  "<Teleport to=\"body\">",
  "  <div class=\"dialogo\">...</div>",
  "</Teleport>",
  "```",
  "",
  "El contenido se pinta al final del `body`, y **sigue siendo hijo del componente para todo lo",
  "demás**: recibe las props, emite sus eventos, se destruye con él.",
  "",
  "Para qué sirve: un diálogo dentro de un contenedor con `overflow: hidden` se recorta; dentro",
  "de uno con `transform` se le rompe el `position: fixed`; y con `z-index` se pelea con lo que",
  "tenga alrededor. Teletransportándolo al `body`, ninguno de esos problemas existe.",
  "",
  "Es la solución de los diálogos, los avisos flotantes y los menús desplegables, y antes de",
  "que existiera había que sacar esos componentes de su sitio natural en el código, con lo",
  "incómodo que era.",
  "",
  "**El destino tiene que existir antes.** Si el selector no encuentra nada, Vue avisa por",
  "consola y no pinta el contenido. Por eso el destino suele ser `body` o algo del",
  "`index.html`, no un elemento creado por otro componente.",
  "",
  "## `<KeepAlive>` aparta en vez de destruir",
  "",
  "```html",
  "<KeepAlive>",
  "  <component :is=\"pestanaActual\" />",
  "</KeepAlive>",
  "```",
  "",
  "Un `v-if` falso **destruye** el componente: su estado, sus refs, lo que hubiera escrito el",
  "usuario en un campo. Al volver, nace de cero.",
  "",
  "`<KeepAlive>` lo aparta y lo guarda. Al volver, sigue como estaba: el formulario a medio",
  "rellenar, el scroll donde estaba, los datos ya cargados.",
  "",
  "Y cambia los ganchos. Un componente apartado no se desmonta, así que `onUnmounted` no se",
  "dispara; los que valen son `onActivated` y `onDeactivated`. Si tienes un intervalo que",
  "pararías en `onUnmounted`, con `<KeepAlive>` seguiría corriendo.",
  "",
  "Tiene precio: lo apartado ocupa memoria. Por eso admite `:max` para guardar solo los últimos.",
  "",
  "## Lo que despista",
  "",
  "**El nombre de las clases sale de `name`.** Sin `name`, el prefijo es `v-`. Con",
  "`name=\"desvanecer\"`, es `desvanecer-`. Cambiar uno y no el otro es el fallo de la primera",
  "vez.",
  "",
  "**Una transición sobre algo que ya está en pantalla al cargar no se ve**, salvo que le pongas",
  "`appear`.",
  "",
  "**`<Teleport>` no mueve estilos.** El elemento se pinta fuera, así que los selectores CSS",
  "que dependían de estar dentro de un contenedor dejan de aplicar. Los estilos con ámbito",
  "siguen funcionando, porque van por atributo.",
  "",
  "**Los tres se pueden anidar**, y el orden importa: `<KeepAlive>` por fuera de",
  "`<Transition>`, no al revés.",
  "",
  "## Pruébalo tú",
  "",
  "¿Por qué no se ve nada aquí?",
  "",
  "```html",
  "<Transition name=\"suave\">",
  "  <p v-if=\"visible\">Hola</p>",
  "</Transition>",
  "```",
  "",
  "```css",
  ".suave-enter, .suave-leave-to { opacity: 0; }",
  ".suave-enter-active { transition: opacity 1s; }",
  "```",
  "",
  "Porque `.suave-enter` **no existe**. En Vue 2 esa clase se llamaba así; en Vue 3 se llama",
  "`.suave-enter-from`. Es de los cambios entre versiones que más tiempo hacen perder, porque",
  "no da error: simplemente el elemento aparece de golpe, como si no hubiera transición.",
  "",
  "Cuando una transición «no hace nada», el orden de comprobación es: ¿coincide el `name` con",
  "el prefijo de las clases?, ¿están los nombres de Vue 3?, ¿hay un `transition` en la clase",
  "`-active`?",
  "",
  "## Cómo se plantea",
  "",
  "1. **¿Se recorta o se tapa?** Teleport.",
  "2. **¿Quiero que conserve lo escrito al volver?** KeepAlive, y recuerda que cambian los",
  "   ganchos.",
  "3. **¿Quiero que entre o salga con gracia?** Transition, y **escribe el CSS**: Vue solo pone",
  "   las clases.",
  "4. **¿Son varios a la vez?** TransitionGroup, con `key`.",
  "5. **¿Se enseña y se esconde muchas veces?** `v-show` en vez de `v-if`.",
)
