/**
 * El apunte de Raoden para «dor-09-monta-la-ventana».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Una función de ventana tiene siempre la misma forma, y lo único que cambia es lo que",
  "va dentro del paréntesis:",
  "",
  "```sql",
  "funcion() OVER ( PARTITION BY ...  ORDER BY ...  )",
  "--               ^ los grupos      ^ el orden dentro del grupo",
  "```",
  "",
  "Y las dos partes de dentro son opcionales, lo que da cuatro combinaciones que hacen",
  "cuatro cosas distintas:",
  "",
  "```sql",
  "OVER ()                                -- la tabla entera, sin orden",
  "OVER (ORDER BY id)                      -- la tabla entera, acumulando",
  "OVER (PARTITION BY dia)                 -- por días, sin orden",
  "OVER (PARTITION BY dia ORDER BY id)     -- por días, acumulando dentro de cada día",
  "```",
  "",
  "Con `ROW_NUMBER` las dos primeras dan un número por fila del 1 al 16, y las dos",
  "últimas reinician la cuenta en cada día. Con `SUM` la diferencia es otra, y es el reto",
  "once.",
  "",
  "## Los dos ORDER BY",
  "",
  "Este es el enredo del reto y merece verlo escrito:",
  "",
  "```sql",
  "SELECT dia, monedas,",
  "       ROW_NUMBER() OVER (PARTITION BY dia ORDER BY id ASC) AS numero",
  "FROM ventas",
  "ORDER BY numero DESC;",
  "```",
  "",
  "Hay dos `ORDER BY` y no compiten:",
  "",
  "- **El de dentro del `OVER`** decide **qué número** le toca a cada fila. Aquí, por",
  "  `id`: la primera venta que se anotó ese día es la número 1.",
  "- **El de la última línea** decide **cómo se pinta** el resultado. Podrías pintarlo en",
  "  cualquier orden y los números serían los mismos.",
  "",
  "Cambiar el de dentro cambia los datos. Cambiar el de fuera solo cambia el orden en que",
  "los lees.",
  "",
  "## Una ventana no filtra ni agrupa",
  "",
  "Salen las dieciséis ventas, porque no hay `WHERE` ni `GROUP BY`. Eso es lo que la",
  "distingue de todo lo demás de El mercado: **una ventana anota, no resume**.",
  "",
  "Es lo que permite hacer lo que el reto siete hace en dos pasos: primero anotar todas",
  "las filas con su número, después quedarse con las que llevan el 1. Si la ventana",
  "filtrara sola, no habría manera de mirarla antes de decidir.",
  "",
  "## Cómo se plantea",
  "",
  "Al escribir una ventana, di la frase en español y las piezas caen solas:",
  "",
  "> «Para cada **venta**, qué número hace **dentro de su día**, contando **de más caras",
  "> a más baratas**.»",
  "",
  "- «para cada venta» → no se agrupa: salen todas.",
  "- «dentro de su día» → `PARTITION BY dia`.",
  "- «de más caras a más baratas» → `ORDER BY monedas DESC`.",
  "",
  "Y después ejecútala **sin el filtro y sin el orden de fuera**, y mira la numeración",
  "entera. Ahí se ve si los grupos son los que creías, que es lo único que se puede",
  "equivocar aquí.",
  "",
  "## Pruébalo tú",
  "",
  "Quita el `PARTITION BY` y ejecuta. La numeración va del 1 al 16 de un tirón. Ahora",
  "cambia el `DESC` por `ASC` y mira quién es el número 1 de cada día: la venta más",
  "barata. Dos cambios de una palabra y tres resultados distintos, todos correctos para",
  "preguntas distintas.",
)
