/**
 * El apunte de Raoden para «muros-10-monta-la-union».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Una unión con filtro, entera, y con las piezas de este mundo ya colocadas donde van:",
  "",
  "```sql",
  "SELECT p.nombre AS puesto, p.monedas AS monedas",
  "FROM puestos AS p",
  "JOIN gremios AS g",
  "  ON p.gremio_id = g.id",
  "WHERE g.maestro = 'Adien'",
  "ORDER BY p.monedas DESC;",
  "```",
  "",
  "Seis líneas y cada una hace una cosa. Si sabes decir en voz alta qué hace cada una,",
  "ya sabes unir.",
  "",
  "## Filtrar por una columna de la otra tabla",
  "",
  "Aquí hay algo que no se podía hacer antes de saber unir: **filtrar por un dato que",
  "no está en la tabla que te interesa**.",
  "",
  "Lo que se quiere es una lista de puestos. Pero la condición -«que su gremio lo",
  "dirija Adien»- habla del **maestro** del gremio, y eso no está en `puestos`: está en",
  "`gremios`. Sin unir, no hay manera de escribir esa condición.",
  "",
  "Con la unión hecha, el `WHERE` puede hablar de las columnas de las dos tablas como",
  "si fueran una sola, porque a esas alturas ya lo son.",
  "",
  "## Por dónde filtrar, cuando hay dos maneras",
  "",
  "Se podría filtrar por el número en vez de por el nombre:",
  "",
  "```sql",
  "WHERE p.gremio_id = 1       -- funciona, y es peor",
  "WHERE g.nombre = 'escribas' -- lo mismo, y se entiende",
  "```",
  "",
  "Las dos devuelven lo mismo hoy. La primera obliga a quien la lea a saberse de",
  "memoria que el 1 son los escribas, y deja de ser verdad el día que alguien recoloque",
  "los identificadores. Los números internos son para que la base cosa las tablas, no",
  "para que las personas escriban condiciones con ellos.",
  "",
  "## Aquí un JOIN normal es lo correcto",
  "",
  "Fíjate en que el reto usa `JOIN` y no `LEFT JOIN`, y es a propósito: se piden los",
  "puestos **de un gremio concreto**. Un puesto sin gremio no es de los canteros, así",
  "que perderlo no es perder nada.",
  "",
  "Es la primera pregunta de siempre: ¿una fila sin pareja tiene que salir? Aquí, no.",
  "Y usar un `LEFT JOIN` aquí no sería más seguro: sería confuso, porque el `WHERE`",
  "sobre `g.nombre` iba a tirar esas filas de todas formas. Un `LEFT JOIN` cuyo efecto",
  "se deshace dos líneas más abajo es el fallo del reto ocho escrito a mano.",
  "",
  "## Cómo se plantea",
  "",
  "El orden en que conviene escribir una unión con filtro:",
  "",
  "1. `FROM` la tabla de la que quieres una fila por cosa.",
  "2. La unión y su `ON`. **Ejecuta y cuenta.**",
  "3. El `WHERE`. **Ejecuta y cuenta**: tienen que ser menos.",
  "4. El `ORDER BY`.",
  "5. Y al final el `SELECT` de verdad, con sus alias.",
  "",
  "El `SELECT` al final no es una rareza: mientras montas la unión da igual qué",
  "columnas pidas, y decidir los nombres del resultado cuando ya sabes qué filas salen",
  "es más fácil que al contrario.",
  "",
  "## Pruébalo tú",
  "",
  "Cambia `'canteros'` por `'aones'` y ejecuta. Cero filas, sin error: ese gremio",
  "existe y no tiene puestos. Un resultado vacío es una respuesta, no un fallo, y",
  "distinguir las dos cosas es media hora ahorrada cada vez.",
)
