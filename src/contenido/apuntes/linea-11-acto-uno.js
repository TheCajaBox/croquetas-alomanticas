/**
 * El apunte de Raoden para «linea-11-acto-uno».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Un aon al que le falta una línea no explota. No humea, no se rompe, no da error.",
  "Simplemente no hace nada, o hace otra cosa. Ese ha sido el hilo de los cinco mundos y este",
  "es el final.",
  "",
  "## El GROUP BY que falta",
  "",
  "```sql",
  "SELECT p.nombre AS puesto, COUNT(v.id) AS ventas, COALESCE(SUM(v.monedas), 0) AS total",
  "FROM puestos AS p",
  "LEFT JOIN ventas AS v ON v.puesto_id = p.id",
  "ORDER BY total DESC;",
  "```",
  "",
  "Sin `GROUP BY`, los agregados resumen **la tabla entera en una sola fila**. Dieciséis",
  "ventas, 1.275 monedas: los totales del mercado, y son correctos.",
  "",
  "Lo que no es correcto es `p.nombre`. En una consulta agrupada el `SELECT` solo puede llevar",
  "columnas del `GROUP BY` y agregados, y ahí no hay `GROUP BY`, así que `p.nombre` no es",
  "ninguna de las dos cosas.",
  "",
  "## Casi todas las bases dan error. SQLite no",
  "",
  "Y ahí está el remate. PostgreSQL para en seco -«column must appear in the GROUP BY",
  "clause»- y te ahorra el disgusto. SQLite te devuelve el valor de **una fila cualquiera** del",
  "montón, y no dice nada.",
  "",
  "Así que sale una fila con un nombre de verdad al lado de dos números de verdad, y no",
  "significan lo mismo: el nombre es de un puesto y los números son de nueve.",
  "",
  "Esa permisividad de SQLite está documentada y tiene su motivo -es útil en algunos casos",
  "muy concretos-, y significa que **el rigor lo tienes que poner tú**. En esta base, escribir",
  "una consulta agrupada correcta es una disciplina, no algo que se comprueba solo.",
  "",
  "## Por qué nadie lo vio",
  "",
  "Merece la pena repasar por qué esta consulta pasó una revisión, porque es la lección de",
  "verdad:",
  "",
  "- El `LEFT JOIN` está bien puesto, y es la pieza en la que todo el mundo se fija.",
  "- El `COUNT(v.id)` está bien y **no** es `COUNT(*)`: quien lo escribió sabía del fallo de",
  "  El mercado.",
  "- El `COALESCE` está bien: el hueco estaba decidido.",
  "- Y el `ORDER BY total DESC` **no chirría**, porque ordenar una fila funciona.",
  "",
  "Todo lo que se suele revisar estaba correcto. Lo que faltaba era una línea, y una línea que",
  "falta no se ve leyendo: se ve **contando las filas del resultado**.",
  "",
  "Nueve puestos, una fila. Ese es el único síntoma que había.",
  "",
  "## La comprobación que lo caza",
  "",
  "La primera de las tres: **cuenta las filas y compáralas con lo que esperabas**. Es la más",
  "tonta de las tres y es la que caza el fallo más difícil de ver del camino.",
  "",
  "Y hay una versión de bolsillo para las consultas agrupadas: **el número de filas del",
  "resultado es el número de valores distintos de la columna del `GROUP BY`**. Si no hay",
  "`GROUP BY`, es uno. Sabiendo eso, «una fila» delata la línea que falta sin mirar nada más.",
  "",
  "## Cómo se plantea",
  "",
  "Delante de cualquier consulta con un agregado dentro, dos preguntas:",
  "",
  "1. **¿Tiene `GROUP BY`?** Si no, el resultado es una fila. ¿Es eso lo que quieres?",
  "2. **¿Todas las columnas del `SELECT` son del `GROUP BY` o agregados?** Si alguna no lo",
  "   es, la consulta está mal aunque SQLite la acepte.",
  "",
  "Dos preguntas mecánicas, y con la segunda contestada esta consulta no se publica.",
  "",
  "## Y ahora el acto II",
  "",
  "Ya sabes cuál era la línea. Trazarla es lo de menos; el acto II pide el informe entero, con",
  "todo lo del camino dentro. No hay pistas y no hace falta: lo has escrito todo antes, en",
  "trozos.",
)
