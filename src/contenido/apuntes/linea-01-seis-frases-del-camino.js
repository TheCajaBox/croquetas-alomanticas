/**
 * El apunte de Raoden para «linea-01-seis-frases-del-camino».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "El camino entero en una página. No hay nada nuevo aquí: es la lista de lo que hay que",
  "tener a mano al escribir cualquier consulta.",
  "",
  "## Kae: preguntar",
  "",
  "```sql",
  "SELECT · FROM · WHERE · GROUP BY · HAVING · ORDER BY · LIMIT",
  "```",
  "",
  "Ese orden al escribir. Y el orden en que trabaja la base es otro -`FROM`, `WHERE`,",
  "`GROUP BY`, `HAVING`, `SELECT`, `ORDER BY`, `LIMIT`- y explica todo lo demás: por qué un",
  "`WHERE` no puede hablar de una suma, por qué un `HAVING` sí, y por qué el `ORDER BY`",
  "puede usar los alias y el `WHERE` no.",
  "",
  "Y las dos que se olvidan: **sin `ORDER BY` no hay orden prometido**, y `SELECT *` es",
  "pedir lo que todavía no existe.",
  "",
  "## Dentro de los muros: unir",
  "",
  "Un `JOIN` sin `ON` multiplica. Un `JOIN` normal pierde las filas sin pareja. Un `LEFT",
  "JOIN` las conserva **y** una condición sobre la tabla de la derecha en el `WHERE` deshace",
  "ese trabajo: va en el `ON`.",
  "",
  "Y al revés, que es lo que se olvida: una condición sobre la tabla de la **izquierda** va",
  "en el `WHERE`, porque en el `ON` no filtraría nada.",
  "",
  "## El mercado: resumir",
  "",
  "Agrupar **no inventa filas**: solo reparte las que hay. Un puesto sin ventas no tiene",
  "montón en la tabla de ventas, así que no sale ni con un cero.",
  "",
  "Y las dos que muerden: `COUNT(*)` cuenta filas -y después de un `LEFT JOIN` cuenta la",
  "fila vacía-, y `SUM` con nada que sumar devuelve **nulo**, no cero.",
  "",
  "## El Dor: lo que da poder",
  "",
  "Un nulo es «no lo sé». De ahí sale todo: que no empareje, que `= NULL` no encuentre",
  "nada, que una desigualdad deje fuera los huecos, y que un `NOT IN` con un nulo dentro no",
  "devuelva **nunca** ninguna fila.",
  "",
  "Y las herramientas: subconsultas, `WITH` para nombrar pasos -que cambia la unidad de la",
  "fila-, y ventanas para calcular sin agrupar.",
  "",
  "## Los trazos: escribir",
  "",
  "Sin `WHERE` se aplica a todo. La costumbre: el `SELECT` primero y **contar las filas**.",
  "Las reglas van en el esquema porque ahí las cumple todo el mundo. Y los índices se ponen",
  "mirando el plan, no por si acaso.",
  "",
  "## Cómo se plantea: las cuatro preguntas",
  "",
  "Antes de escribir cualquier consulta, cuatro cosas por escrito:",
  "",
  "1. **De qué quiero una fila.** Eso decide la tabla del `FROM` y el `GROUP BY`.",
  "2. **Si las filas sin pareja tienen que salir.** Eso decide `JOIN` o `LEFT JOIN`.",
  "3. **Qué columnas pueden tener nulos.** Eso decide qué `COUNT` usar y qué huecos tapar.",
  "4. **Si el orden importa.** Si importa, se pide.",
  "",
  "Con esas cuatro contestadas, la consulta está escrita. La mitad de los informes mal",
  "hechos que vas a ver son la respuesta equivocada a la primera.",
  "",
  "## Pruébalo tú",
  "",
  "Coge cualquiera de tus consultas de los mundos anteriores y contéstale las cuatro",
  "preguntas por escrito. Si alguna no sabes contestarla mirando la consulta, es que la",
  "consulta no dice lo que crees.",
)
