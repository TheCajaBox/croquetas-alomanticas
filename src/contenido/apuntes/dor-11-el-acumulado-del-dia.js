/**
 * El apunte de Raoden para «dor-11-el-acumulado-del-dia».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Las funciones de ventana no solo numeran. **Cualquier agregado puede llevar un**",
  "**`OVER`**, y entonces en vez de resumir el montón, acompaña a cada fila.",
  "",
  "```sql",
  "SUM(monedas) OVER (ORDER BY id)",
  "```",
  "",
  "Eso es un acumulado, y sale gratis. La ventana no es todo: con un `ORDER BY` dentro,",
  "es **«desde la primera fila hasta esta»**, y va creciendo.",
  "",
  "## Con y sin ORDER BY dentro del OVER",
  "",
  "La diferencia es total y es la única cosa nueva de hoy:",
  "",
  "```sql",
  "SUM(monedas) OVER ()                -- 1275 en todas las filas: el total",
  "SUM(monedas) OVER (ORDER BY id)      -- 40, 100, 140, ... 1275: el acumulado",
  "```",
  "",
  "Sin orden, la ventana es el montón entero y la suma es la misma en cada fila -que",
  "también sirve, para calcular porcentajes sobre el total sin una subconsulta-. Con",
  "orden, la ventana crece fila a fila.",
  "",
  "Y añadiendo `PARTITION BY`, el acumulado **se reinicia** en cada grupo:",
  "",
  "```sql",
  "SUM(monedas) OVER (PARTITION BY dia ORDER BY id)",
  "```",
  "",
  "La última fila de cada día trae el total de ese día, y todas las de por medio traen",
  "cuánto se llevaba hasta ese momento. Es el informe que pide cualquiera que lleve una",
  "caja.",
  "",
  "## Lo que hay debajo: el marco",
  "",
  "Esto no hace falta para resolver el reto y explica por qué funciona. La ventana tiene",
  "un **marco**, y con un `ORDER BY` el marco por omisión es:",
  "",
  "```sql",
  "ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW",
  "```",
  "",
  "«Desde el principio hasta esta fila». Eso es lo que hace que la suma acumule. Y se",
  "puede cambiar:",
  "",
  "```sql",
  "-- La media de esta venta y las dos anteriores: una media móvil.",
  "AVG(monedas) OVER (ORDER BY id ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)",
  "```",
  "",
  "Con eso se hacen medias móviles y comparaciones con la fila de al lado sin unir la",
  "tabla consigo misma. No hace falta hoy; conviene saber que existe, porque el día que",
  "necesites una media de siete días vas a saber que se pide en una línea.",
  "",
  "## La comprobación que cuadra siempre",
  "",
  "Un acumulado bien hecho tiene dos propiedades que se miran en un segundo:",
  "",
  "1. **La primera fila vale lo que ella misma.**",
  "2. **La última vale el total.** Y si hay `PARTITION BY`, la última de cada grupo vale",
  "   el total de su grupo, y esos totales suman el total general.",
  "",
  "Si alguna de las dos no cuadra, el `ORDER BY` de dentro no es el que creías, o falta.",
  "",
  "## Cómo se plantea",
  "",
  "Un acumulado necesita un orden **estable**. Si ordenas por una columna que se repite",
  "-el día, por ejemplo- el acumulado dentro de las filas empatadas no está determinado,",
  "y en algunas bases suma todas las empatadas de golpe.",
  "",
  "Así que ordena por algo que no se repita: el `id`, o la fecha con el `id` detrás. Es la",
  "misma regla de siempre, y en un acumulado se nota más porque los números intermedios",
  "cambian aunque el total no.",
  "",
  "## Pruébalo tú",
  "",
  "Añade una tercera columna: `SUM(monedas) OVER ()`. Las mismas 1.275 en las dieciséis",
  "filas. Con esa y el acumulado juntos puedes calcular qué porcentaje del mercado se",
  "llevaba hecho en cada venta, y eso en una consulta plana necesitaría dos subconsultas.",
)
