/**
 * El apunte de Kelsier para «tripulacion-01-decidir».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Hasta ahora tu código iba de arriba abajo haciendo siempre lo mismo. Un `if` es lo",
  "que le permite mirar antes de actuar, y con eso ya es un programa y no una lista.",
  "",
  "```php",
  "if ($cantidad >= 100) {",
  "    echo 'mucho';",
  "} elseif ($cantidad >= 20) {",
  "    echo 'suficiente';",
  "} else {",
  "    echo 'poco';",
  "}",
  "```",
  "",
  "## El orden de los tramos, que es lo que se falla",
  "",
  "Las condiciones se miran **de arriba abajo y se para en la primera que se cumple**. Con",
  "los tramos al revés:",
  "",
  "```php",
  "if ($cantidad >= 20) { echo 'suficiente'; }      // 500 entra aquí",
  "elseif ($cantidad >= 100) { echo 'mucho'; }      // y no llega nunca",
  "```",
  "",
  "500 es mayor que 20, así que se queda en el primer tramo y la segunda línea es código",
  "muerto. La regla: **del caso más estrecho al más amplio**, o del número más alto al más",
  "bajo.",
  "",
  "## Con `return` no hace falta `else`",
  "",
  "Dentro de una función, `return` termina la función ahí mismo. Así que esto:",
  "",
  "```php",
  "if ($cantidad >= 100) {",
  "    return 'mucho';",
  "}",
  "if ($cantidad >= 20) {",
  "    return 'suficiente';",
  "}",
  "return 'poco';",
  "```",
  "",
  "hace lo mismo que la versión con `elseif` y se lee mejor: cada caso es una frase corta y",
  "no hay que seguir la pista de dónde acaba cada llave. Se llama «salir pronto» y es la",
  "costumbre que más aclara el código de decisiones.",
  "",
  "## Los comparadores",
  "",
  "```php",
  "$a > $b     $a >= $b     $a < $b     $a <= $b",
  "$a === $b   $a !== $b",
  "```",
  "",
  "Y para juntar condiciones:",
  "",
  "- `&&` — las dos cosas.",
  "- `||` — una de las dos, o las dos.",
  "- `!` — lo contrario.",
  "",
  "```php",
  "if ($bruma && $vigilantes < 3) { ... }",
  "if (!$vistos) { ... }",
  "```",
  "",
  "`&&` y `||` tienen una propiedad que se aprovecha mucho: si la izquierda ya decide el",
  "resultado, **la derecha no se ejecuta**. Con `false && algo()`, `algo()` no se llama. Eso",
  "permite escribir `if (isset($x) && $x > 0)` sin miedo.",
  "",
  "## Cómo se plantea",
  "",
  "Escribe los casos como una lista antes de escribir código:",
  "",
  "```",
  "100 o más     -> mucho",
  "20 a 99       -> suficiente",
  "1 a 19        -> poco",
  "0 o menos     -> nada",
  "```",
  "",
  "Con la lista delante, el orden sale solo y los bordes también: fíjate en que 100 y 20",
  "están **dentro** de su tramo, así que van con `>=` y no con `>`. La mitad de los fallos de",
  "condiciones son ese carácter.",
  "",
  "## Los casos límite",
  "",
  "Los bordes exactos —el 100, el 20, el 0— y el negativo. Pruébalos siempre: son donde vive",
  "el error, y son cuatro números.",
)
