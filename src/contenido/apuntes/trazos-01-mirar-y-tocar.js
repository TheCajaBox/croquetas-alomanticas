/**
 * El apunte de Raoden para «trazos-01-mirar-y-tocar».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Cuatro mundos leyendo. A partir de aquí se escribe, y hay una diferencia que conviene",
  "sentir antes de empezar:",
  "",
  "> Un `SELECT` mal escrito **devuelve** otra cosa. Un `DELETE` mal escrito **borra** otra",
  "> cosa.",
  "",
  "Lo primero se arregla mirando otra vez. Lo segundo, no.",
  "",
  "## Las tres palabras que tocan",
  "",
  "```sql",
  "INSERT INTO tabla (columnas) VALUES (valores);   -- mete filas nuevas",
  "UPDATE tabla SET columna = valor WHERE …;         -- cambia filas que ya están",
  "DELETE FROM tabla WHERE …;                        -- borra filas",
  "```",
  "",
  "Fíjate en las tres preposiciones, que se confunden y no se pueden cambiar: `INSERT`",
  "**INTO**, `DELETE` **FROM**, y `UPDATE` sin nada.",
  "",
  "## El WHERE no es opcional. Lo es para la base",
  "",
  "```sql",
  "UPDATE puestos SET abierto = 0;    -- cierra los seis",
  "DELETE FROM ventas;                 -- vacía la tabla",
  "```",
  "",
  "Las dos son órdenes válidas y la base las ejecuta sin pestañear, porque hay veces en",
  "que cambiar o vaciar una tabla entera es lo que se quiere. No hay manera de que la base",
  "sepa que se te ha olvidado media línea.",
  "",
  "De aquí sale **la costumbre del mundo**, y es la que hay que llevarse aunque se olvide",
  "todo lo demás:",
  "",
  "```sql",
  "-- 1. Escribe la condición en un SELECT y mira las filas.",
  "SELECT * FROM puestos WHERE gremio_id = 2;",
  "",
  "-- 2. Solo entonces, cambia el verbo.",
  "UPDATE puestos SET abierto = 0 WHERE gremio_id = 2;",
  "```",
  "",
  "Cuesta cinco segundos y caza el noventa por ciento de los accidentes. Y con un dato de",
  "más: **cuenta las filas**. Si el `SELECT` devuelve más de las que esperabas, la",
  "condición no es la que creías.",
  "",
  "## Lo que devuelve una orden que escribe",
  "",
  "No devuelve filas: devuelve **cuántas ha cambiado**. Eso sale en la consola de este",
  "juego -«3 filas cambiadas»- y es lo primero que hay que mirar después de ejecutar.",
  "",
  "Es la única señal que da la base, y es buena: si esperabas una y dice tres, acabas de",
  "enterarte a tiempo... siempre que estuvieras dentro de una transacción. Fuera de una,",
  "enterarse a tiempo no sirve de nada.",
  "",
  "## Las tablas ahora traen reglas",
  "",
  "Mira el esquema de este mundo: `NOT NULL`, `UNIQUE`, `CHECK`, claves ajenas. Son reglas",
  "escritas en la tabla y **las hace cumplir la base**.",
  "",
  "Eso cambia lo que pasa cuando te equivocas: en vez de guardar un dato absurdo, la orden",
  "falla. Y ahí está la gracia, que tiene su propio reto: es mucho mejor un error que un",
  "dato roto.",
  "",
  "## Pruébalo tú",
  "",
  "Ejecuta un `SELECT` cualquiera y mira la consola: sale la tabla y ninguna línea de",
  "filas cambiadas. Ahora un `UPDATE` con su `WHERE`, y mira el número. Es el único",
  "informe que vas a tener de aquí en adelante, así que conviene mirarlo siempre.",
)
