/**
 * El apunte de Wax para «elendel-02b-el-dato-que-no-venia».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Hay dos maneras de que un dato de fuera te arruine la tarde. La primera es que llegue mal",
  "escrito, y esa se resuelve con un `try`: el formato no cuela y te enteras al momento.",
  "",
  "La segunda es peor, porque no se queja: **el formato está impecable y el contenido no es",
  "el que tú suponías**. Una lista que hoy viene vacía. Un campo que ese día el servidor no",
  "mandó. Un objeto con una clave menos que ayer. Nada de eso es un error de JSON, así que",
  "`JSON.parse` te lo entrega con toda la naturalidad y el programa se cae tres líneas",
  "después, en un sitio que parece no tener nada que ver.",
  "",
  "## El problema que resuelve",
  "",
  "Escribes un programa con un ejemplo delante. El ejemplo tiene tres agentes, así que",
  "escribes esto y funciona:",
  "",
  "```js",
  "const alMando = parte.agentes[0]",
  "console.log(alMando.nombre)",
  "```",
  "",
  "Y un martes de agosto no hay nadie de servicio. La lista llega vacía, `parte.agentes[0]`",
  "vale `undefined` y el `.nombre` revienta. Tu código no ha cambiado; ha cambiado el dato.",
  "",
  "Lo que estabas haciendo, sin darte cuenta, era **describir el ejemplo en vez de describir",
  "el formato**. El formato dice «una lista de agentes», y una lista de cero elementos sigue",
  "siendo una lista de agentes.",
  "",
  "## La idea: preguntar por el camino",
  "",
  "Para eso están los dos operadores que ya conoces de los Áridos, y aquí es donde ganan su",
  "sueldo. `?.` recorre el camino y se para en cuanto encuentra un vacío:",
  "",
  "```js",
  "parte.agentes[0]?.nombre    // undefined, sin reventar",
  "parte.turno?.jefe?.nombre   // undefined aunque no exista ni `turno`",
  "```",
  "",
  "Y `??` pone algo en el hueco:",
  "",
  "```js",
  "parte.agentes[0]?.nombre ?? 'sin asignar'   // 'sin asignar'",
  "```",
  "",
  "Los dos juntos son la frase completa: «dame esto si está, y si no, esto otro». Sin `if`,",
  "sin comprobar tres niveles a mano, y en una línea que se lee del tirón.",
  "",
  "También sirve para llamadas y para listas, y eso se usa menos de lo que debería:",
  "",
  "```js",
  "parte.contar?.()            // llama solo si `contar` existe",
  "const lista = parte.agentes ?? []",
  "console.log(lista.length)   // 0, y no un error",
  "```",
  "",
  "Esa última línea es un patrón que merece la pena copiar tal cual: **si esperas una lista,",
  "asegúrate de tener una lista** antes de recorrerla. Un `.map` sobre `undefined` es el",
  "segundo error más frecuente de este mundo.",
  "",
  "## El modelo mental",
  "",
  "Un dato de fuera es la declaración de un testigo, no un hecho. Puede ser verdad, puede",
  "estar incompleta y puede haber cambiado desde ayer sin avisarte.",
  "",
  "Con esa idea en la cabeza, el código se escribe de otra manera: en el borde -en la línea",
  "donde el dato entra en tu programa- se comprueba y se le da forma; a partir de ahí, dentro,",
  "se puede confiar. Se le llama poner el dato en forma cuanto antes, y ahorra tener `?.`",
  "sembrado por todo el archivo.",
  "",
  "```js",
  "// En el borde, una sola vez:",
  "const agentes = parte.agentes ?? []",
  "const ciudad = parte.ciudad ?? 'desconocida'",
  "",
  "// Y de aquí para adentro, ya son un array y un texto de verdad.",
  "```",
  "",
  "## Comprobar la forma, no solo el valor",
  "",
  "`??` te salva de la ausencia. No te salva de que el dato **esté y sea otra cosa**, que",
  "pasa más de lo que parece cuando al otro lado hay un programa escrito por otra persona:",
  "",
  "```js",
  "const crudo = JSON.parse('{ \"agentes\": \"tres\" }')",
  "",
  "console.log((crudo.agentes ?? []).length)   // 4  ← son las letras de «tres»",
  "```",
  "",
  "Ahí `??` no ha saltado, porque había algo: había un texto donde tenía que haber una lista.",
  "Y los textos también tienen `length`, así que la cuenta sale y sale mal.",
  "",
  "Para eso hay una pregunta concreta, y no vale `typeof`:",
  "",
  "```js",
  "console.log(typeof [])            // 'object'  ← inservible",
  "console.log(Array.isArray([]))    // true",
  "console.log(Array.isArray({}))    // false",
  "```",
  "",
  "`typeof` dice `object` para las listas, para los objetos y hasta para `null`, así que para",
  "distinguir una lista hay una función aparte. Con ella, poner el dato en forma es una línea:",
  "",
  "```js",
  "const agentes = Array.isArray(crudo.agentes) ? crudo.agentes : []",
  "```",
  "",
  "## Lo que despista",
  "",
  "**`??` y `||` no son lo mismo**, y la diferencia se paga con dinero:",
  "",
  "```js",
  "const ficha = { casos: 0, nota: '' }",
  "",
  "console.log(ficha.casos ?? 99)   // 0",
  "console.log(ficha.casos || 99)   // 99  ← ¡ojo!",
  "```",
  "",
  "`||` salta con todo lo que se parezca a falso: el cero, el texto vacío, el `false`. Así que",
  "un agente con cero casos aparece con noventa y nueve. `??` solo salta cuando de verdad no",
  "hay nada. Para valores por defecto, siempre `??`.",
  "",
  "**`?.` no arregla el error, lo aplaza.** Si el dato tenía que estar y no está, seguir",
  "adelante con un `undefined` a cuestas no es mejor que pararse: es peor, porque el fallo",
  "aparecerá más lejos. Úsalo donde la ausencia sea **legítima** —un campo opcional, una lista",
  "que puede venir vacía—, y donde no lo sea, quéjate a propósito.",
  "",
  "**Salirse de una lista no es un error.** En otros lenguajes te para; aquí `lista[99]` es",
  "`undefined` y sigue. Cómodo, y una fuente inagotable de fallos silenciosos.",
  "",
  "## Pruébalo tú",
  "",
  "```js",
  "const parte = JSON.parse('{ \"ciudad\": \"Elendel\", \"agentes\": [] }')",
  "",
  "console.log(parte.agentes.length)",
  "console.log(parte.agentes[0])",
  "console.log(parte.agentes[0]?.nombre ?? 'sin asignar')",
  "console.log(parte.jefe?.nombre)",
  "```",
  "",
  "Sale `0`, luego `undefined`, luego `sin asignar`, y luego `undefined`.",
  "",
  "Fíjate en la primera: `agentes` **sí** existe, así que preguntarle cuántos hay funciona",
  "perfectamente y da cero. El problema nunca fue la lista; fue lo que se suponía que había",
  "dentro.",
  "",
  "Y en la última: `jefe` no existe en absoluto, y con `?.` eso no revienta. Sin el `?.`, esa",
  "línea sería el mismo `TypeError` de siempre.",
  "",
  "## Cómo se plantea",
  "",
  "Cuando llegue un `TypeError` de los de «no puedo leer propiedades de undefined»:",
  "",
  "1. **Lee qué propiedad pedía.** Está entre paréntesis en el mensaje. Eso te da la línea.",
  "2. **Pregunta por el dueño, no por la propiedad.** El fallo no es de `nombre`: es de la",
  "   cosa a la que le pides `nombre`. Imprímela.",
  "3. **Ve hacia atrás hasta el borde.** ¿De dónde salió ese dato? Casi siempre de un `parse`,",
  "   de una respuesta o de un archivo, y ahí es donde hay que ponerle forma.",
  "4. **Decide si la ausencia es normal.** Si lo es, `?.` y `??` con un valor por defecto que",
  "   signifique algo. Si no lo es, para el programa con un mensaje que explique qué faltaba,",
  "   que es mucho más útil que dejarlo seguir cojo.",
)
