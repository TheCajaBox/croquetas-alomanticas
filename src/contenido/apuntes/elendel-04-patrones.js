/**
 * El apunte de Wax para «elendel-04-patrones».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "A veces no sabes qué buscas, solo **qué forma tiene**. Tres dígitos seguidos. Una palabra en",
  "mayúsculas. Algo con arroba en medio. Para eso están las expresiones regulares.",
  "",
  "Tienen fama de ilegibles y se la merecen a partir de cierto tamaño. Pero el ochenta por",
  "ciento de lo que se usa a diario cabe en media docena de símbolos.",
  "",
  "## Los símbolos que hacen falta",
  "",
  "| Escribes | Significa |",
  "|---|---|",
  "| `\\d` | un dígito |",
  "| `\\w` | una letra, dígito o guion bajo |",
  "| `\\s` | un espacio, tabulador o salto de línea |",
  "| `.` | cualquier carácter |",
  "| `+` | uno o más del anterior |",
  "| `*` | cero o más |",
  "| `?` | cero o uno: opcional |",
  "| `[abc]` | una de esas |",
  "| `[^abc]` | cualquiera menos esas |",
  "| `^` `$` | principio y final del texto |",
  "| `(...)` | **un grupo: lo que te quieres quedar** |",
  "",
  "Con eso ya se lee `/recompensa: (\\d+)/`: la palabra «recompensa», dos puntos, un espacio, y",
  "uno o más dígitos que además me quiero quedar.",
  "",
  "## Las tres formas de usarla",
  "",
  "**¿Está?** `test` devuelve verdadero o falso:",
  "",
  "```js",
  "/\\d/.test('SE BUSCA: Miles')      // false",
  "```",
  "",
  "**¿Qué había?** `match` devuelve lo encontrado, o `null`:",
  "",
  "```js",
  "const encontrado = 'recompensa: 500 marcos'.match(/recompensa: (\\d+)/)",
  "encontrado[0]    // 'recompensa: 500'  ← todo lo que encajó",
  "encontrado[1]    // '500'              ← lo del primer paréntesis",
  "```",
  "",
  "Ese `null` cuando no hay nada es la mitad del trabajo: no se puede leer `[1]` sin comprobar",
  "antes.",
  "",
  "**Cambiarlo:** `replace`, y con `$1` se puede reutilizar el grupo:",
  "",
  "```js",
  "'recompensa: 500'.replace(/(\\d+)/, '$1 marcos')     // 'recompensa: 500 marcos'",
  "```",
  "",
  "## Lo que sale es texto, siempre",
  "",
  "```js",
  "const encontrado = '(recompensa: 500)'.match(/(\\d+)/)",
  "encontrado[1]        // '500', con comillas",
  "encontrado[1] + 1    // '5001'",
  "Number(encontrado[1]) // 500",
  "```",
  "",
  "Una expresión regular trabaja sobre texto y devuelve texto. Si querías el número, hay que",
  "convertirlo, y olvidarlo da el clásico `'5001'` en vez de `501`.",
  "",
  "## Grupos con nombre, que se leen",
  "",
  "Cuando hay más de un grupo, acordarse de si el año era el `[2]` o el `[3]` es una fuente de",
  "erratas. Se les puede poner nombre:",
  "",
  "```js",
  "const patron = /(?<dia>\\d{2})-(?<mes>\\d{2})-(?<anio>\\d{4})/",
  "const { groups } = '14-03-2015'.match(patron)",
  "groups.anio      // '2015'",
  "```",
  "",
  "Más largo de escribir y mucho más fácil de leer seis meses después. `\\d{2}` es «exactamente",
  "dos dígitos», por cierto: las llaves sirven para decir cuántos.",
  "",
  "## Las banderas",
  "",
  "Van después de la barra final y cambian cómo se busca:",
  "",
  "```js",
  "/wax/i          // i: da igual mayúsculas y minúsculas",
  "/\\d+/g          // g: todas las apariciones, no solo la primera",
  "```",
  "",
  "Con `g`, `match` cambia de comportamiento: devuelve **todos los trozos encontrados** y",
  "**pierde los grupos**. Si necesitas las dos cosas, `matchAll`:",
  "",
  "```js",
  "const todos = [...texto.matchAll(/recompensa: (\\d+)/g)]",
  "todos.map((uno) => Number(uno[1]))",
  "```",
  "",
  "## Lo que despista",
  "",
  "**El punto significa «cualquier cosa».** Para buscar un punto de verdad hay que escaparlo:",
  "`\\.`. Igual con `?`, `+`, `(`, `[` y `$`.",
  "",
  "**En un texto normal, la barra se dobla.** `new RegExp('\\\\d+')` lleva dos barras porque el",
  "texto se procesa antes. Con `/\\d+/` directamente, una.",
  "",
  "**`+` es glotón.** `/\".+\"/` sobre `\"a\" y \"b\"` se lleva desde la primera comilla hasta la",
  "última, no el primer par. Se arregla con `+?`, que es la versión perezosa, o siendo más",
  "específico: `/\"[^\"]+\"/`.",
  "",
  "**No sirven para todo.** Para HTML no, para JSON no, para direcciones de correo casi",
  "tampoco: el patrón que valida un correo de verdad es célebre por tener cientos de",
  "caracteres. Para datos con estructura, usa un analizador de verdad; las expresiones",
  "regulares son para sacar trozos de texto plano.",
  "",
  "**Escribe siempre lo que hace al lado.** Una expresión regular de veinte caracteres es",
  "ilegible al mes. Un comentario de una línea encima cuesta cinco segundos.",
  "",
  "## Pruébalo tú",
  "",
  "¿Qué devuelve cada una?",
  "",
  "```js",
  "const cartel = 'SE BUSCA: Miles Dagouter (recompensa: 500 marcos)'",
  "",
  "console.log(cartel.match(/\\d+/)[0])",
  "console.log(cartel.match(/[A-Z]+/)[0])",
  "console.log(cartel.match(/\\((.+)\\)/)[1])",
  "console.log(cartel.match(/oro/))",
  "```",
  "",
  "Sale `'500'`, luego `'SE'`, luego `'recompensa: 500 marcos'`, y luego `null`.",
  "",
  "La segunda se para en el espacio, porque el espacio no es `[A-Z]`. La tercera enseña dos",
  "cosas: los paréntesis escapados buscan paréntesis de verdad, y los de dentro son el grupo.",
  "Y la cuarta es el recordatorio de siempre — cuando no hay nada, `null`, y leerle `[0]` a un",
  "`null` revienta.",
  "",
  "## Cómo se plantea",
  "",
  "1. **Escribe tres ejemplos que tienen que encajar y dos que no.** Antes de escribir el",
  "   patrón. Los que no encajan son los que evitan que te pases de listo.",
  "2. **Empieza por lo literal.** Escribe el texto tal cual y ve sustituyendo por símbolos solo",
  "   lo que varía.",
  "3. **Pon paréntesis solo alrededor de lo que te quieres quedar.**",
  "4. **Comprueba el caso en que no hay nada.** `match` devuelve `null` y ese es el camino que",
  "   nadie prueba.",
  "5. **Y pregúntate si de verdad hace falta.** `texto.includes('recompensa')` se lee mucho",
  "   mejor que `/recompensa/.test(texto)`, y `split` resuelve más casos de los que parece.",
)
