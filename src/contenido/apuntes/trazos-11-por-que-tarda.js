/**
 * El apunte de Raoden para «trazos-11-por-que-tarda».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Todo ha contestado al instante durante cinco mundos porque hay seis filas. Con seis",
  "millones, la diferencia entre una consulta pensada y una no pensada son horas.",
  "",
  "Y no se adivina. **Se pregunta.**",
  "",
  "```sql",
  "EXPLAIN QUERY PLAN SELECT nombre FROM puestos WHERE gremio_id = 1;",
  "```",
  "",
  "## Las dos palabras que hay que saber leer",
  "",
  "- **`SCAN tabla`** — recorre la tabla entera, fila por fila. Con seis filas es",
  "  instantáneo; con seis millones son seis millones de filas leídas.",
  "- **`SEARCH tabla USING INDEX …`** — va directo. Mira unas pocas y para.",
  "",
  "Hay más -`COVERING INDEX` significa que el índice tenía ya todo lo que se pedía y no ha",
  "hecho falta ni abrir la tabla; `USE TEMP B-TREE` significa que ha tenido que ordenar",
  "aparte- y con esas dos se entiende el ochenta por ciento de los planes.",
  "",
  "## Qué es un índice",
  "",
  "Una copia ordenada de una columna que la base mantiene al día. Como el índice de un libro:",
  "para buscar «gremio 1» no hace falta leer el libro, se mira el índice y se va a la página.",
  "",
  "```sql",
  "CREATE INDEX idx_puestos_gremio ON puestos(gremio_id);",
  "```",
  "",
  "El nombre lo pones tú, y la costumbre es que diga de qué es: tabla y columna. Los vas a",
  "leer en los planes, así que que se entiendan importa.",
  "",
  "## Lo que ya tienes sin pedirlo, y lo que no",
  "",
  "Esto es lo que más se equivoca, y es lo que hace que la gente cree índices que no sirven:",
  "",
  "- **La clave primaria ya tiene índice.** Buscar por `id` va directo desde el primer día.",
  "- **Cada `UNIQUE` trae el suyo.** La base necesita un índice para poder comprobar la",
  "  restricción, así que lo crea sola: son los `sqlite_autoindex_…` que salen en los planes.",
  "  Y un `UNIQUE (puesto_id, dia)` sirve además para buscar por `puesto_id` a secas, porque",
  "  es la primera columna del índice. **Crear otro sobre `puesto_id` sería trabajo y espacio",
  "  de más sin ganar nada, y nadie te lo diría.**",
  "- **Las claves ajenas NO.** SQLite no les pone índice por su cuenta, y esa es la causa",
  "  número uno de `JOIN` lentos: la tabla hija se recorre entera en cada unión.",
  "",
  "Así que si hay un índice que poner casi a ciegas, es el de las claves ajenas. Los demás,",
  "con el plan delante.",
  "",
  "## Lo que cuesta",
  "",
  "Un índice **acelera las lecturas que puedan usarlo y encarece todas las escrituras**,",
  "porque hay que mantenerlo al día en cada `INSERT`, `UPDATE` y `DELETE`. Y ocupa espacio.",
  "",
  "Una tabla con ocho índices que nadie usa es una tabla en la que escribir cuesta ocho veces",
  "más de lo necesario. De ahí la regla: **no se ponen por si acaso**.",
  "",
  "Y hay una manera de saber si uno sirve, que es la única honrada: mirar el plan de la",
  "consulta que va lenta, antes y después.",
  "",
  "## Cuándo un índice no se usa aunque exista",
  "",
  "Conviene saber estos tres, porque son horas perdidas:",
  "",
  "- Si la columna va dentro de una función: `WHERE UPPER(nombre) = 'AON AON'` no puede usar",
  "  un índice sobre `nombre`.",
  "- Si el `LIKE` empieza por comodín: `LIKE '%piedra%'` no tiene por dónde empezar.",
  "- Si la base calcula que recorrer la tabla sale más barato, cosa que con pocas filas es",
  "  verdad. Con seis filas puede ignorar tu índice, y hace bien.",
  "",
  "## Cómo se plantea",
  "",
  "1. Una consulta va lenta. **Mide** -no supongas cuál-.",
  "2. `EXPLAIN QUERY PLAN` delante. Si dice `SCAN` sobre una tabla grande, ahí está.",
  "3. Mira **qué columna** hay en el `WHERE` o en el `ON`. Esa es la candidata.",
  "4. Crea el índice y **vuelve a mirar el plan**. Si sigue diciendo `SCAN`, el índice no",
  "   servía: bórralo.",
  "",
  "El paso cuatro es el que casi nadie hace, y es el que distingue poner un índice de",
  "adornar la tabla.",
  "",
  "## Pruébalo tú",
  "",
  "Pon `EXPLAIN QUERY PLAN` delante de una consulta que use `ORDER BY` sin índice y busca el",
  "`USE TEMP B-TREE`: eso es la base ordenando aparte porque no tenía nada ordenado a mano.",
  "Ahora crea un índice sobre esa columna y vuelve a mirar.",
)
