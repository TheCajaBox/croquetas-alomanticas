/**
 * El apunte de Kelsier para «ceniza-07-var-dump».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Cuando algo no hace lo que esperabas, hay dos maneras de averiguar por qué: mirar",
  "el código durante veinte minutos, o pedirle a PHP que te enseñe lo que tiene",
  "dentro. La segunda es más rápida y se hace con `var_dump`.",
  "",
  "## Cómo se lee",
  "",
  "El formato es `tipo(contenido)`:",
  "",
  "```php",
  "var_dump(3);          // int(3)",
  "var_dump(2.5);        // float(2.5)",
  "var_dump('3');        // string(1) \"3\"",
  "var_dump('');         // string(0) \"\"",
  "var_dump(true);       // bool(true)",
  "var_dump(null);       // NULL",
  "```",
  "",
  "En los textos el número es **cuántos caracteres tiene**. Ese número delata cosas",
  "que no se ven: un `string(7) \"peltre\"` te está diciendo que hay un espacio de más",
  "en algún sitio.",
  "",
  "## `echo` no vale para esto",
  "",
  "```php",
  "echo '3';        // 3",
  "echo 3;          // 3",
  "echo true;       // 1",
  "echo null;       //          <- nada, ni un carácter",
  "```",
  "",
  "Con `echo` las cuatro cosas se ven igual o casi, y son cuatro cosas distintas.",
  "Para imprimir, `echo`. Para investigar, `var_dump`.",
  "",
  "## `==` y `===`, que es lo que se practica aquí",
  "",
  "```php",
  "var_dump(3 == '3');    // bool(true)",
  "var_dump(3 === '3');   // bool(false)",
  "```",
  "",
  "- `==` pregunta «¿valen lo mismo?» y se permite convertir para averiguarlo.",
  "- `===` pregunta «¿valen lo mismo **y** son del mismo tipo?».",
  "",
  "La costumbre buena es usar `===` por defecto y `==` solo cuando de verdad quieras",
  "esa conversión. Cuesta una tecla más y quita una clase entera de fallos.",
  "",
  "## Cómo se plantea una predicción",
  "",
  "No lo leas de arriba abajo intentando adivinar el total. Ve variable por variable:",
  "primero apunta de qué tipo es cada una, y solo después escribe la línea que",
  "saldría. Los fallos de predicción casi nunca son de lógica: son de haberse",
  "saltado un tipo.",
  "",
  "## Los casos límite",
  "",
  "`var_dump(null)` imprime `NULL`, en mayúsculas y sin paréntesis: es el único que",
  "rompe el formato, porque no hay contenido que enseñar. Y `var_dump` de un array",
  "imprime varias líneas con todo lo que hay dentro, indentado; muy útil, algo más",
  "largo de leer.",
  "",
  "## Pruébalo tú",
  "",
  "La próxima vez que algo salga raro, en vez de mirar el código pon un `var_dump`",
  "justo antes y mira. Es la costumbre que más tiempo ahorra de todas las que hay en",
  "este mundo.",
)
