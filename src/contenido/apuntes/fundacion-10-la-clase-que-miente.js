/**
 * El apunte de Elend para «fundacion-10-la-clase-que-miente».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Este código se probó con `new Expedicion(...)` y funcionó. En cuanto alguien heredó de ella,",
  "dejó de funcionar. Los dos fallos son los dos que más se repiten heredando, y los dos",
  "aparecen **solo en la hija**: por eso pasaron las pruebas.",
  "",
  "## Uno: el constructor de la madre no se ejecuta",
  "",
  "```php",
  "class ExpedicionUrgente extends Expedicion",
  "{",
  "    public function __construct(public int $prioridad) {}",
  "}",
  "```",
  "",
  "Ese constructor **sustituye** al de la madre. Así que `$nombre` y `$sacos` no se rellenan",
  "nunca: quedan sin inicializar, y leerlas lanza un error de los que sí paran el programa.",
  "",
  "El arreglo es recibirlas y pasarlas:",
  "",
  "```php",
  "public function __construct(string $nombre, int $sacos, public int $prioridad)",
  "{",
  "    parent::__construct($nombre, $sacos);",
  "}",
  "```",
  "",
  "Fíjate en que `$nombre` y `$sacos` **no** llevan visibilidad aquí: no son propiedades de la",
  "hija, son datos de paso para la madre. Ponerles `public` crearía una segunda propiedad con",
  "el mismo nombre, y ahí ya no hay quien lo entienda.",
  "",
  "## Dos: `private` no lo ven las hijas",
  "",
  "```php",
  "// en la madre",
  "public function __construct(private string $nombre, private int $sacos) {}",
  "",
  "// en la hija",
  "return $this->nombre . ': ' . $this->sacos . '!';   // no puede",
  "```",
  "",
  "`private` es «solo esta clase», y una hija es otra clase. La visibilidad que abre la puerta",
  "a las hijas y la deja cerrada para el resto del programa es `protected`.",
  "",
  "Y hay un arreglo mejor que ese, que es el que se usa cuando se puede: **no tocar los datos",
  "de la madre**. Si la madre ya sabe hacer el resumen, la hija llama a `parent::resumen()` y",
  "le añade lo suyo. Entonces `private` puede quedarse como estaba y la hija no depende de cómo",
  "guarda la madre sus cosas.",
  "",
  "```php",
  "public function resumen(): string",
  "{",
  "    return parent::resumen() . '!';",
  "}",
  "```",
  "",
  "Esa es la solución de este reto, y la lección: **cuanto menos sepa la hija de la madre,",
  "mejor**. Cada propiedad `protected` es un trozo de la madre que ya no se puede cambiar sin",
  "mirar a las hijas.",
  "",
  "## Cómo se cazan estos dos",
  "",
  "Los dos dan **errores ruidosos**, no silenciosos, y eso es una suerte. El truco es leerlos:",
  "",
  "- «must not be accessed before initialization» → alguien no rellenó esa propiedad. Mira los",
  "  constructores, de la hija hacia arriba.",
  "- «Cannot access private property» → la visibilidad. Y dice desde dónde se intentaba.",
  "",
  "El error dice la propiedad y la línea. La costumbre de leerlo entero antes de tocar nada",
  "ahorra más tiempo que cualquier otra cosa de este oficio.",
  "",
  "## Los casos límite",
  "",
  "- **Si la hija no define constructor**, hereda el de la madre entero y no hay problema. El",
  "  fallo aparece justo al añadir uno.",
  "- **`parent::__construct()` va normalmente en la primera línea**, antes de tocar nada",
  "  propio: hasta que la madre no ha terminado, el objeto está a medias.",
  "- **`protected` sigue estando cerrado desde fuera.** Un test que compruebe eso es barato y",
  "  evita que alguien lo abra a `public` por comodidad.",
  "",
  "## Los casos límite del arreglo",
  "",
  "Después de arreglarlo, comprueba las tres cosas que el arreglo podría haber roto: que una",
  "expedición normal siga funcionando igual, que la urgente siga siendo una expedición, y que",
  "los datos sigan sin poderse tocar desde fuera. Un arreglo que abre la clase entera a",
  "`public` hace pasar el reto y estropea la lección.",
)
