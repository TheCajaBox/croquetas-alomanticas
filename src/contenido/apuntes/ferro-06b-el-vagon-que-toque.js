/**
 * El apunte de Wax para «ferro-06b-el-vagon-que-toque».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Un componente se escribe en la plantilla por su nombre, y ese nombre está clavado ahí para",
  "siempre. Eso vale para el noventa por ciento de lo que se escribe, y el diez por ciento",
  "restante es donde está toda la gracia de una aplicación: cuando **qué componente se pinta",
  "es un dato**, no una decisión escrita a mano.",
  "",
  "El router hace eso a lo grande, con una pantalla entera por dirección. Dentro de una",
  "pantalla hace falta lo mismo en pequeño, y para eso está `<component :is>`.",
  "",
  "## El problema que resuelve",
  "",
  "Un tablón con tres pestañas. Sin la herramienta, sale esto:",
  "",
  "```html",
  "<Horario v-if=\"cual === 'horario'\" />",
  "<Averias v-else-if=\"cual === 'averias'\" />",
  "<Retrasos v-else-if=\"cual === 'retrasos'\" />",
  "```",
  "",
  "Funciona. Y tiene tres problemas que crecen con el tiempo:",
  "",
  "- **La lista está en dos sitios.** Los botones de las pestañas también hay que escribirlos",
  "  uno a uno, y el día que se añade un panel hay que acordarse de los dos sitios.",
  "- **Cada opción nueva es una línea más de escalera.** Con ocho paneles son ocho `v-else-if`,",
  "  y todos dicen lo mismo con otro nombre.",
  "- **No se puede decidir desde fuera.** Si el panel que toca viene del servidor o de la",
  "  dirección, la escalera no sirve: hay que traducir un texto a un componente de todas",
  "  formas.",
  "",
  "## La idea: el mapa y la etiqueta",
  "",
  "Dos piezas. La primera es un objeto normal, fuera del componente, que empareja nombres con",
  "componentes:",
  "",
  "```js",
  "const PANELES = { horario: Horario, averias: Averias, retrasos: Retrasos }",
  "```",
  "",
  "Eso no tiene nada de Vue: es un objeto de toda la vida donde los valores resultan ser",
  "componentes. La segunda pieza es la etiqueta que pinta lo que le pases:",
  "",
  "```html",
  "<component :is=\"actual\" />",
  "```",
  "",
  "`<component>` no pinta nada propio. Es un hueco que se rellena con lo que valga `:is`, y",
  "acepta tres cosas distintas: un componente, el nombre de un componente registrado, o hasta",
  "el nombre de una etiqueta de HTML.",
  "",
  "```html",
  "<component :is=\"'h2'\" class=\"titulo\">hola</component>   <!-- pinta <h2 class=\"titulo\">hola</h2> -->",
  "```",
  "",
  "Esa última forma se usa poco y salva el día cuando hay que pintar un encabezado cuyo nivel",
  "depende de la profundidad.",
  "",
  "## Cómo se juntan",
  "",
  "Lo que el usuario elige es **un texto**; lo que se pinta es **un componente**. Son dos cosas",
  "y conviene tenerlas separadas: un `ref` para la elección y un `computed` para la traducción.",
  "",
  "```js",
  "const cual = ref('horario')",
  "const actual = computed(() => PANELES[cual.value] ?? Vacio)",
  "```",
  "",
  "Ese `??` es la mitad del trabajo bien hecho. `cual` puede acabar valiendo cualquier cosa -un",
  "parámetro de la dirección que alguien escribió a mano, un dato viejo guardado en el",
  "navegador- y sin repuesto lo que se pinta es nada, en silencio. Un panel que diga «esto no",
  "existe» es infinitamente mejor que un hueco vacío.",
  "",
  "Y las pestañas salen del propio mapa, no de una lista aparte:",
  "",
  "```html",
  "<button v-for=\"nombre in pestanas\" :key=\"nombre\" @click=\"cual = nombre\">{{ nombre }}</button>",
  "```",
  "",
  "Con `pestanas` siendo `Object.keys(PANELES)`. Así, añadir un panel es tocar **una** línea:",
  "la del mapa. Los botones aparecen solos.",
  "",
  "## El modelo mental",
  "",
  "Un tablón de estación con un solo marco y varios carteles. El marco es `<component>` y no",
  "cambia nunca; los carteles se guardan juntos en un cajón -el mapa- y el marco enseña el que",
  "le digas.",
  "",
  "Lo que se gana no es escribir menos: es que **la lista de lo posible viva en un solo sitio**.",
  "Ese es el criterio para saber cuándo usar esto y cuándo no. Si tienes dos opciones y no van",
  "a crecer, un `v-if` se lee mejor. Si tienes una familia de cosas del mismo tipo, va al mapa.",
  "",
  "## Lo que despista",
  "",
  "**Guardar un componente en un `ref` provoca un aviso.** Este:",
  "",
  "```",
  "[Vue warn]: Vue received a Component that was made a reactive object.",
  "This can lead to unnecessary performance overhead…",
  "```",
  "",
  "Un `ref` envuelve su contenido para vigilarlo por dentro, y vigilar un componente entero es",
  "trabajo tirado a la basura. Por eso el `computed` es mejor: devuelve el componente tal cual,",
  "sin envolverlo. Si de verdad necesitas guardarlo, existen `shallowRef` y `markRaw`.",
  "",
  "**Cambiar de panel destruye el anterior.** No se esconde: se desmonta, con su estado y lo",
  "que el usuario hubiera escrito dentro. Si al volver a una pestaña tiene que estar como la",
  "dejaste, hay que envolver el marco en `<KeepAlive>`.",
  "",
  "**El mapa va fuera del `setup`.** Si lo declaras dentro, se construye otra vez en cada",
  "montaje y los componentes que contiene son objetos nuevos cada vez. Fuera se crea una vez y",
  "se comparte, que es lo que quieres.",
  "",
  "**Las props se pasan como siempre.** `<component :is=\"actual\" :linea=\"7\" />` le pasa `linea`",
  "al componente que toque. Eso obliga a que todos los del mapa acepten las mismas props, o al",
  "menos a que ignoren con elegancia las que no entienden.",
  "",
  "## Pruébalo tú",
  "",
  "¿Qué pinta esto?",
  "",
  "```js",
  "const Uno = { template: `<p class=\"p\">uno</p>` }",
  "const Dos = { template: `<p class=\"p\">dos</p>` }",
  "",
  "const componente = {",
  "  components: { Uno, Dos },",
  "  setup() {",
  "    return { cual: ref('Dos') }",
  "  },",
  "  template: `<component :is=\"cual\" />`,",
  "}",
  "```",
  "",
  "Pinta `<p class=\"p\">dos</p>`.",
  "",
  "Y eso enseña la otra forma de usarlo: `cual` es el **texto** `'Dos'`, no el componente, y",
  "funciona porque `Dos` está registrado en `components`. Es cómodo cuando el nombre viene de",
  "fuera tal cual, y es frágil por lo mismo que es cómodo: si alguien renombra el componente y",
  "no toca el texto, no falla nada y se pinta un hueco.",
  "",
  "Con un mapa explícito eso no pasa, porque el nombre del mapa y el componente están escritos",
  "en la misma línea y se ven juntos.",
  "",
  "## Cómo se plantea",
  "",
  "Cuando en una pantalla haya que enseñar «uno de estos»:",
  "",
  "1. **Cuenta las opciones y pregúntate si van a crecer.** Dos fijas, `v-if`. Una familia,",
  "   mapa.",
  "2. **Escribe el mapa primero**, fuera del componente, con las claves que vas a recibir de",
  "   fuera tal cual se escriben.",
  "3. **Separa la elección de lo pintado.** Un `ref` con el texto y un `computed` con el",
  "   componente. No mezcles las dos cosas en una variable.",
  "4. **Pon el repuesto en el `computed`.** Con `??`, y que diga algo. Pruébalo con una clave",
  "   inventada antes de dar el trabajo por hecho.",
  "5. **Saca la navegación del propio mapa.** Si las pestañas están escritas a mano, el mapa",
  "   no te ha ahorrado el problema que venías a resolver.",
)
