/**
 * El apunte de Raoden para «muros-07-cada-union-lo-suyo».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Las cuatro maneras de unir estas dos tablas, con el mismo `ON` en todas, y lo que",
  "devuelve cada una. Nueve puestos -uno sin gremio- y seis gremios -uno sin puestos-:",
  "",
  "```sql",
  "puestos JOIN gremios          -->  8 filas   solo las parejas",
  "puestos LEFT JOIN gremios     -->  9 filas   todos los puestos",
  "gremios LEFT JOIN puestos     -->  9 filas   todos los gremios (otras nueve)",
  "puestos JOIN gremios (sin ON) --> 54 filas   todo contra todo",
  "```",
  "",
  "## Las dos de nueve no son la misma",
  "",
  "Dan el mismo número por casualidad y no dicen lo mismo:",
  "",
  "- `puestos LEFT JOIN gremios` conserva **los nueve puestos**. La fila que sobra",
  "  respecto al `JOIN` es el tenderete, con el gremio a nulo.",
  "- `gremios LEFT JOIN puestos` conserva **los seis gremios**. Ocho filas son las",
  "  parejas y la novena es el gremio de los aones, con el puesto a nulo. El tenderete",
  "  aquí **no sale**: no es de ningún gremio.",
  "",
  "Que coincidan en nueve es un accidente de estos datos. Lo que hay que mirar no es",
  "el número: es qué tabla está a la izquierda, porque esa es la que no pierde nada.",
  "",
  "## La quinta fila del reto, que es la importante",
  "",
  "```sql",
  "FROM puestos AS p",
  "LEFT JOIN gremios AS g ON p.gremio_id = g.id",
  "WHERE g.nombre IS NOT NULL      -- 8 filas",
  "```",
  "",
  "Un `LEFT JOIN` con una condición sobre la tabla de la derecha en el `WHERE`",
  "devuelve lo mismo que un `JOIN` normal. El `LEFT` salva la fila, y el `WHERE`, que",
  "trabaja después, la tira.",
  "",
  "Hay que reconocerlo de vista, porque se escribe sin querer: alguien pone el `LEFT`",
  "a propósito, añade un filtro razonable dos líneas más abajo y se queda tan tranquilo",
  "creyendo que conserva las filas sin pareja. No las conserva. Y no hay error.",
  "",
  "La regla: **en un `LEFT JOIN`, cualquier condición sobre la tabla de la derecha va",
  "en el `ON`, no en el `WHERE`**. La única excepción es cuando lo que quieres es",
  "precisamente quedarte con las que no emparejaron, y entonces la condición es",
  "`IS NULL` y está puesta a propósito.",
  "",
  "## Cómo se plantea: la tabla de la izquierda es la que decides primero",
  "",
  "Antes de escribir la unión, contéstate en este orden:",
  "",
  "1. **De qué es la lista** que quiero. ¿Una fila por puesto, o una por gremio? Esa es",
  "   la tabla que va en el `FROM`.",
  "2. **Si las filas sin pareja tienen que salir.** Sí, `LEFT JOIN`. No, `JOIN`.",
  "3. **Dónde va cada condición.** Sobre la tabla de la izquierda, en el `WHERE`.",
  "   Sobre la de la derecha, y con un `LEFT` delante, en el `ON`.",
  "",
  "Tres preguntas y la unión está decidida. La mitad de los informes mal hechos que",
  "vas a ver en tu vida son la respuesta equivocada a la primera.",
  "",
  "## Pruébalo tú",
  "",
  "Escribe las dos uniones de nueve filas, una detrás de otra, y compara los",
  "resultados fila a fila. Busca el tenderete en las dos. Está en una y no está en la",
  "otra, y las dos son correctas: contestan a preguntas distintas.",
)
