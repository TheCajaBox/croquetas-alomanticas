/**
 * El apunte de Kelsier para «tripulacion-02-lo-que-es-cierto».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Un `if` no pregunta «¿esto es `true`?». Pregunta «¿esto cuenta como cierto?», y ahí PHP",
  "tiene su propia lista.",
  "",
  "## Lo que cuenta como falso, y no hay más",
  "",
  "```php",
  "false        null        0        0.0",
  "''           '0'         []",
  "```",
  "",
  "Siete cosas. **Todo lo demás cuenta como cierto**, incluidos `-1`, `'false'`, `'0.0'`,",
  "`' '` (un espacio) y `[0]` (un array con un cero dentro).",
  "",
  "Las dos que muerden:",
  "",
  "- **`'0'`** es el único texto, aparte del vacío, que cuenta como falso. Con datos de un",
  "  formulario —donde todo llega como texto— una cantidad de 0 se comporta como si no",
  "  hubieras escrito nada.",
  "- **`'0.0'` sí cuenta como cierto.** La excepción es literalmente el texto `'0'`, no",
  "  «cualquier cosa que parezca cero».",
  "",
  "El arreglo no es memorizar la lista: es **decir lo que quieres decir**.",
  "",
  "```php",
  "if ($cantidad > 0) { ... }          // quiero decir «hay cantidad»",
  "if ($texto !== '') { ... }          // quiero decir «hay texto»",
  "if (count($lista) > 0) { ... }      // quiero decir «hay elementos»",
  "```",
  "",
  "Cuesta cuatro caracteres más y no depende de acordarse de nada.",
  "",
  "## El `=` donde iba un `==`",
  "",
  "```php",
  "if ($usuario = 'admin') { ... }     // ¡entra siempre!",
  "```",
  "",
  "Eso no es un error de sintaxis: es una asignación. Le mete `'admin'` a `$usuario` y luego",
  "pregunta si `'admin'` cuenta como cierto, que sí. Así que entra **siempre** y además te ha",
  "cambiado la variable. Es el fallo tipográfico más caro que existe y no avisa de nada.",
  "",
  "La costumbre que lo evita: cuando compares con un valor fijo, ponlo a la izquierda.",
  "`if ('admin' === $usuario)`. Si te comes un `=`, eso sí es un error de sintaxis y lo caza",
  "el propio PHP.",
  "",
  "## `elseif` no es un adorno",
  "",
  "```php",
  "if ($n >= 100) { $x = 'mucho'; }",
  "if ($n >= 20)  { $x = 'suficiente'; }   // ojo",
  "```",
  "",
  "Con dos `if`, las dos condiciones se miran **siempre**. Con 500, entra en la primera y",
  "también en la segunda, y `$x` acaba valiendo `'suficiente'`. Con `elseif`, la segunda solo",
  "se mira si la primera falló.",
  "",
  "Dos `if` seguidos son correctos cuando las condiciones no se pueden cumplir a la vez. En",
  "cuanto se solapan, `elseif`.",
  "",
  "## Cómo se plantea",
  "",
  "Ante una condición que se porta raro: `var_dump` de lo que hay dentro del paréntesis,",
  "antes de tocar la condición. Nueve de cada diez veces el valor no es del tipo que creías,",
  "y entonces el arreglo está en de dónde viene y no en el `if`.",
)
