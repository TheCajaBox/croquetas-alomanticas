/**
 * El apunte de Raoden para «trazos-10-monta-el-traspaso».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Un trámite con dos cambios en dos tablas distintas, y las dos cosas son el mismo trámite.",
  "",
  "```sql",
  "BEGIN;",
  "",
  "UPDATE puestos SET gremio_id = 2 WHERE id = 1;",
  "UPDATE ventas  SET monedas = 200 WHERE puesto_id = 1 AND dia = 'martes';",
  "",
  "COMMIT;",
  "```",
  "",
  "-Ese ejemplo mueve otro puesto y corrige otra venta: el del reto lo escribes tú.-",
  "",
  "Nada nuevo. Lo que hay que fijar es cuándo se usa cada palabra, porque a estas alturas ya",
  "hay siete que se parecen.",
  "",
  "## Las palabras que se confunden",
  "",
  "```sql",
  "UPDATE tabla SET columna = valor WHERE …    -- cambiar",
  "INSERT INTO tabla (cols) VALUES (vals)      -- meter",
  "DELETE FROM tabla WHERE …                   -- borrar",
  "```",
  "",
  "`SET` solo existe en el `UPDATE`. `VALUES` solo existe en el `INSERT`. Y `WHERE` está en",
  "los tres que tocan filas que ya existen, es decir en el `UPDATE` y en el `DELETE`, y no en",
  "el `INSERT`: no hay filas que elegir cuando la fila todavía no está.",
  "",
  "## El WHERE de dos condiciones, otra vez",
  "",
  "```sql",
  "WHERE puesto_id = 6 AND dia = 'lunes'",
  "```",
  "",
  "Ese `AND` no es un adorno: la tabla tiene `UNIQUE (puesto_id, dia)`, así que esas dos",
  "columnas juntas identifican una fila. Con una sola de las dos, el `UPDATE` podría tocar",
  "varias.",
  "",
  "Cuando una tabla tiene una clave única compuesta, esa pareja **es** la manera de señalar",
  "una fila, tanto como la clave primaria. Lo dice el esquema y merece leerlo antes de",
  "escribir el `WHERE`.",
  "",
  "## COMMIT y ROLLBACK: las dos maneras de cerrar",
  "",
  "```sql",
  "COMMIT;     -- publica lo hecho",
  "ROLLBACK;   -- lo tira y deja la base como estaba",
  "```",
  "",
  "Las dos cierran la transacción y hacen lo contrario. Es la única pareja de palabras de",
  "SQL en la que equivocarse cuesta trabajo perdido -con `ROLLBACK` cuando querías",
  "`COMMIT`- o un disgusto -al contrario-.",
  "",
  "Y hay una tercera manera de cerrarla que no se escribe: **que se caiga el programa**. Si",
  "la conexión se corta con una transacción abierta, la base hace `ROLLBACK` sola. Eso es",
  "una promesa y es la razón de que un corte de luz no deje los datos a medias.",
  "",
  "## Cómo se plantea",
  "",
  "Cuando un trámite toque más de una tabla, escríbelo así:",
  "",
  "1. Enumera los cambios en español, uno por línea.",
  "2. Para cada uno, la orden y **cómo señala su fila**. Si no sabes señalarla con algo que",
  "   no se repita, para y mira el esquema.",
  "3. `BEGIN` arriba y `COMMIT` abajo.",
  "4. Ejecuta y **cuenta las filas cambiadas de cada orden**: tantas como líneas tenía tu",
  "   lista.",
  "",
  "El paso cuatro es la comprobación que caza el `WHERE` de menos: dos órdenes que dicen «1",
  "fila cambiada» cada una son dos cambios; una que diga «6» es un accidente.",
  "",
  "## Pruébalo tú",
  "",
  "Cambia el `COMMIT` por un `ROLLBACK` y ejecuta. Los tests fallan todos, y la base está",
  "exactamente como estaba. Ahí se ve lo que hace cada una de las dos, y es mejor verlo aquí",
  "que la primera vez que importe.",
)
