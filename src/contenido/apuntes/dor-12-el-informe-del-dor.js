/**
 * El apunte de Raoden para «dor-12-el-informe-del-dor».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "No hay nada nuevo. Están las piezas de los once retos anteriores y hay que usarlas a la",
  "vez.",
  "",
  "Este apunte no explica: **ordena**, y trae una pieza de sintaxis que no ha salido",
  "todavía.",
  "",
  "## CASE WHEN: decidir dentro del SELECT",
  "",
  "```sql",
  "CASE WHEN total > 100 THEN 'encima' ELSE 'debajo' END AS sobre_la_media",
  "```",
  "",
  "Es el `if` de SQL, y va donde iría una columna. Se pueden encadenar condiciones:",
  "",
  "```sql",
  "CASE",
  "  WHEN total > 300 THEN 'mucho'",
  "  WHEN total > 100 THEN 'normal'",
  "  ELSE 'poco'",
  "END AS cuanto",
  "```",
  "",
  "Se prueban en orden y gana la primera que sea verdadera. Sin `ELSE`, lo que no encaje",
  "sale a nulo, y eso casi nunca es lo que se quiere: pon el `ELSE`.",
  "",
  "## Lo que pide el enunciado, traducido a decisiones",
  "",
  "- «los puestos que **han vendido algo**» → se une desde `ventas`, no desde `puestos`.",
  "  Un `JOIN` normal, y `La muralla` se queda fuera **a propósito**.",
  "- «su gremio, o `'sin gremio'`» → un `LEFT JOIN` a `gremios` -el tenderete no tiene- y",
  "  un `COALESCE` para que el hueco se lea.",
  "- «qué número hace dentro de su gremio» → `ROW_NUMBER` con `PARTITION BY`.",
  "- «la media de los totales de todos los puestos» → una subconsulta escalar **sobre el",
  "  paso intermedio**, no sobre la tabla de ventas.",
  "",
  "Ese último punto es la trampa del jefe, y es la del reto dos: hay dos medias creíbles.",
  "La de las ventas sueltas es 79,69 y deja a siete puestos «encima»; la de los totales de",
  "puesto es 159,375 y deja a cuatro. El enunciado dice cuál, y hay que leerlo.",
  "",
  "## Por qué el COALESCE va dentro del paso",
  "",
  "Fíjate en un detalle que decide si el informe sale bien:",
  "",
  "```sql",
  "COALESCE(g.nombre, 'sin gremio') AS gremio    -- dentro del WITH",
  "...",
  "ROW_NUMBER() OVER (PARTITION BY t.gremio ...)  -- fuera, sobre el texto ya resuelto",
  "```",
  "",
  "Si el `PARTITION BY` se hiciera sobre `gremio_id`, el tenderete caería en un grupo cuyo",
  "identificador es nulo. Funcionaría -SQLite agrupa los nulos juntos en un `PARTITION",
  "BY`- y el informe tendría un grupo sin nombre. Resolviendo el texto antes, el grupo se",
  "llama `sin gremio` y se ordena alfabéticamente con los demás, que es lo que hace que el",
  "informe se lea de un tirón.",
  "",
  "## Los sitios por donde se cae",
  "",
  "**La media que no era.** Cuatro filas «encima» o siete, y las dos parecen bien.",
  "",
  "**El `LEFT JOIN` que falta.** Sin él, el tenderete desaparece: es el puesto que no",
  "tiene gremio y el informe lo tira al unir.",
  "",
  "**El `JOIN` que sobra.** Si en vez de unir desde `ventas` unes desde `puestos` con un",
  "`LEFT`, aparece `La muralla` con el total a nulo. El enunciado pide los que han",
  "vendido, así que esa fila no va.",
  "",
  "**El `COALESCE` fuera del paso.** Funciona y deja un grupo sin nombre en la",
  "numeración.",
  "",
  "**El sitio filtrado dentro.** Aquí no se filtra el número -salen todos los puestos- así",
  "que esta vez no hace falta un paso más. Pero si lo intentaras filtrar en el mismo",
  "`SELECT` donde lo calculas, la base te lo diría.",
  "",
  "## Cómo se plantea",
  "",
  "1. El `WITH` con **una fila por puesto que ha vendido**: su nombre, su gremio ya",
  "   resuelto y su total. Ejecútalo solo: ocho filas.",
  "2. Sobre él, la numeración. Ejecuta y mira que cada gremio empiece por 1.",
  "3. La media, **ejecutada aparte** para ver el número: 159,375.",
  "4. El `CASE`, y contar cuántos quedan encima.",
  "5. El orden.",
  "",
  "El paso tres es el que caza la trampa, y es una consulta de una línea.",
  "",
  "## Pruébalo tú",
  "",
  "Cambia la media de la subconsulta por `AVG(monedas) FROM ventas` y cuenta los",
  "«encima». Siete en vez de cuatro. Ninguna de las dos consultas da error, las dos",
  "columnas dicen `encima` o `debajo`, y una de las dos está contestando a otra pregunta.",
)
