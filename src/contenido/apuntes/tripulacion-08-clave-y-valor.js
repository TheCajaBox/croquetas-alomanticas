/**
 * El apunte de Kelsier para «tripulacion-08-clave-y-valor».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Un `foreach` sobre un array asociativo puede sacar **la clave además del valor**:",
  "",
  "```php",
  "foreach ($inventario as $metal => $cuantos) {",
  "    echo $metal . ': ' . $cuantos . PHP_EOL;",
  "}",
  "```",
  "",
  "Se lee tal cual: «para cada elemento de `$inventario`, llama `$metal` a su clave y",
  "`$cuantos` a su valor». La flecha es la misma con la que se escribe el array, y eso ayuda a",
  "recordar de qué lado va cada cosa.",
  "",
  "Sin la flecha, `foreach ($inventario as $cuantos)` recorre **solo los valores**. Es válido y",
  "es lo que quieres cuando las claves no te hacen falta.",
  "",
  "## Los dos nombres los pones tú",
  "",
  "```php",
  "foreach ($inventario as $k => $v) { ... }          // funciona",
  "foreach ($inventario as $metal => $cuantos) { ... } // se entiende",
  "```",
  "",
  "El código se lee muchas más veces de las que se escribe. En singular y diciendo qué es.",
  "",
  "## Acumular y listar a la vez",
  "",
  "Es el bucle más frecuente que hay: sacar un total mientras se imprime cada línea.",
  "",
  "```php",
  "$total = 0;",
  "foreach ($inventario as $metal => $cuantos) {",
  "    $total += $cuantos;",
  "    echo $metal . ': ' . $cuantos . PHP_EOL;",
  "}",
  "echo 'Total: ' . $total . PHP_EOL;",
  "```",
  "",
  "Fíjate en dónde está cada línea: el `$total = 0` **antes** del bucle, la suma y el `echo`",
  "**dentro**, y el total **después**. La pregunta que lo resuelve siempre: ¿esto pasa una vez",
  "o pasa cada vez?",
  "",
  "## `+=` frente a `=`",
  "",
  "```php",
  "$total += $cuantos;   // le suma",
  "$total = $cuantos;    // lo reemplaza",
  "```",
  "",
  "Con `=` a secas, al final `$total` vale lo último que pasó por el bucle. Con una lista de un",
  "elemento las dos versiones dan lo mismo, y de ahí viene que este fallo llegue a producción.",
  "",
  "## El orden se conserva",
  "",
  "Un array de PHP recuerda el orden en que se metieron las claves, y `foreach` las recorre en",
  "ese orden. No hay que ordenar nada para que el informe salga como el inventario.",
  "",
  "## Pruébalo tú",
  "",
  "Cambia `+=` por `=` y ejecuta. Ver el total valiendo lo que vale el último elemento explica",
  "el operador mejor que este párrafo.",
)
