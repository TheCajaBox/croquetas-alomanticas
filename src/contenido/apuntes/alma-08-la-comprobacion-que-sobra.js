/**
 * El apunte de Gaotona para «alma-08-la-comprobacion-que-sobra».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Una comprobación **de más** que abre un agujero. Es el reto tres de este mundo escrito en",
  "código: añadir defensas no siempre mejora.",
  "",
  "## La línea",
  "",
  "```js",
  "// «por si la sesión viene sin usuario»",
  "const quien = sesion.usuario ?? peticion.usuario",
  "```",
  "",
  "Quien la escribió estaba siendo cuidadoso: se estaba defendiendo de una sesión rara. Y lo",
  "que ha hecho es dejar que **la petición diga quién eres** cuando la sesión no lo dice.",
  "",
  "El ataque es una línea: manda una sesión sin usuario —o con el usuario en otro campo— y el",
  "`usuario` que quieras en la petición.",
  "",
  "## Los valores de repuesto y la identidad",
  "",
  "Un valor por omisión es una buena costumbre en casi todo:",
  "",
  "```js",
  "const pagina = paginaPedida(p.pagina) ?? 1              // bien",
  "const permisos = TABLA[usuario] ?? []                    // bien: y deniega por omisión",
  "const quien = sesion.usuario ?? peticion.usuario         // MAL",
  "```",
  "",
  "La diferencia está en **de dónde sale el repuesto**. Los dos primeros caen en un valor",
  "escrito por ti, que es lo más restrictivo posible. El tercero cae en un valor que manda el",
  "cliente, que es lo más permisivo posible.",
  "",
  "La regla, corta: **la identidad no tiene valor por omisión**. Si la sesión no dice quién es,",
  "no hay nadie, y la respuesta es 401.",
  "",
  "## Y el `??` frente al `||`, que aquí importa",
  "",
  "```js",
  "sesion.usuario ?? peticion.usuario   // cae solo con null o undefined",
  "sesion.usuario || peticion.usuario   // cae también con '' y con 0",
  "```",
  "",
  "Con `??`, una sesión con `usuario: ''` **no** cae en el repuesto: `quien` vale cadena vacía",
  "y no coincide con ningún dueño, así que rechaza. Con `||` sí caería, y el agujero sería más",
  "grande.",
  "",
  "Y eso lleva a una cosa incómoda: la versión con `??` es **menos vulnerable por casualidad**.",
  "Funciona un poco mejor sin que nadie lo haya decidido, y ese es justo el tipo de código que",
  "sobrevive a las revisiones.",
  "",
  "## Lo que hay que reforzar además de quitar",
  "",
  "```js",
  "if (!sesion || !sesion.usuario) return null",
  "```",
  "",
  "Quitar el `??` no basta: sin esta segunda mitad, una sesión sin usuario compara",
  "`documento.de !== undefined`, que es verdadero, y devuelve `null`. **Funciona por",
  "casualidad**, y funcionar por casualidad deja de funcionar el día que alguien cambie la",
  "comparación o llegue un documento con `de` sin definir.",
  "",
  "Comprobarlo a propósito cuesta cinco caracteres y convierte una casualidad en una decisión.",
  "",
  "## Y una norma que no se puede escribir",
  "",
  "Este reto no tiene ninguna norma que prohíba el `??`, y no por descuido: el código que se te",
  "da lo usa en `buscar`, y las normas se comprueban sobre el fichero entero. Es la misma cosa",
  "que en La prueba de que la sabías: **hay cosas que la máquina no puede vigilar por ti**, y",
  "para esas están los tests hostiles.",
  "",
  "## Cómo se plantea",
  "",
  "El valor de repuesto se va, y la primera comprobación pasa a exigir que la sesión traiga",
  "usuario. Nada más: aquí el arreglo consiste en tener menos código, no más.",
)
