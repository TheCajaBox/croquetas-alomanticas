/**
 * El apunte de Wax para «vue3-03-computed».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Lo mismo que en la mansión, con otra forma. Una `computed` es un valor que se",
  "deduce de otros y que se mantiene al día solo.",
  "",
  "```js",
  "const { ref, computed } = Vue",
  "",
  "const colonia = ref([...])",
  "const soloHambrientos = ref(false)",
  "",
  "const visibles = computed(() =>",
  "  soloHambrientos.value ? colonia.value.filter((g) => g.hambre > 50) : colonia.value",
  ")",
  "```",
  "",
  "**Una `computed` devuelve un `ref`.** Por eso dentro de `setup` se lee con",
  "`.value`, y en la plantilla sin él, igual que cualquier otro.",
  "",
  "```js",
  "visibles.value.length      // en setup",
  "```",
  "",
  "```html",
  "{{ visibles.length }}      <!-- en la plantilla -->",
  "```",
  "",
  "## Sigue guardando el resultado",
  "",
  "Igual que en Vue 2: se calcula una vez y se recuerda hasta que cambie alguno de",
  "los datos de los que depende. Y las dependencias se descubren solas, apuntando qué",
  "ha leído la función al ejecutarse.",
  "",
  "De ahí la misma exigencia: **tiene que ser pura**. Solo leer y devolver. Si dentro",
  "tocas otro dato, pides algo a un servidor o lees la hora, o no se actualizará",
  "nunca o hará cosas impredecibles.",
  "",
  "## Se apoyan unas en otras",
  "",
  "Y aquí es donde se ve la ventaja de tenerlo todo junto:",
  "",
  "```js",
  "const visibles = computed(() => ...)",
  "const resumen = computed(() => `${visibles.value.length} de ${colonia.value.length}`)",
  "```",
  "",
  "`resumen` no vuelve a filtrar: usa el resultado guardado de `visibles`. Se",
  "encadenan, cada una calcula lo suyo una vez, y cada una tiene un nombre que dice",
  "qué es.",
  "",
  "Ese encadenamiento es la forma de partir un cálculo grande en pasos con nombre, y",
  "se lee infinitamente mejor que una computada de veinte líneas.",
  "",
  "## Computadas que se pueden escribir",
  "",
  "Poco frecuente y conviene conocerlo:",
  "",
  "```js",
  "const nombreCompleto = computed({",
  "  get: () => `${nombre.value} ${apellido.value}`,",
  "  set: (valor) => {",
  "    const [n, a] = valor.split(' ')",
  "    nombre.value = n",
  "    apellido.value = a",
  "  },",
  "})",
  "```",
  "",
  "Sirve para un `v-model` sobre algo que en realidad son dos datos. Se usa poco y,",
  "cuando encaja, encaja muy bien.",
  "",
  "## Lo que no es una computada",
  "",
  "Tres cosas que se confunden, y saber distinguirlas es la mitad de escribir Vue",
  "decente:",
  "",
  "**No es un `watch`.** Si lo que quieres es *hacer* algo cuando un dato cambia",
  "—guardar, avisar, pedir— eso es `watch`. La computada produce un valor y nada más.",
  "",
  "**No es un dato.** Si te encuentras con un `ref` que actualizas a mano cada vez que",
  "cambia otro, eso era una computada. El día que alguien añada un tercer sitio donde",
  "cambia el original, se desincronizará.",
  "",
  "```js",
  "const cuantos = ref(0)",
  "const anadir = (g) => { colonia.value.push(g); cuantos.value += 1 }   // mal",
  "",
  "const cuantos = computed(() => colonia.value.length)                 // bien",
  "```",
  "",
  "**Y no es un método.** Si necesita argumentos, no puede ser computada:",
  "",
  "```js",
  "const describir = (gato) => `${gato.nombre}, ${gato.hambre}`",
  "```",
  "",
  "Eso es una función normal que se devuelve y ya está. En la plantilla se llama con",
  "paréntesis, y se recalcula cada vez, que en esa posición es lo correcto.",
  "",
  "## Cuidado con lo caro dentro de una computada",
  "",
  "Como el resultado se guarda, una computada cara solo se paga cuando cambian sus",
  "dependencias. Muy bien. Pero conviene mirar **de qué depende**:",
  "",
  "```js",
  "const ordenados = computed(() =>",
  "  [...colonia.value].sort((a, b) => b.hambre - a.hambre)",
  ")",
  "```",
  "",
  "Eso depende de `colonia` entera. Cualquier cambio en cualquier gato —incluido uno",
  "que no afecte al orden— vuelve a ordenar la lista completa.",
  "",
  "Casi siempre da igual. Cuando no dé igual, la salida es depender de menos cosas: en",
  "vez de una computada que lo hace todo, varias que dependan cada una de lo suyo.",
  "",
  "## Y una que hay que tener presente",
  "",
  "Fíjate en el `[...colonia.value]` de ahí arriba: la copia antes de ordenar. Sin",
  "ella, `sort` reordenaría la lista original **dentro de una computada**, que es",
  "justo lo que no debe hacer una función pura.",
  "",
  "Y en Vue tiene un castigo extra: modificar un dato reactivo dentro de una",
  "computada puede disparar un recálculo que vuelve a modificarlo, y eso es un bucle",
  "infinito de repintados. Vue lo detecta y avisa por consola con un mensaje sobre",
  "recursión, pero es de los errores que más desconciertan cuando no se sabe de dónde",
  "vienen.",
  "",
  "## Lo que despista",
  "",
  "**No corre al declararlo ni al cambiar su dependencia.** Solo al **leerlo**, y solo si algo",
  "cambió desde la última vez. Declarar computados de más no cuesta nada.",
  "",
  "**Depender de algo no reactivo lo deja congelado.** Una variable normal, `Date.now()`, algo",
  "de `localStorage`: Vue no puede saber que cambiaron.",
  "",
  "**No lo hagas `async`.** Devolvería una promesa. Para datos que hay que ir a buscar,",
  "`watchEffect` y una `ref` donde dejar el resultado.",
  "",
  "**Escribir en una `ref` desde dentro de un computed** parece funcionar y es un bucle",
  "esperando: el computed depende de algo que él mismo cambia.",
  "",
  "**Un computed que solo se lee una vez y nunca cambia** no está haciendo nada por ti. Podía",
  "ser una constante.",
  "",
  "## Pruébalo tú",
  "",
  "¿Cuántas veces se ejecuta la función?",
  "",
  "```js",
  "let veces = 0",
  "const a = ref(1)",
  "const b = ref(10)",
  "const suma = computed(() => {",
  "  veces += 1",
  "  return a.value + b.value",
  "})",
  "",
  "a.value = 2",
  "b.value = 20",
  "console.log(suma.value)",
  "console.log(suma.value)",
  "console.log(veces)",
  "```",
  "",
  "**Una.** Los dos cambios ocurren antes de que nadie lea nada, así que solo lo marcan como",
  "sucio; la primera lectura lo calcula y la segunda usa lo guardado.",
  "",
  "Es exactamente lo que quieres: da igual cuántas veces cambies los datos antes de mirar, el",
  "trabajo se hace una vez y en el último momento. Y es la razón de que se pueda usar el mismo",
  "computed cinco veces en una plantilla sin pensarlo.",
  "",
  "## Cómo se plantea",
  "",
  "1. **¿Se puede deducir de otros datos?** Entonces computed, siempre. No lo guardes como",
  "   `ref` y lo mantengas a mano: el día que se olvide una actualización, la pantalla miente.",
  "2. **¿Mi función hace algo además de devolver?** Entonces no era un computed, era un `watch`.",
  "3. **¿Todo lo que leo dentro es reactivo?** Si no, no se actualizará, y parecerá un fallo de",
  "   Vue.",
  "4. **Encadena computados sin miedo.** Uno que se apoya en otro se lee mucho mejor que uno",
  "   enorme, y el coste es el mismo.",
)
