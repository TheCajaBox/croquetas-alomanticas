/**
 * El apunte de Kelsier para «ceniza-10-linea-a-linea».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Imprimir una lista es lo primero que hace todo programa que sirve para algo:",
  "un informe, un listado, una factura. Son tres piezas y ya las tienes todas.",
  "",
  "## `foreach`, la forma de recorrer",
  "",
  "```php",
  "foreach ($lineas as $linea) {",
  "    echo $linea . PHP_EOL;",
  "}",
  "```",
  "",
  "Se lee tal cual está escrito: «para cada elemento de `$lineas`, llámalo `$linea` y",
  "haz esto». La palabra clave es `as` y no se puede cambiar por nada.",
  "",
  "El nombre que le pongas a `$linea` te lo inventas tú, y conviene inventárselo",
  "bien: en singular y diciendo qué es. `foreach ($lineas as $l)` funciona igual y se",
  "lee peor, y el código se lee muchas más veces de las que se escribe.",
  "",
  "## `PHP_EOL`, el salto de línea",
  "",
  "`PHP_EOL` es una constante que vale el salto de línea que use el sistema donde",
  "corre el programa. En Linux y en Mac es `\\n`; en Windows son dos caracteres.",
  "Usándola, el mismo código sale bien en los tres.",
  "",
  "```php",
  "echo 'Informe' . PHP_EOL;",
  "```",
  "",
  "Dentro de un texto con comillas dobles puedes poner `\\n` directamente, y para",
  "salida por pantalla suele dar igual. Para ficheros que va a leer otro programa,",
  "`PHP_EOL`.",
  "",
  "## Cómo se plantea",
  "",
  "Escribe el resultado que quieres **primero**, tal cual, en un comentario:",
  "",
  "```php",
  "// Informe",
  "// - ceniza: mucha",
  "// - bruma: la normal",
  "```",
  "",
  "Con eso delante se ve solo lo que hay que hacer: una línea suelta antes, y luego",
  "una línea por elemento con un `- ` delante. Lo que va antes del bucle va antes del",
  "bucle; lo que se repite, dentro.",
  "",
  "Es la pregunta que resuelve la mitad de los bucles mal escritos: **¿esto pasa una",
  "vez o pasa cada vez?**",
  "",
  "## Los casos límite",
  "",
  "La lista vacía: el bucle no da ni una vuelta y solo sale el título. Eso casi",
  "siempre es lo correcto, pero conviene preguntarse si un informe vacío debería",
  "decir «no hay nada» en vez de quedarse a medias. Los casos límite no son solo",
  "cosa del código: también son decisiones.",
  "",
  "## Pruébalo tú",
  "",
  "Quita el `PHP_EOL` del bucle y ejecuta. Verlo todo pegado en una línea explica",
  "para qué sirve mejor que cualquier párrafo.",
)
