/**
 * El apunte de Vin para «ruina-03-el-contrato-roto».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Esto no arranca. Y eso es una buena noticia: los tres fallos de aquí son de los que PHP",
  "comprueba **antes** de ejecutar nada, así que se enteras en el momento y no en un informe",
  "raro tres semanas después.",
  "",
  "## Uno: la clase promete y no cumple",
  "",
  "La interfaz pide dos métodos y `Saco` solo tiene uno. PHP lo dice al cargar la clase:",
  "«contains 1 abstract method... must therefore be declared abstract or implement the",
  "remaining methods».",
  "",
  "El mensaje es largo y dice exactamente qué falta. La costumbre de leerlo entero ahorra más",
  "tiempo que cualquier otra cosa.",
  "",
  "## Dos: el constructor de la madre no se ejecuta",
  "",
  "```php",
  "class SacoGrande extends Saco",
  "{",
  "    public function __construct(public int $extra) {}",
  "}",
  "```",
  "",
  "Ese constructor **sustituye** al de la madre, así que `$cuantos` no se rellena nunca. El",
  "arreglo es recibirlo y pasarlo:",
  "",
  "```php",
  "public function __construct(int $cuantos, public int $extra)",
  "{",
  "    parent::__construct($cuantos);",
  "}",
  "```",
  "",
  "Fíjate en que `$cuantos` **no** lleva visibilidad: no es una propiedad de la hija, es un",
  "dato de paso para la madre. Ponerle `public` crearía una segunda propiedad con el mismo",
  "nombre y ahí no hay quien lo entienda.",
  "",
  "## Tres: `private` no lo ven las hijas",
  "",
  "`private` es «solo esta clase», y una hija es otra clase. Lo que abre la puerta a las hijas",
  "y la deja cerrada para el resto del programa es `protected`.",
  "",
  "Y hay una alternativa mejor cuando se puede: **que la hija no toque los datos de la madre**",
  "y le pida un método. Aquí no se puede -la hija necesita el número para sumarle su extra-,",
  "así que `protected`. En La Fundación sí se podía, y ahí la solución era `parent::resumen()`.",
  "",
  "La regla: cada propiedad `protected` es un trozo de la madre que ya no se puede cambiar sin",
  "mirar a las hijas. Se usan las que hagan falta y ni una más.",
  "",
  "## Cómo se plantea un arreglo de estos",
  "",
  "Uno a la vez, y ejecutando entre uno y otro. Los tres errores salen en orden -PHP para en el",
  "primero- así que el ciclo es: ejecutar, leer el error, arreglar **ese**, ejecutar.",
  "",
  "Intentar arreglar los tres de golpe leyendo el código es cómo se acaba cambiando cosas que",
  "estaban bien.",
  "",
  "## Los casos límite del arreglo",
  "",
  "Comprueba las tres cosas que el arreglo podría haber roto:",
  "",
  "- que `$extra` siga siendo público, que era lo que era;",
  "- que los sacos sigan sin poderse tocar desde fuera -`protected` no es `public`-;",
  "- y que las dos clases sigan cumpliendo el contrato.",
  "",
  "Hay un test para cada una.",
  "",
  "## Pruébalo tú",
  "",
  "```php",
  "<?php",
  "class Madre { public function __construct(private int $n) {} }",
  "class Hija extends Madre",
  "{",
  "    public function __construct() {}   // sin parent::",
  "    public function leer(): int { return $this->n; }",
  "}",
  "",
  "try { (new Hija())->leer(); } catch (Throwable $e) { echo $e->getMessage(); }",
  "```",
)
