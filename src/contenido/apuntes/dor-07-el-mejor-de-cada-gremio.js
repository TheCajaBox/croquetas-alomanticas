/**
 * El apunte de Raoden para «dor-07-el-mejor-de-cada-gremio».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "En El mercado quedó una cuenta pendiente. `MAX` dice **cuánto** fue la mejor venta, no",
  "**de quién**, y agrupar no sirve para arreglarlo: agrupar reduce filas, y lo que hace",
  "falta es quedarse con una fila concreta.",
  "",
  "Las **funciones de ventana** hacen exactamente eso: calculan mirando las filas de al",
  "lado **sin agrupar**.",
  "",
  "```sql",
  "ROW_NUMBER() OVER (PARTITION BY gremio_id ORDER BY total DESC) AS sitio",
  "```",
  "",
  "Cada fila sigue saliendo -eso es lo nuevo- y además trae el número que le toca dentro",
  "de su grupo.",
  "",
  "## Las dos piezas del OVER",
  "",
  "- **`PARTITION BY`** hace los grupos. Es un `GROUP BY` que no junta nada.",
  "- **`ORDER BY`** (el de dentro del `OVER`) decide en qué orden se numera dentro de cada",
  "  grupo.",
  "",
  "Las dos son opcionales. Sin `PARTITION BY`, la ventana es la tabla entera. Sin",
  "`ORDER BY`, el número que sale no significa nada.",
  "",
  "Y cuidado con no confundir ese `ORDER BY` con el de la consulta: son dos y hacen cosas",
  "distintas. El de dentro decide la numeración; el de fuera, cómo se pinta el resultado.",
  "",
  "## La receta de «el mejor de cada grupo»",
  "",
  "Siempre la misma, y merece la pena memorizarla porque se usa constantemente:",
  "",
  "1. **Numera** con `ROW_NUMBER() OVER (PARTITION BY el_grupo ORDER BY lo_que_decide DESC)`.",
  "2. **Filtra** el número 1, **desde fuera**.",
  "",
  "El segundo paso tiene que ir fuera y no es un capricho: una función de ventana se",
  "calcula **después** del `WHERE` de su propia consulta, así que no se puede filtrar por",
  "ella ahí mismo. Hay que nombrar el paso -un `WITH`- y filtrar en la consulta de arriba.",
  "",
  "La base lo dice con un error claro si lo intentas, y conviene verlo una vez.",
  "",
  "## Qué se puede poner con un OVER",
  "",
  "Casi todo. Las tres que numeran -`ROW_NUMBER`, `RANK`, `DENSE_RANK`-, los agregados de",
  "siempre (`SUM`, `COUNT`, `AVG`, `MIN`, `MAX`) y algunas que solo existen así:",
  "",
  "```sql",
  "LAG(monedas)  OVER (ORDER BY id)   -- lo que valía la fila anterior",
  "LEAD(monedas) OVER (ORDER BY id)   -- la siguiente",
  "```",
  "",
  "`LAG` y `LEAD` son las que contestan a «cuánto ha subido respecto a ayer» sin unir la",
  "tabla consigo misma, que es como se hacía antes de que existieran.",
  "",
  "## Ventana o agrupación: cómo decidir",
  "",
  "La pregunta es una sola:",
  "",
  "> ¿Quiero **menos filas** o **las mismas filas con un dato más**?",
  "",
  "- Menos filas → `GROUP BY`. «Cuánto recaudó cada gremio» es una fila por gremio.",
  "- Las mismas con un dato más → ventana. «Cada puesto con su sitio en su gremio» son",
  "  todos los puestos.",
  "",
  "Y a veces hacen falta las dos, en dos pasos: primero se agrupa para tener una fila por",
  "puesto, y después se numera. Eso es lo que hace este reto.",
  "",
  "## Cómo se plantea",
  "",
  "1. El paso que deja **una fila por cosa que vas a numerar**. Ejecútalo y cuéntalo.",
  "2. El paso que numera. Ejecútalo **entero, sin filtrar**, y mira los números: cada",
  "   grupo tiene que empezar por 1.",
  "3. Y solo entonces el filtro del 1.",
  "",
  "El paso dos sin filtrar es el que enseña: se ve la numeración completa y se comprueba",
  "que los grupos son los que creías.",
  "",
  "## Pruébalo tú",
  "",
  "Ejecuta el paso que numera sin el filtro. Ocho filas con sus números, y los escribas",
  "con 1, 2 y 3. Ahora quita el `PARTITION BY` y vuelve a mirar: una sola numeración del",
  "1 al 8 para todo el mercado. Esa es la diferencia entre «el mejor de cada gremio» y «el",
  "mejor del mercado».",
)
