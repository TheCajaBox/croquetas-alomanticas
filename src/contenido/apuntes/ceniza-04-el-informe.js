/**
 * El apunte de Kelsier para «ceniza-04-el-informe».
 *
 * De la primera era: más corto que los de Wax a propósito, porque aquí la mitad
 * de lo que se aprende está en los retos, que son más y más pequeños.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Este es el jefe del mundo, así que no hay pistas y no hay nada nuevo: todo lo que",
  "necesitas está en los tres retos de antes. Lo único que cambia es que hay que usar",
  "las tres cosas a la vez, que es exactamente en lo que consiste programar.",
  "",
  "## Acumular texto en vez de números",
  "",
  "Ya sumaste números con un acumulador. Con texto es el mismo patrón y el mismo",
  "operador de siempre, el punto:",
  "",
  "```php",
  "$texto = '';",
  "foreach (['uno', 'dos'] as $palabra) {",
  "    $texto .= $palabra . ' ';",
  "}",
  "// $texto vale 'uno dos '",
  "```",
  "",
  "`.=` es a los textos lo que `+=` a los números: pega al final lo que le des. Y",
  "empieza en cadena vacía por la misma razón por la que el otro empezaba en cero.",
  "",
  "## Contar mientras recorres",
  "",
  "`foreach` te da los elementos, no su número de orden. Si hace falta numerar, se",
  "lleva un contador aparte:",
  "",
  "```php",
  "$numero = 1;",
  "foreach ($cosas as $cosa) {",
  "    // usa $numero aquí",
  "    $numero += 1;",
  "}",
  "```",
  "",
  "Empieza en 1 y no en 0 porque lo que se está haciendo es una lista para leer, no",
  "una posición de array. Son dos cosas distintas y se confunden mucho.",
  "",
  "## Devolver, no imprimir",
  "",
  "Aquí está la trampa de este reto, y es una trampa que se repite toda la vida",
  "profesional: **`echo` imprime, `return` devuelve**. La función tiene que devolver",
  "la cadena; si la imprimes, quien la llame no recibe nada.",
  "",
  "```php",
  "function mal(array $lista): string { echo 'algo'; }   // devuelve null",
  "function bien(array $lista): string { return 'algo'; }",
  "```",
  "",
  "Una función que promete `: string` y no devuelve nada revienta al llamarla, y el",
  "mensaje es de los claros: dice que ha devuelto null donde esperaba texto.",
  "",
  "## Cómo se plantea",
  "",
  "Empieza por el caso más pequeño que no sea vacío: una sola persona. Escribe qué",
  "tiene que salir exactamente, con su salto de línea final. Cuando eso funcione,",
  "prueba con tres: si el patrón está bien, sale gratis. Y el caso vacío, al final,",
  "que casi siempre ya funciona solo.",
)
