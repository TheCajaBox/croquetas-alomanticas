/**
 * El apunte de Kelsier para «ceniza-06-los-tipos».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Un valor en PHP siempre es de algún tipo. No hace falta declararlo —PHP lo",
  "deduce— pero sí hace falta saber cuál es, porque el tipo decide lo que pasa.",
  "",
  "## Los que se usan todo el rato",
  "",
  "```php",
  "$entero  = 42;            // int",
  "$decimal = 3.5;           // float",
  "$texto   = 'peltre';      // string",
  "$si      = true;          // bool",
  "$lista   = ['a', 'b'];    // array",
  "$nada    = null;          // null",
  "```",
  "",
  "Hay más —objetos, funciones, recursos— y llegarán a su hora. Con estos seis se",
  "hace casi todo.",
  "",
  "## `'42'` no es `42`",
  "",
  "Esta es la que hay que llevarse de aquí. Con comillas es un **texto** que resulta",
  "estar formado por dos dígitos; sin comillas es un **número**. Se parecen al leerlos",
  "y no se comportan igual:",
  "",
  "```php",
  "echo 42 + 1;        // 43",
  "echo '42' + 1;      // 43   <- PHP convierte y suma",
  "echo '42' . 1;      // 421  <- aquí pega, no suma",
  "```",
  "",
  "PHP es servicial y convierte solo cuando cree saber lo que quieres. Eso ahorra",
  "trabajo el 90% de las veces y el 10% restante produce fallos que cuesta encontrar.",
  "",
  "## Cómo se mira",
  "",
  "Dos funciones, y hacen cosas distintas:",
  "",
  "```php",
  "echo get_debug_type($x);   // el nombre del tipo, a secas: int, string, array…",
  "var_dump($x);              // el tipo Y el contenido, con detalle",
  "```",
  "",
  "`get_debug_type` es la que quieres cuando solo preguntas «¿qué es esto?».",
  "`var_dump` es la que quieres cuando algo no cuadra y necesitas verlo todo.",
  "",
  "## `null` no es cero",
  "",
  "`null` no es `0`, ni `''`, ni `false`. Es «aquí no hay nada» y tiene su propio",
  "tipo. La diferencia importa: una nota que vale `0` es un cero; una nota que vale",
  "`null` es una nota sin poner. Confundirlas es cómo se acaba con medias que no",
  "cuadran.",
  "",
  "## Los casos límite",
  "",
  "`0.0` es float aunque no tenga decimales visibles. `[]` es array aunque esté",
  "vacío. Y `'0'` es string, lo cual da lugar a una de las comparaciones más",
  "traicioneras del lenguaje —está en el reto siguiente—.",
  "",
  "## Pruébalo tú",
  "",
  "Coge cinco valores cualesquiera y pásalos por `get_debug_type` antes de mirar la",
  "respuesta. Los que falles son exactamente los que te van a morder algún día.",
)
