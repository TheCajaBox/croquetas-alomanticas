/**
 * El apunte de Raoden para «trazos-05-cerrar-un-puesto».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Cambiar una fila:",
  "",
  "```sql",
  "UPDATE ventas SET monedas = 200 WHERE id = 4;",
  "```",
  "",
  "`SET` dice qué columna se cambia y a qué; `WHERE` dice en qué filas. Se pueden cambiar",
  "varias columnas a la vez, separadas por comas:",
  "",
  "```sql",
  "UPDATE puestos SET abierto = 0, gremio_id = NULL WHERE id = 5;",
  "```",
  "",
  "## Filtra por lo que identifica",
  "",
  "```sql",
  "WHERE id = 5                    -- bien",
  "WHERE nombre = 'La muralla'      -- funciona, y es peor",
  "```",
  "",
  "La segunda funciona porque `nombre` es `UNIQUE` en esta tabla. Es una costumbre peor por",
  "dos motivos: el día que la columna deje de ser única, el `UPDATE` toca varias filas; y",
  "el día que alguien corrija una errata en el nombre, el `UPDATE` no toca ninguna y no",
  "avisa.",
  "",
  "La clave primaria está justamente para esto: es el nombre que no cambia.",
  "",
  "## La costumbre, otra vez, porque es la del mundo",
  "",
  "```sql",
  "-- 1. Mira.",
  "SELECT id, nombre, abierto FROM puestos WHERE gremio_id = 2;",
  "-- 2. Cuenta las filas. ¿Dos? No era eso. Estrecha la condición.",
  "-- 3. Y solo cuando el SELECT devuelva lo que esperabas, cambia el verbo.",
  "UPDATE puestos SET abierto = 0 WHERE gremio_id = 2;",
  "```",
  "",
  "Cinco segundos. Y el paso dos es el importante: no basta con que el `SELECT` devuelva",
  "algo razonable, hay que **contar**. Un `WHERE` que devuelve tres filas cuando esperabas",
  "una es un `UPDATE` que va a tocar tres.",
  "",
  "En este juego los retos son de una sola orden, así que el `SELECT` se prueba aparte y se",
  "borra antes de enviar. En un trabajo de verdad se queda escrito en el historial de la",
  "consola, que es donde se mira cuando algo sale mal.",
  "",
  "## No tocar lo que no se pide",
  "",
  "Un `SET` con columnas de más no da error y hace daño en silencio:",
  "",
  "```sql",
  "-- Se pedía cerrarlo. Esto además lo saca de su gremio.",
  "UPDATE puestos SET abierto = 0, gremio_id = NULL WHERE id = 5;",
  "```",
  "",
  "Los tests de «está cerrado» pasarían igual. Por eso los de este mundo comprueban también",
  "**lo que no ha cambiado**: la fila entera, el número de filas de la tabla, las otras",
  "tablas. Es la costumbre que conviene copiar al escribir tests propios: no basta con",
  "comprobar lo que querías hacer, hay que comprobar que no has hecho nada más.",
  "",
  "## Cómo se plantea",
  "",
  "Tres preguntas antes de escribir un `UPDATE`:",
  "",
  "1. **Qué filas.** Y se comprueba con un `SELECT` y una cuenta.",
  "2. **Qué columnas.** Solo las que se piden. Ni una más.",
  "3. **Qué pasa si el `WHERE` no encuentra nada.** No es un error: son cero filas",
  "   cambiadas, y la base no se queja. Si esperabas cambiar algo, mira el número.",
  "",
  "La tercera es la que se olvida. Un `UPDATE` que no encuentra su fila es tan silencioso",
  "como uno que encuentra demasiadas.",
  "",
  "## Pruébalo tú",
  "",
  "Ejecuta el mismo `UPDATE` con `WHERE id = 99`. Ninguna línea de filas cambiadas, ningún",
  "error, ningún cambio. Ahora imagina ese `UPDATE` dentro de un programa que da por hecho",
  "que ha funcionado.",
)
