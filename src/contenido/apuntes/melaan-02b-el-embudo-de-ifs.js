/**
 * El apunte de Wax para «melaan-02b-el-embudo-de-ifs».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Hay una forma de escribir condiciones que sale sola cuando vas añadiendo casos según",
  "aparecen: cada caso nuevo se mete dentro del anterior. Al cuarto, la línea que de verdad",
  "hace algo está en el centro de un embudo y sus `else` están a treinta renglones.",
  "",
  "Se arregla siempre igual y siempre entero, y se llama guarda.",
  "",
  "## El problema que resuelve",
  "",
  "Lee esto y fíjate en lo que te obliga a hacer:",
  "",
  "```js",
  "function tarifa(cliente) {",
  "  if (cliente) {",
  "    if (cliente.activo) {",
  "      if (cliente.plan) {",
  "        return cliente.plan.precio",
  "      } else {",
  "        return 0",
  "      }",
  "    } else {",
  "      return 0",
  "    }",
  "  } else {",
  "    return 0",
  "  }",
  "}",
  "```",
  "",
  "Para saber qué pasa con un cliente sin plan tienes que llevar en la cabeza tres condiciones",
  "que ya se cumplieron. Y para saber qué hace un `else`, tienes que subir con el dedo buscando",
  "su `if`. Esas dos cosas -acumular contexto y buscar la pareja- son todo el coste.",
  "",
  "Ahora al revés:",
  "",
  "```js",
  "function tarifa(cliente) {",
  "  if (!cliente) return 0",
  "  if (!cliente.activo) return 0",
  "  if (!cliente.plan) return 0",
  "  return cliente.plan.precio",
  "}",
  "```",
  "",
  "Cuatro líneas y ni un `else`. Y la última no necesita saber nada: si has llegado ahí, es que",
  "el cliente existe, está activo y tiene plan. **El contexto se ha quedado arriba en vez de",
  "acumularse.**",
  "",
  "## Qué es una guarda, exactamente",
  "",
  "Un `if` que comprueba un caso que no vale y **sale**. Con `return`, con `throw`, con",
  "`continue`, con `break`: lo importante es que después de una guarda no hay nada que discutir.",
  "",
  "Y de ahí sale la propiedad que lo cambia todo: **después de una guarda no hace falta `else`**.",
  "Si sigues leyendo, la guarda no se cumplió. El `else` desaparece porque su trabajo lo hace el",
  "propio flujo del programa.",
  "",
  "El modelo mental es el portero de un local. No hay un empleado por cada motivo de rechazo",
  "escondido en habitaciones cada vez más pequeñas: hay uno en la puerta que va diciendo «tú",
  "no, por esto» y quien pasa ya está dentro y no vuelve a dar explicaciones.",
  "",
  "## Dos casos que se aplanan con una sola línea",
  "",
  "Mira las tres primeras guardas del ejemplo. Las dos primeras preguntan por lo mismo: que",
  "algo exista antes de mirar dentro. Eso es exactamente para lo que está `?.`:",
  "",
  "```js",
  "if (!cliente?.plan) return 0",
  "```",
  "",
  "Y cuando lo único que queda al final es un sí o un no, ni guarda hace falta:",
  "",
  "```js",
  "const puedeCobrar = (cliente) => Boolean(cliente?.activo && cliente?.plan)",
  "```",
  "",
  "Ojo con el `Boolean` de ahí: sin él, la función devuelve `undefined` cuando no hay cliente y",
  "el objeto del plan cuando sí lo hay. Funciona en un `if` y miente en cuanto alguien compare",
  "con `false` o lo guarde en un JSON. Una función que promete un sí o un no devuelve un sí o un",
  "no.",
  "",
  "## El ejemplo, con su salida",
  "",
  "```js",
  "const precio = (pedido) => {",
  "  if (!pedido?.destino) return 0",
  "  if (pedido.peso <= 0) return 0",
  "  return pedido.peso > 20 ? 30 : 15",
  "}",
  "",
  "console.log(precio(null))",
  "console.log(precio({ peso: 4 }))",
  "console.log(precio({ destino: 'ahí', peso: 0 }))",
  "console.log(precio({ destino: 'ahí', peso: 4 }))",
  "console.log(precio({ destino: 'ahí', peso: 40 }))",
  "```",
  "",
  "Imprime:",
  "",
  "```",
  "0",
  "0",
  "0",
  "15",
  "30",
  "```",
  "",
  "Cinco casos, tres líneas de código y ni una sangría de más. Compáralo con lo que ocuparía el",
  "embudo equivalente: veinte líneas y cinco niveles.",
  "",
  "## Lo que despista",
  "",
  "**«Varios `return` es malo».** Es una regla vieja que venía de lenguajes donde había que",
  "liberar memoria a mano antes de salir. Aquí no hay nada que liberar, y un `return` temprano",
  "es lo que hace que el resto de la función no tenga que defenderse.",
  "",
  "**Aplanar no es negar todo.** Se niega la condición de entrada, no la de salida: donde había",
  "«si vale, sigue», la guarda dice «si no vale, sal». Es donde más se equivoca la gente, y el",
  "síntoma es una función que devuelve lo contrario de lo que devolvía.",
  "",
  "**El orden de las guardas importa.** Si la segunda mira dentro de algo que la primera todavía",
  "no ha garantizado que exista, la función revienta. Van de la más básica a la más específica.",
  "",
  "**`?.` no es lo mismo que un `if` de existencia.** `cliente?.plan` da `undefined` si no hay",
  "cliente; también da `undefined` si hay cliente y no hay plan. Aquí eso viene bien porque los",
  "dos casos van al mismo sitio. Cuando hay que distinguirlos, siguen haciendo falta dos guardas.",
  "",
  "**Un `if` dentro de otro no siempre es un embudo.** Dos niveles se leen perfectamente. Lo que",
  "se aplana es lo que tiene tres o más, o lo que lleva `else` cargados de código.",
  "",
  "## Pruébalo tú",
  "",
  "Un clásico. ¿Qué le pasa a esta función?",
  "",
  "```js",
  "function esMayor(persona) {",
  "  if (persona.edad >= 18) {",
  "    return true",
  "  } else {",
  "    return false",
  "  }",
  "}",
  "```",
  "",
  "No le pasa nada grave, y tiene seis líneas para hacer lo que hace una:",
  "",
  "```js",
  "const esMayor = (persona) => persona.edad >= 18",
  "```",
  "",
  "Porque `persona.edad >= 18` **ya es** un `true` o un `false`. El `if` que devuelve `true` o",
  "`false` está preguntando por una respuesta que ya tenía en la mano. Es el mismo vicio del",
  "embudo en su versión más pequeña: envolver una decisión que ya estaba tomada.",
  "",
  "Y ojo con el borde: si `persona` puede no venir, hace falta `persona?.edad >= 18`, que con",
  "`undefined` da `false` y no revienta.",
  "",
  "## Cómo se plantea",
  "",
  "1. **Cuenta los niveles.** Tres o más de sangría en una función corta es el aviso.",
  "2. **Busca los `else` que devuelven lo mismo.** Cada uno es una guarda esperando a subir.",
  "3. **Sube las guardas de una en una**, de la más básica a la más específica, negando la",
  "   condición y saliendo.",
  "4. **Junta las que preguntan por existencia** con `?.`.",
  "5. **Mira lo que queda al final.** Si es una decisión de dos, cabe en un ternario; si es un sí",
  "   o un no, cabe en una expresión sin ningún `if`.",
  "6. **Y ejecuta los tests entre paso y paso.** Un embudo aplanado del tirón es el sitio donde",
  "   más fácil es invertir una condición sin darse cuenta.",
)
