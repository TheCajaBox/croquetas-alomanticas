/**
 * El apunte de Raoden para «trazos-03-que-deja-la-orden».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Predecir el resultado de un `SELECT` es predecir qué filas salen. Predecir el de un",
  "`UPDATE` es predecir **en qué estado queda la tabla**, y es lo que hay que aprender a",
  "ver antes de escribir en una base de verdad.",
  "",
  "## Lo que hace un UPDATE, fila por fila",
  "",
  "```sql",
  "UPDATE puestos SET abierto = 0 WHERE gremio_id <> 1;",
  "```",
  "",
  "La base recorre las filas y, para cada una:",
  "",
  "1. Prueba la condición.",
  "2. Si es **verdadera**, le pone el valor nuevo.",
  "3. Si es falsa **o desconocida**, la deja como estaba.",
  "",
  "El paso tres es donde vuelve el nulo, y ahora ya sabes por qué. `NULL <> 1` no es",
  "verdadero, así que la fila del tenderete **no se toca**: se queda abierta.",
  "",
  "Ese es el mismo mecanismo que te dejaba filas fuera de un `SELECT`. La diferencia es",
  "que allí faltaba una fila en un informe y aquí queda un puesto sin cerrar, y nadie se",
  "entera hasta que alguien pregunta por él.",
  "",
  "## Cambiar algo a lo que ya valía",
  "",
  "`Aon Ien` ya estaba cerrado, y es del gremio 1, así que la condición es falsa y el",
  "`UPDATE` no lo toca. Aunque lo tocara, daría igual: ponerle 0 a lo que ya vale 0 no",
  "cambia nada.",
  "",
  "Y sin embargo **cuenta como fila cambiada** en algunas bases. SQLite cuenta las filas",
  "que ha tocado, no las que han quedado distintas. Así que «4 filas cambiadas» no",
  "significa «cuatro cosas son distintas ahora»: significa «he escrito en cuatro filas».",
  "Es un detalle y explica números que no cuadran.",
  "",
  "## El SET puede usar el valor de antes",
  "",
  "Esto no sale en el reto y hace falta a menudo:",
  "",
  "```sql",
  "UPDATE ventas SET monedas = monedas + 10 WHERE puesto_id = 1;",
  "```",
  "",
  "La columna se usa en su propio cálculo, y la base coge el valor de cada fila. No hay",
  "que leerlo primero, y **es importante que no lo hagas**: entre un `SELECT` y un",
  "`UPDATE` separados, otro programa puede haber cambiado el dato, y entonces sumas sobre",
  "un número viejo. Con una sola orden eso no puede pasar.",
  "",
  "## Cómo se plantea: la tabla en un papel",
  "",
  "Para predecir un `UPDATE`, haz la tabla en tres columnas:",
  "",
  "```sql",
  "fila          | ¿condición? | queda",
  "Aon Aon (g1)  | falsa       | abierto",
  "La piedra(g2) | verdadera   | cerrado",
  "El tenderete  | desconocida | abierto  <- el nulo",
  "```",
  "",
  "Con eso escrito, no hay manera de equivocarse, y se tarda menos que en discutir el",
  "resultado después. La columna del medio es la que hay que rellenar con cuidado: tiene",
  "tres valores posibles y no dos.",
  "",
  "## Pruébalo tú",
  "",
  "Después de acertar la predicción, ejecuta el `UPDATE` cambiando `<> 1` por",
  "`<> 1 OR gremio_id IS NULL` y mira qué pasa con el tenderete. Ahí está la diferencia",
  "entre cerrar cuatro puestos y cerrar cinco, en media línea.",
)
