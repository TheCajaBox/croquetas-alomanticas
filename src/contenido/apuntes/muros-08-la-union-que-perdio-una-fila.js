/**
 * El apunte de Raoden para «muros-08-la-union-que-perdio-una-fila».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Este es el fallo con el que más se pierde el tiempo en este mundo, y tiene la mala",
  "costumbre de escribirse solo. Alguien pone un `LEFT JOIN` a propósito, añade un",
  "filtro razonable un par de líneas más abajo, y el `LEFT JOIN` deja de servir para",
  "nada.",
  "",
  "```sql",
  "FROM puestos AS p",
  "LEFT JOIN gremios AS g ON p.gremio_id = g.id",
  "WHERE g.nombre <> 'aones'",
  "```",
  "",
  "## Por qué se cae la fila",
  "",
  "Dos cosas a la vez, y hay que verlas las dos:",
  "",
  "**El `WHERE` trabaja después de unir.** Ese es el orden y ya lo sabes: `FROM`,",
  "`WHERE`, `SELECT`, `ORDER BY`, `LIMIT`. La unión pasa en el `FROM`. Así que cuando",
  "el `WHERE` mira, la fila del tenderete ya está ahí, salvada por el `LEFT`, con",
  "`g.nombre` a nulo.",
  "",
  "**Comparar con nulo no da verdadero.** `NULL <> 'aones'` no vale verdadero ni falso:",
  "vale «no se sabe». Y el `WHERE` solo deja pasar lo verdadero, así que la fila cae.",
  "",
  "Fíjate en lo traicionero: la condición ni siquiera hablaba del tenderete. Hablaba de",
  "excluir a los aones. El tenderete se cae de rebote, porque su nulo no supera **nada**.",
  "",
  "## Cómo se arregla, según lo que quisieras decir",
  "",
  "**Si la condición es parte de con quién emparejar** -«únelo solo con los gremios",
  "que no sean el de los aones»- se sube al `ON`:",
  "",
  "```sql",
  "LEFT JOIN gremios AS g",
  "  ON p.gremio_id = g.id AND g.nombre <> 'aones'",
  "```",
  "",
  "Ahí el `LEFT` sigue cumpliendo su promesa: las nueve filas salen, y las que no",
  "emparejan -incluidos los puestos de los aones, si hubiera- salen con el gremio a",
  "nulo.",
  "",
  "**Si es un filtro de verdad** -«quita del informe las filas de los aones»- hay que",
  "decir a mano qué pasa con los nulos, porque hay que decidirlo:",
  "",
  "```sql",
  "WHERE g.nombre IS NULL OR g.nombre <> 'aones'",
  "```",
  "",
  "Se lee raro y es exacto: deja pasar las filas sin gremio **y** las que tienen un",
  "gremio que no es ese. Escribirlo obliga a pensar qué querías, que es justo lo que",
  "el fallo se salta.",
  "",
  "## La regla corta",
  "",
  "> En un `LEFT JOIN`, toda condición sobre la tabla de la derecha va en el `ON`.",
  "",
  "Con una sola excepción, y es la que se usa a propósito: `IS NULL` en el `WHERE`",
  "para quedarse **solo** con las que no emparejaron. Ese es el truco del reto seis, y",
  "es el mismo mecanismo usado a favor en vez de en contra.",
  "",
  "## Cómo se plantea: leer el error hacia atrás",
  "",
  "El aviso aquí no es un error de la base: es un número de filas que no cuadra. Y se",
  "acorrala igual que cualquier otro fallo, quitando cosas:",
  "",
  "1. Ejecuta **solo** el `FROM` con su unión, sin `WHERE`. Cuenta.",
  "2. Añade el `WHERE`. Cuenta otra vez.",
  "3. Si en el paso dos ha desaparecido alguna fila que el paso uno había salvado, ya",
  "   sabes quién se la lleva.",
  "",
  "Dos ejecuciones y el culpable está señalado. Mirar la consulta entera buscando el",
  "fallo a ojo funciona bastante peor.",
  "",
  "## Pruébalo tú",
  "",
  "Sube la condición al `ON` y ejecuta. Nueve filas. Ahora bájala otra vez al `WHERE`.",
  "Ocho. La misma condición, el mismo `LEFT JOIN`, dos resultados: eso es lo que hay",
  "que llevarse de este reto.",
)
