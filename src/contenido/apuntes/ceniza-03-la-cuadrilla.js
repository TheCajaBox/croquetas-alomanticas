/**
 * El apunte de Kelsier para «ceniza-03-la-cuadrilla».
 *
 * De la primera era: más corto que los de Wax a propósito -3.000 a 4.500
 * caracteres- porque aquí la mitad de lo que se aprende está en los retos, que
 * son más y más pequeños. Se carga al abrir su reto, no al arrancar el juego.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Una lista en PHP se llama **array** y se escribe entre corchetes:",
  "",
  "```php",
  "$cuadrilla = ['Kelsier', 'Brisa', 'Ham'];",
  "```",
  "",
  "Se accede por su posición, empezando en cero, que es la costumbre en casi todos",
  "los lenguajes y la fuente de casi todos los despistes:",
  "",
  "```php",
  "echo $cuadrilla[0];   // Kelsier",
  "echo $cuadrilla[2];   // Ham",
  "```",
  "",
  "Y para saber cuántos hay, `count`:",
  "",
  "```php",
  "echo count($cuadrilla);   // 3",
  "```",
  "",
  "## Recorrer: `foreach`",
  "",
  "Es la manera de PHP de pasar por todos los elementos de una lista, uno a uno:",
  "",
  "```php",
  "foreach ($cuadrilla as $persona) {",
  "    echo $persona . PHP_EOL;",
  "}",
  "```",
  "",
  "`$persona` es una variable que tú nombras y que vale una cosa distinta en cada",
  "vuelta. No hay que llevar la cuenta de por dónde va: eso lo hace `foreach`.",
  "",
  "## El acumulador",
  "",
  "Sumar una lista es el ejercicio con el que se aprende el patrón más repetido de la",
  "programación: una variable **fuera** del bucle que se va llenando **dentro**.",
  "",
  "```php",
  "$total = 0;",
  "foreach ([2, 5, 9] as $numero) {",
  "    $total += $numero;",
  "}",
  "echo $total;   // 16",
  "```",
  "",
  "`$total += $numero` es lo mismo que `$total = $total + $numero`, escrito corto.",
  "",
  "Lo importante es **dónde** vive `$total`. Si se declara dentro del bucle, se pone a",
  "cero en cada vuelta y al final vale lo que valía el último elemento. Es el fallo",
  "número uno con los acumuladores, y pasa en todos los lenguajes.",
  "",
  "## Pruébalo tú",
  "",
  "¿Qué devuelve esta función con `[4, 4, 4]`?",
  "",
  "```php",
  "function mal(array $numeros): int",
  "{",
  "    foreach ($numeros as $numero) {",
  "        $total = 0;",
  "        $total += $numero;",
  "    }",
  "    return $total;",
  "}",
  "```",
  "",
  "Devuelve 4. Y con `[1, 2, 3]` devuelve 3, que casualmente es el número de",
  "elementos: dos maneras distintas de que un fallo parezca otra cosa.",
  "",
  "## Cómo se plantea",
  "",
  "Ante un «recorre esto y saca un número», escribe siempre las tres líneas en este",
  "orden antes de pensar en nada más: la variable a cero, el bucle vacío, y el",
  "`return` al final. Luego rellenas el bucle. Poniendo las tres primero, el error de",
  "declarar dentro no llega a ocurrir.",
  "",
  "## Los casos límite",
  "",
  "La lista vacía. `count([])` es 0 y sumar una lista vacía tiene que dar 0, no un",
  "error: el acumulador ya vale 0 antes de entrar al bucle, así que sale gratis. Los",
  "negativos también entran solos, que es la ventaja de sumar en vez de comparar.",
)
