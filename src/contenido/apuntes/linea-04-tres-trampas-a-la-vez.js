/**
 * El apunte de Raoden para «linea-04-tres-trampas-a-la-vez».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Tres trampas del camino en la misma consulta, y un resultado perfectamente creíble. Es lo",
  "que pasa en la vida real: los fallos no vienen de uno en uno y con carteles.",
  "",
  "## Las tres, y qué se lleva cada una",
  "",
  "```sql",
  "FROM puestos AS p",
  "JOIN ventas AS v ON v.puesto_id = p.id     -- 1",
  "WHERE p.gremio_id <> 1                      -- 2",
  "GROUP BY p.id                               -- con COUNT(*) arriba -- 3",
  "```",
  "",
  "**El `JOIN` que no es `LEFT`.** Se lleva `La muralla`, que no ha vendido nada. Nueve",
  "puestos entran y ocho llegan al `WHERE`.",
  "",
  "**El `<>` sobre una columna con nulos.** Se lleva los tres escribas -que es lo que se",
  "pedía- **y también el tenderete**, cuyo `gremio_id` es nulo. Cuatro filas, y una de las",
  "cuatro se va sin que nadie la haya nombrado.",
  "",
  "**El `COUNT(*)`**, que aquí **no hace daño**, y eso es lo interesante.",
  "",
  "## Por qué el COUNT(*) no miente esta vez",
  "",
  "`COUNT(*)` cuenta filas, y miente cuando en un montón hay una fila vacía. Esas filas",
  "vacías las fabrica un `LEFT JOIN`, y aquí el `JOIN` es normal: **todo lo que llega a los",
  "montones son ventas de verdad**.",
  "",
  "Así que en esta consulta `COUNT(*)` y `COUNT(v.id)` dan lo mismo.",
  "",
  "Eso importa por dos motivos. El primero es que saber **por qué** un fallo no aparece es",
  "tan útil como saber por qué aparece: la regla no es «`COUNT(*)` está prohibido», es",
  "«`COUNT(*)` cuenta filas, y después de un `LEFT JOIN` hay filas que no son datos».",
  "",
  "El segundo es más incómodo: si mañana alguien cambia ese `JOIN` por un `LEFT JOIN` -con",
  "toda la razón, para no perder puestos- el `COUNT(*)` **empieza a mentir** sin que nadie",
  "haya tocado esa línea. Escribir `COUNT(v.id)` desde el principio es gratis y aguanta el",
  "cambio.",
  "",
  "## Cómo se plantea: una trampa a la vez",
  "",
  "Con tres cosas a la vez, la única manera de no perderse es quitarlas:",
  "",
  "1. Ejecuta solo el `FROM` y la unión. **Cuenta.** Ocho, no nueve: ahí está la primera.",
  "2. Añade el `WHERE`. **Cuenta.** Cuatro, no cinco: ahí está la segunda.",
  "3. Añade el `GROUP BY`. Cuatro montones.",
  "4. Y pregúntate por cada agregado si su montón puede tener filas vacías.",
  "",
  "Cuatro ejecuciones y las tres trampas identificadas. Leyendo la consulta entera, ninguna",
  "salta a la vista, y eso es exactamente lo que hace que se publiquen informes así.",
  "",
  "## Pruébalo tú",
  "",
  "Arréglala del todo: `LEFT JOIN`, `WHERE (gremio_id <> 1 OR gremio_id IS NULL)` y",
  "`COUNT(v.id)`. Deberían salir seis filas y una de ellas con un cero. Compara ese informe",
  "con el de cuatro filas y pregúntate cuál habrías firmado sin mirar.",
)
