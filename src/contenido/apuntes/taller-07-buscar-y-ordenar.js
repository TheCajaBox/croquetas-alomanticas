/**
 * El apunte de Wax para «taller-07-buscar-y-ordenar».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
    "## Buscar uno",
    "",
    "```js",
    "casos.find((caso) => caso.titulo === 'Bleeder')     // el objeto, o undefined",
    "casos.findIndex((caso) => caso.recompensa > 1000)   // la posición, o -1",
    "```",
    "",
    "`find` para en cuanto encuentra el primero: no recorre la lista entera si no hace",
    "falta. Y devuelve `undefined` cuando no hay nada, así que si te piden `null`, hay",
    "que traducirlo: `?? null`.",
    "",
    "## Comprobar",
    "",
    "```js",
    "casos.every((caso) => caso.agente)   // ¿TODOS cumplen?",
    "casos.some((caso) => !caso.agente)   // ¿ALGUNO cumple?",
    "```",
    "",
    "Con la lista vacía, `every` da `true` y `some` da `false`. Suena raro y es lo",
    "correcto: «todos los casos de una lista sin casos están asignados» no es mentira.",
    "",
    "## Ordenar, que es donde está la trampa",
    "",
    "**Trampa uno: `sort` estropea tu lista.** No devuelve una lista nueva, reordena la",
    "que le des y encima te la devuelve, así que parece que sí.",
    "",
    "```js",
    "const ordenados = casos.sort(...)   // casos también ha quedado ordenado",
    "const ordenados = [...casos].sort(...)   // así no",
    "```",
    "",
    "**Trampa dos: por defecto ordena como texto.**",
    "",
    "```js",
    "[10, 9, 100].sort()      // [10, 100, 9]  ¡como en un diccionario!",
    "```",
    "",
    "Para números hay que decirle cómo comparar. La función recibe dos elementos y",
    "devuelve un número: negativo si el primero va antes, positivo si va después, cero",
    "si da igual.",
    "",
    "```js",
    "numeros.sort((a, b) => a - b)      // de menor a mayor",
    "numeros.sort((a, b) => b - a)      // de mayor a menor",
    "```",
    "",
    "La resta funciona porque el signo dice el orden. Con objetos, se resta el campo:",
    "",
    "```js",
    "[...casos].sort((a, b) => b.recompensa - a.recompensa)",
    "```",
    "",
    "Y con textos no se puede restar; ahí se usa `a.nombre.localeCompare(b.nombre)`,",
    "que además entiende de acentos y de eñes.",
)
