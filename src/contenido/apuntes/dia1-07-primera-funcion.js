/**
 * El apunte de Wax para «dia1-07-primera-funcion».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Una función es un encargo con nombre. Le das algo, hace su trabajo, y te devuelve",
  "un resultado. Igual que cuando mando a alguien a comprobar una dirección: le doy",
  "la dirección, se va, y vuelve con la respuesta.",
  "",
  "```js",
  "function saludar(nombre) {",
  "  return `Buenas, ${nombre}.`",
  "}",
  "",
  "saludar('Wayne')      // 'Buenas, Wayne.'",
  "```",
  "",
  "Cuatro piezas, y conviene nombrarlas bien porque las vas a oír toda la vida:",
  "",
  "- `function` — «voy a definir un encargo»",
  "- `saludar` — el nombre. Se elige como el de una variable, pero suele ser un",
  "  verbo: la función **hace** algo",
  "- `(nombre)` — el **parámetro**: lo que hay que darle para que pueda trabajar",
  "- `return` — lo que entrega al terminar",
  "",
  "## Definir no es llamar",
  "",
  "Esta distinción cuesta un par de días y luego es evidente.",
  "",
  "Escribir la función no ejecuta nada. Es como dejar unas instrucciones escritas",
  "encima de la mesa: existen, pero nadie las ha seguido todavía.",
  "",
  "```js",
  "function saludar(nombre) {        // aquí no pasa nada",
  "  return `Buenas, ${nombre}.`",
  "}",
  "",
  "saludar('Wayne')                  // aquí sí: ahora se ejecuta",
  "```",
  "",
  "Los paréntesis del final son los que dan la orden. Sin ellos, `saludar` a secas",
  "no llama a nadie: es la función misma, el papel con las instrucciones. Verlo",
  "escrito sin paréntesis y esperar que se ejecute es un clásico.",
  "",
  "## Parámetro y argumento",
  "",
  "Dos palabras para dos cosas parecidas, y ahora que las ves juntas ya no las vas a",
  "confundir:",
  "",
  "- El **parámetro** es el hueco que dejas al definirla: `nombre`.",
  "- El **argumento** es lo que metes en el hueco al llamarla: `'Wayne'`.",
  "",
  "El parámetro es una variable que solo existe **dentro** de la función y que nace",
  "valiendo lo que le hayas pasado. Fuera no existe:",
  "",
  "```js",
  "function saludar(nombre) {",
  "  return `Buenas, ${nombre}.`",
  "}",
  "",
  "console.log(nombre)     // error: aquí fuera no hay ningún nombre",
  "```",
  "",
  "Eso es bueno, no una limitación. Significa que puedes usar el nombre `nombre`",
  "dentro de veinte funciones distintas sin que se pisen.",
  "",
  "## El return, que es donde está el 90% de los tropiezos",
  "",
  "`return` hace dos cosas a la vez, y la segunda se olvida: **entrega el valor** y",
  "**termina la función ahí mismo**. Lo que venga después no se ejecuta.",
  "",
  "```js",
  "function saludar(nombre) {",
  "  return `Buenas, ${nombre}.`",
  "  console.log('esto no se ejecuta nunca')",
  "}",
  "```",
  "",
  "Y una función sin `return` devuelve `undefined`. No da error, no avisa: devuelve",
  "«nada» y sigue.",
  "",
  "```js",
  "function saludar(nombre) {",
  "  `Buenas, ${nombre}.`         // se calcula y se tira",
  "}",
  "",
  "saludar('Wayne')               // undefined",
  "```",
  "",
  "Ese es el fallo número uno del principio y el síntoma es siempre el mismo: te",
  "sale `undefined` donde esperabas un texto. Cuando lo veas, mira si te falta el",
  "`return`.",
  "",
  "Y el hermano de ese fallo, ya lo viste: escribir en vez de devolver.",
  "",
  "```js",
  "function saludar(nombre) {",
  "  console.log(`Buenas, ${nombre}.`)    // lo enseña y lo tira",
  "}",
  "```",
  "",
  "En pantalla se ve el saludo, así que parece que funciona. Pero la función",
  "devuelve `undefined`, y en cuanto alguien intente usar el resultado se rompe.",
  "`console.log` es para que mires tú; `return` es para que el programa siga.",
  "",
  "## Para qué sirven de verdad",
  "",
  "Tres razones, y van de menos a más importante.",
  "",
  "**No repetirte.** Si el saludo aparece en ocho sitios y hay que cambiar la coma,",
  "lo cambias en un sitio.",
  "",
  "**Ponerle nombre a una idea.** Esto vale incluso para una función de una línea:",
  "",
  "```js",
  "if (edad >= 18 && tieneDocumento && !estaFichado) { ... }",
  "",
  "if (puedeEntrar(persona)) { ... }",
  "```",
  "",
  "La segunda se lee. La primera hay que descifrarla. Una función es la forma de",
  "decir «esto de aquí significa *puede entrar*», y a partir de ahí quien lea tu",
  "código puede seguir adelante sin pararse.",
  "",
  "**Poder probar las cosas por separado.** Una función pequeña con entradas y",
  "salidas claras se puede comprobar sola, sin montar el programa entero. Es",
  "exactamente lo que hacen los tests de este juego con lo que tú escribes.",
  "",
  "## Cómo se sabe si una función está bien planteada",
  "",
  "Tres preguntas, y son las mismas que te harán en cualquier revisión de código:",
  "",
  "**¿Hace una sola cosa?** Si al explicar qué hace tienes que usar un «y», suelen",
  "ser dos funciones. «Calcula el total **y** lo imprime **y** lo guarda» son tres.",
  "",
  "**¿Su nombre dice lo que hace?** Si `saludar` además borra un registro, el nombre",
  "miente, y un nombre que miente es peor que no tener nombre.",
  "",
  "**¿Depende solo de lo que le pasas?** Una función que solo mira sus parámetros y",
  "devuelve un resultado se entiende leyéndola, se prueba sola y no da sorpresas.",
  "En cuanto empieza a depender de cosas de fuera, hay que leer el programa entero",
  "para saber qué hará.",
  "",
  "No hace falta que lo apliques hoy con una función de una línea. Guárdalo: en",
  "cuanto escribas la número veinte, estas tres preguntas son la diferencia entre",
  "un programa que se puede tocar y uno al que nadie quiere acercarse.",
)
