/**
 * El apunte de Raoden para «linea-02-el-informe-completo».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "El informe de los gremios es la consulta que has escrito de cinco maneras distintas en",
  "cinco mundos, y en cada una faltaba una pieza. Aquí van todas.",
  "",
  "## Las tres trampas, en una sola consulta",
  "",
  "**Uno: la unión no puede perder gremios.** Se empieza por `gremios` y se baja con dos",
  "`LEFT JOIN`. Las **dos** tienen que ser `LEFT`: si la segunda es normal, un gremio con",
  "puestos pero sin ventas desaparece, y eso es una fila que falta sin ningún aviso.",
  "",
  "**Dos: contar puestos después de unir con las ventas los cuenta una vez por venta.** Los",
  "escribas tienen tres puestos y siete ventas, así que `COUNT(p.id)` diría siete. Hace",
  "falta `COUNT(DISTINCT p.id)`.",
  "",
  "**Tres: el hueco hay que decidirlo.** `SUM` sobre un montón sin ventas devuelve nulo, y",
  "un informe con un blanco donde va un cero se lee mal y se copia peor.",
  "",
  "## Por qué hacen falta las dos uniones",
  "",
  "Merece la pena verlo despacio, porque es lo que separa un informe correcto de uno que",
  "parece correcto:",
  "",
  "```sql",
  "FROM gremios AS g",
  "LEFT JOIN puestos AS p ON p.gremio_id = g.id      -- los gremios sin puestos se salvan",
  "LEFT JOIN ventas  AS v ON v.puesto_id = p.id      -- los puestos sin ventas también",
  "```",
  "",
  "El gremio de los aones no tiene puestos, así que la primera unión le fabrica una fila con",
  "`p.*` a nulo. Y entonces la segunda tiene que unir `v.puesto_id = NULL`, que no empareja",
  "con nada: si fuera un `JOIN` normal, esa fila se caería ahí y el gremio desaparecería",
  "**después** de haber sido salvado.",
  "",
  "Es el mismo mecanismo del `WHERE` que deshace un `LEFT JOIN`, una línea más abajo. Cuando",
  "hay una cadena de uniones, **basta con que una no sea `LEFT` para tirar lo que las",
  "anteriores salvaron**.",
  "",
  "## El COUNT que va bien y el que va mal, juntos",
  "",
  "En esta consulta hay dos `COUNT` y son distintos a propósito:",
  "",
  "```sql",
  "COUNT(DISTINCT p.id)  -- puestos: hay que quitar las copias que hizo la unión",
  "COUNT(v.id)           -- ventas: hay que no contar las filas vacías",
  "```",
  "",
  "Los dos son `COUNT` de una columna y ninguno es `COUNT(*)`. El primero se defiende de la",
  "duplicación y el segundo de los nulos, que son los dos daños que hace una unión.",
  "",
  "## La comprobación de siempre",
  "",
  "Los totales tienen que sumar 1.110 y no 1.275: las 165 monedas del tenderete no son de",
  "ningún gremio. Ese número **hay que sabérselo antes de ejecutar**, y es la única manera",
  "de darse cuenta de que una unión duplica.",
  "",
  "## Cómo se plantea",
  "",
  "Por pasos, contando en cada uno:",
  "",
  "1. `FROM gremios` con el nombre. **Seis filas.**",
  "2. La primera unión. Nueve filas -ocho puestos con gremio más el gremio sin puestos- y",
  "   **seis gremios distintos**.",
  "3. La segunda. Cuenta, y predice el número antes de mirarlo.",
  "4. `GROUP BY`. **Seis filas otra vez.** Si son cinco, alguna unión no es `LEFT`.",
  "5. Los agregados, uno a uno, mirando la fila de los aones después de cada uno.",
  "",
  "El paso cuatro es el que caza el fallo gordo, y se caza contando.",
  "",
  "## Pruébalo tú",
  "",
  "Cambia la segunda unión por un `JOIN` normal y cuenta las filas: cinco gremios en vez de",
  "seis. Y el que falta es el que el informe existía para encontrar.",
)
