/**
 * El apunte de Raoden para «trazos-02-el-primer-trazo».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Meter una fila:",
  "",
  "```sql",
  "INSERT INTO gremios (nombre) VALUES ('herreros');",
  "```",
  "",
  "Las columnas entre paréntesis, `VALUES`, y los valores entre paréntesis en el mismo",
  "orden. Los textos con comillas simples y los números sin comillas, como siempre.",
  "",
  "## Nombrar las columnas no es opcional",
  "",
  "Se puede escribir sin nombres, dando los valores en el orden de la tabla:",
  "",
  "```sql",
  "INSERT INTO gremios VALUES (5, 'herreros');   -- funciona, y es una mala idea",
  "```",
  "",
  "Funciona hoy. El día que alguien añada una columna a la tabla, ese `INSERT` deja de",
  "cuadrar -o peor, cuadra y mete el valor en la columna equivocada-. Nombrar las columnas",
  "cuesta cinco palabras y la orden sigue significando lo mismo dentro de tres años.",
  "",
  "## Lo que no hace falta dar",
  "",
  "Dos cosas se rellenan solas y las dos están en el esquema:",
  "",
  "**La clave primaria.** Si la columna es `INTEGER PRIMARY KEY`, la base pone el",
  "siguiente número. Ponerlo a mano funciona y es la manera de chocar con un `id` que ya",
  "existe el día que te equivoques con la cuenta.",
  "",
  "**Las columnas con `DEFAULT`.** `abierto INTEGER NOT NULL DEFAULT 1` significa «si no",
  "dices nada, vale 1». Eso es una decisión de quien hizo la tabla -un puesto nuevo se",
  "supone abierto- y está escrita donde se puede leer.",
  "",
  "Lo que sí hace falta dar es todo lo que sea `NOT NULL` y no tenga `DEFAULT`. Si te",
  "olvidas, la orden falla, y eso es exactamente lo que quieres que pase.",
  "",
  "## Varias filas de una vez",
  "",
  "```sql",
  "INSERT INTO gremios (nombre) VALUES ('herreros'), ('tejedores'), ('barqueros');",
  "```",
  "",
  "Tres filas y una sola orden, que es lo que has visto en los `datos` de cada reto. Y",
  "tiene una propiedad que importa: es **una** orden, así que o entran las tres o no entra",
  "ninguna. Si la segunda choca con una regla, las tres se quedan fuera.",
  "",
  "## Cómo comprobar que ha ido bien",
  "",
  "Un `INSERT` no devuelve la fila que ha metido. Devuelve «1 fila cambiada», y para ver",
  "el resultado hay que preguntar:",
  "",
  "```sql",
  "SELECT * FROM gremios ORDER BY id DESC LIMIT 3;",
  "```",
  "",
  "Es lo que hacen los tests de este mundo, y por eso están escritos con `consulta(...)`:",
  "miran la tabla **después**, porque lo que hay que comprobar es el estado en que queda",
  "la base y no lo que la orden devuelve.",
  "",
  "## Cómo se plantea",
  "",
  "1. **Mira el esquema** y apunta qué columnas son obligatorias, cuáles tienen valor por",
  "   omisión y cuáles se rellenan solas.",
  "2. Escribe el `INSERT` con las columnas que de verdad tienes que dar.",
  "3. Ejecuta, mira el número de filas cambiadas, y **pregunta** para ver la fila.",
  "",
  "El paso uno es el que evita el error de dar una columna de más -que da error- y el de",
  "olvidar una obligatoria -que también-. Los dos errores son buenos: aparecen a la",
  "primera.",
  "",
  "## Pruébalo tú",
  "",
  "Después de resolverlo, ejecuta el mismo `INSERT` otra vez. La segunda vez falla: el",
  "nombre es `UNIQUE`. Lee el mensaje y quédate con él, porque es el que sale cuando un",
  "formulario se envía dos veces.",
)
