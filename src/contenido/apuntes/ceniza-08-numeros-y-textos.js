/**
 * El apunte de Kelsier para «ceniza-08-numeros-y-textos».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "PHP convierte tipos solo. Le das un texto donde esperaba un número y lo",
  "convierte; le das un número donde esperaba un texto y lo convierte. Eso ahorra",
  "trabajo casi siempre y produce el peor tipo de fallo el resto de las veces: el",
  "que no revienta, no avisa y sale mal.",
  "",
  "## `+` suma, `.` pega",
  "",
  "```php",
  "echo '5' + 5;    // 10   <- convierte y suma",
  "echo '5' . 5;    // 55   <- pega los dos",
  "```",
  "",
  "Cada símbolo tiene un trabajo. Que sean dos y no uno es lo que te salva de que",
  "una suma acabe pegando.",
  "",
  "## `==` frente a `===`",
  "",
  "```php",
  "var_dump(1 == '1');     // true    <- mismo valor, convierte",
  "var_dump(1 === '1');    // false   <- distinto tipo",
  "```",
  "",
  "**Usa `===` por defecto.** Es una tecla más y quita de golpe una familia entera de",
  "sorpresas. Reserva `==` para cuando la conversión sea justo lo que quieres, y",
  "cuando lo hagas, deja un comentario diciendo por qué.",
  "",
  "## Lo que cambió en PHP 8",
  "",
  "Esto conviene saberlo porque hay muchísimo código antiguo por ahí:",
  "",
  "```php",
  "// Antes de PHP 8:   0 == 'abc'  era  true   (!)",
  "// Desde PHP 8:      0 == 'abc'  es   false",
  "```",
  "",
  "El motivo del cambio: comparar un número con un texto convertía el texto a número,",
  "y cualquier texto que no empezara por un dígito valía cero. Eso hacía que",
  "`$clave == 0` fuera cierto para casi cualquier contraseña. Ahora, si el texto no",
  "parece un número, PHP convierte el número a texto y compara textos.",
  "",
  "Lo que **no** cambió: si los dos parecen números, se comparan como números.",
  "",
  "```php",
  "var_dump('10' == '1e1');   // true: los dos son diez",
  "```",
  "",
  "## `empty` no significa lo que parece",
  "",
  "`empty($x)` dice `true` para todo esto: `null`, `''`, `'0'`, `0`, `0.0`, `false` y",
  "`[]`. Para `empty`, **el cero está vacío**.",
  "",
  "```php",
  "$unidades = 0;",
  "if (empty($unidades)) {",
  "    // aquí entra, y casi nunca es lo que quieres",
  "}",
  "```",
  "",
  "Tres preguntas distintas y tres formas de preguntarlas:",
  "",
  "- ¿existe esta variable? → `isset($x)`",
  "- ¿tiene valor de verdad? → `$x !== null`",
  "- ¿está vacía en el sentido amplio? → `empty($x)`, y sabiendo lo que incluye",
  "",
  "## Cómo se plantea",
  "",
  "Ante una comparación que no cuadra, `var_dump` a los dos lados antes de tocar",
  "nada. Nueve de cada diez veces uno de los dos no es del tipo que creías, y el",
  "arreglo es ese y no la comparación.",
  "",
  "## Pruébalo tú",
  "",
  "Coge las seis frases del reto y pásalas por `var_dump` una por una. Verlas salir",
  "hace que se queden; leerlas, no tanto.",
)
