/**
 * El apunte de Raoden para «mercado-04-las-partes-de-un-resumen».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Con agrupar, la lista de cláusulas pasa de cinco a siete. El orden sigue sin",
  "negociarse:",
  "",
  "```sql",
  "SELECT · FROM · WHERE · GROUP BY · HAVING · ORDER BY · LIMIT",
  "```",
  "",
  "Y como siempre, lo que explica todo es el orden en que **trabaja** la base:",
  "",
  "1. `FROM` — de dónde, y las uniones.",
  "2. `WHERE` — tira filas. **Antes de agrupar.**",
  "3. `GROUP BY` — hace los montones.",
  "4. `HAVING` — tira **montones**.",
  "5. `SELECT` — calcula los agregados y pone los alias.",
  "6. `ORDER BY` — ordena.",
  "7. `LIMIT` — corta.",
  "",
  "## WHERE y HAVING: la confusión clásica",
  "",
  "Las dos filtran y no filtran lo mismo:",
  "",
  "```sql",
  "WHERE  dia <> 'miércoles'     -- «no cuentes las ventas del miércoles»",
  "HAVING SUM(monedas) > 150     -- «no me enseñes los puestos que no lleguen a 150»",
  "```",
  "",
  "El `WHERE` mira **una fila** y decide si entra en su montón. El `HAVING` mira **un",
  "montón entero** y decide si sale en el resultado.",
  "",
  "De ahí sale la regla que hay que saberse:",
  "",
  "> Si la condición se puede contestar mirando una sola fila, va en el `WHERE`. Si",
  "> hace falta el montón, va en el `HAVING`.",
  "",
  "Y la consecuencia práctica: **un `WHERE` no puede hablar de una suma**. Cuando el",
  "`WHERE` trabaja, no hay sumas. La base te lo dice, y el mensaje no siempre es claro:",
  "«misuse of aggregate function».",
  "",
  "Al revés también importa, aunque no dé error: un `HAVING` puede filtrar filas -en",
  "muchos casos da el mismo resultado- y hace que la base agrupe filas que iba a tirar.",
  "Lo peor no es el trabajo de más: es que se lee como si la condición hablara del",
  "montón cuando hablaba de una fila.",
  "",
  "## Los alias, y por qué el HAVING es el raro",
  "",
  "El `SELECT` trabaja **después** del `HAVING`. Así que esto, en teoría, no debería",
  "funcionar:",
  "",
  "```sql",
  "SELECT puesto_id, SUM(monedas) AS total",
  "FROM ventas GROUP BY puesto_id",
  "HAVING total > 150;      -- SQLite lo acepta; otras bases, no",
  "```",
  "",
  "SQLite lo acepta por cortesía y PostgreSQL no. Escribir el agregado entero en el",
  "`HAVING` funciona en todas partes y es lo que conviene:",
  "",
  "```sql",
  "HAVING SUM(monedas) > 150",
  "```",
  "",
  "El `ORDER BY` sí puede usar el alias en todas: va después del `SELECT`.",
  "",
  "## Cómo se plantea",
  "",
  "Cuando una consulta agrupada tenga dos condiciones, sepáralas antes de escribir:",
  "",
  "1. Escribe cada condición en una línea aparte.",
  "2. Para cada una, pregúntate: **¿podría contestar esto mirando una sola fila?**",
  "3. Las que sí, al `WHERE`. Las que no, al `HAVING`.",
  "",
  "Tres pasos y no hay manera de equivocarse. Sin hacerlos, la tentación es poner todo",
  "en el mismo sitio, y una de las dos va a estar mal.",
  "",
  "## Pruébalo tú",
  "",
  "Escribe `WHERE SUM(monedas) > 150` y ejecuta. Lee el error entero. Es uno de los",
  "cinco mensajes de esta base que conviene reconocer de vista, porque lo vas a ver",
  "cada vez que confundas las dos cláusulas.",
)
