/**
 * El apunte de Raoden para «mercado-08-el-informe-que-cuenta-mal».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Este es el fallo más silencioso del mundo, y se escribe haciendo todo bien. Alguien",
  "pone el `LEFT JOIN` para no perder ningún puesto -acierta-, agrupa por el",
  "identificador -acierta- y cuenta con `COUNT(*)`, que es lo que ha usado cinco veces",
  "hoy. Y el informe dice que un puesto sin ventas hizo una.",
  "",
  "## Qué hay dentro de ese montón",
  "",
  "Aquí está todo. Después de un `LEFT JOIN`, el montón de `La muralla` tiene **una",
  "fila**: la que el `LEFT` fabricó, con la mitad izquierda llena y todas las columnas",
  "de `ventas` a nulo.",
  "",
  "```sql",
  "p.nombre     | v.id | v.monedas",
  "La muralla   | NULL |   NULL",
  "```",
  "",
  "Una fila es una fila. `COUNT(*)` cuenta filas, así que dice uno. No está roto: está",
  "contestando a la pregunta que le has hecho, que no era la que querías hacer.",
  "",
  "## Los dos COUNT",
  "",
  "```sql",
  "COUNT(*)        -- 1: hay una fila en el montón",
  "COUNT(v.id)     -- 0: no hay ningún v.id que no sea nulo",
  "```",
  "",
  "`COUNT(columna)` cuenta **valores no nulos**. Y de ahí sale la regla de este reto,",
  "que hay que aplicar sin pensar:",
  "",
  "> Después de un `LEFT JOIN`, cuenta una **columna de la tabla de la derecha**, nunca",
  "> `COUNT(*)`.",
  "",
  "La columna que se cuenta suele ser la clave primaria de la derecha -`v.id`- porque",
  "es la que nunca puede ser nula cuando la fila existe de verdad. Contar `v.monedas`",
  "también funcionaría aquí, y dejaría de funcionar el día que una venta tenga las",
  "monedas sin rellenar.",
  "",
  "## Y por qué la suma sí estaba bien",
  "",
  "`SUM` se salta los nulos, así que en ese montón no suma nada y devuelve `NULL`.",
  "",
  "Es incómodo -un hueco en el informe donde debería haber un cero- y **no es falso**:",
  "dice «no hay ninguna venta que sumar». Un hueco se ve; un uno donde va un cero, no.",
  "",
  "Si el informe necesita el cero, se pide:",
  "",
  "```sql",
  "COALESCE(SUM(v.monedas), 0) AS total",
  "```",
  "",
  "`COALESCE` devuelve el primero de sus argumentos que no sea nulo. No es un arreglo",
  "sucio: es decir en voz alta qué significa el hueco en este informe, que es una",
  "decisión que alguien tiene que tomar.",
  "",
  "## Cómo se plantea: mirar el borde primero",
  "",
  "En un informe agrupado con `LEFT JOIN`, la fila que hay que mirar antes que ninguna",
  "es **la del grupo vacío**. Búscala por su nombre y comprueba sus cifras una por una:",
  "",
  "- Las cuentas tienen que ser cero, no uno.",
  "- Las sumas y medias tienen que ser cero o un hueco, y hay que haber decidido cuál.",
  "- El máximo y el mínimo, lo mismo.",
  "",
  "Si esa fila está bien, las demás casi seguro que también, porque son el caso fácil.",
  "Si nadie la mira, el informe se publica contando mal.",
  "",
  "## Pruébalo tú",
  "",
  "Ejecuta la consulta del reto con `COUNT(*)` y con `COUNT(v.id)`, una al lado de la",
  "otra en el mismo `SELECT`. Ocho filas iguales y una distinta. Esa novena es la razón",
  "de todo este apunte.",
)
