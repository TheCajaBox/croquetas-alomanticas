/**
 * El apunte de Raoden para «mercado-11-el-que-no-vendio-nada».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "El puesto que no ha vendido nada es el que más le interesa a quien pide el informe,",
  "y es el que todas las consultas de este mundo se han dejado fuera. Aquí se arregla,",
  "y hay que arreglar tres cosas a la vez.",
  "",
  "## Uno: empezar por la tabla que no puede perder a nadie",
  "",
  "```sql",
  "FROM puestos AS p",
  "LEFT JOIN ventas AS v ON v.puesto_id = p.id",
  "GROUP BY p.id",
  "```",
  "",
  "Agrupar la tabla de ventas nunca va a sacar un puesto sin ventas, porque agrupar no",
  "inventa filas. Empezando por `puestos`, el puesto está, y el `LEFT JOIN` le fabrica",
  "un montón con una fila vacía dentro.",
  "",
  "## Dos: contar la columna de la derecha, no las filas",
  "",
  "```sql",
  "COUNT(v.id) AS ventas      -- 0",
  "COUNT(*)    AS ventas      -- 1, y es mentira",
  "```",
  "",
  "Ya tiene su reto y su apunte. La regla, otra vez: después de un `LEFT JOIN`, se",
  "cuenta una columna de la tabla de la derecha.",
  "",
  "## Tres: decidir qué significa el hueco",
  "",
  "`AVG` y `SUM` se saltan los nulos, así que en ese montón no hay nada que promediar y",
  "devuelven `NULL`. No es falso -no hay ninguna venta, así que no hay media- y en un",
  "informe se lee mal: un hueco donde se espera un número invita a rellenarlo a ojo.",
  "",
  "```sql",
  "COALESCE(ROUND(AVG(v.monedas), 1), 0) AS media",
  "```",
  "",
  "`COALESCE` devuelve el primero de sus argumentos que no sea nulo. Y fíjate en el",
  "orden de las dos funciones, que importa:",
  "",
  "- `COALESCE(ROUND(algo, 1), 0)` — primero redondea, y si no había nada, cero.",
  "- `ROUND(COALESCE(algo, 0), 1)` — también funciona, y redondea un cero, que es",
  "  trabajo tonto.",
  "",
  "Las dos dan lo mismo aquí. Escribir la de dentro hacia fuera -calcula, luego tapa el",
  "hueco- es la que se lee como se piensa.",
  "",
  "## Un cero y un nulo no son lo mismo, y hay que elegir",
  "",
  "Poner el cero es una decisión, no una corrección. Piénsalo así:",
  "",
  "- **`NULL`** dice «no hay datos». Un puesto que no ha vendido nada no tiene media.",
  "- **`0`** dice «el dato es cero». Y una media de cero monedas es una afirmación",
  "  distinta: suena a que vendió y no sacó nada.",
  "",
  "En un informe para leer, el cero se entiende mejor y es lo que se suele querer. En",
  "unos datos que van a alimentar otro cálculo, el nulo es más honrado: ponerle un cero",
  "a lo que no existe mete un valor falso en la siguiente media.",
  "",
  "Lo que no vale es no elegir. Dejarlo en nulo por no escribir el `COALESCE` no es",
  "elegir el nulo: es no haberlo mirado.",
  "",
  "## Cómo se plantea",
  "",
  "Para cualquier informe que tenga que incluir los grupos vacíos:",
  "",
  "1. La tabla del `FROM` es la de las cosas que tienen que salir todas.",
  "2. `LEFT JOIN` hacia los datos.",
  "3. `GROUP BY` por el identificador de la tabla de la izquierda.",
  "4. **Cuenta las filas del resultado**: tantas como cosas hay en la tabla de la",
  "   izquierda. Si son menos, la unión no es `LEFT`.",
  "5. **Busca la fila del grupo vacío por su nombre** y repasa sus cifras una por una.",
  "",
  "El paso cinco es el que caza los dos fallos que quedan, y no hay manera de cazarlos",
  "mirando el número de filas.",
  "",
  "## Pruébalo tú",
  "",
  "Pide en el mismo `SELECT` las cuatro cosas: `COUNT(*)`, `COUNT(v.id)`, `SUM(...)` y",
  "`COALESCE(SUM(...), 0)`. Nueve filas, y en la de `La muralla` cuatro columnas que",
  "dicen 1, 0, nada y 0. Cuatro maneras de contestar a lo mismo y solo dos son ciertas.",
)
