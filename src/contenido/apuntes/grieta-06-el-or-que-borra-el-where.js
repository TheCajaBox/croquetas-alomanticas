/**
 * El apunte de Gaotona para «grieta-06-el-or-que-borra-el-where».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Cuatro filas, y la columna que hay que mirar es la segunda: **si el filtro sigue en pie**.",
  "",
  "## La prioridad de AND y OR",
  "",
  "Esto es lo que hace posible el ataque y casi nunca se enseña: en SQL, como en casi todos",
  "los lenguajes, **`AND` se evalúa antes que `OR`**. Así que:",
  "",
  "```sql",
  "WHERE secreto = 0 AND documento LIKE '%%' OR 1=1",
  "-- la base lo lee así:",
  "WHERE (secreto = 0 AND documento LIKE '%%') OR 1=1",
  "```",
  "",
  "El paréntesis puede ser verdadero o falso: da igual, porque `OR 1=1` es verdadero siempre.",
  "El `WHERE` entero es verdadero para todas las filas.",
  "",
  "**Un `OR` a la derecha de un `WHERE` anula todo lo que había a la izquierda.** Eso es el",
  "ataque completo, y es la razón por la que `' OR 1=1 --` es la carga más famosa que existe.",
  "",
  "## La fila (4), que es la que da miedo",
  "",
  "```sql",
  "… AND documento LIKE '%nada%' OR secreto = 1",
  "```",
  "",
  "Devuelve **tres filas**: exactamente las tres que estaban prohibidas, y ninguna de las",
  "públicas, porque `LIKE '%nada%'` no encuentra nada.",
  "",
  "Y eso es más peligroso que el `OR 1=1` por un motivo operativo: **no llama la atención**.",
  "Una consulta que de pronto devuelve la tabla entera aparece en cualquier vigilancia —un",
  "pico de filas, una respuesta enorme—. Una que devuelve tres filas parece una búsqueda",
  "normal. Quien sabe lo que busca no pide todo: pide lo que quiere.",
  "",
  "## El `WHERE` no se ha perdido",
  "",
  "Esto es lo que conviene entender bien, porque es contraintuitivo: el `WHERE` sigue ahí,",
  "escrito en el código, tal cual. `secreto = 0` está en el fichero y nadie lo ha borrado.",
  "",
  "Lo que ha pasado es que la condición ha dejado de ser la que el programador escribió,",
  "porque el texto del usuario **es parte de la condición**. Escribir `AND secreto = 0` no",
  "garantiza nada si después de esa línea alguien puede añadir un `OR`.",
  "",
  "De ahí sale una consecuencia práctica incómoda: **no puedes garantizar una regla de",
  "seguridad con un `WHERE` en una consulta construida con cadenas**. Ni con dos `WHERE`, ni",
  "con paréntesis. Mientras el dato entre en el texto, la condición es negociable.",
  "",
  "## Con el parámetro",
  "",
  "```sql",
  "WHERE secreto = 0 AND documento LIKE :busca",
  "```",
  "",
  "El `%`, las comillas y el `--` del valor son letras de un texto que se compara. La",
  "condición sigue siendo la que se escribió, con los paréntesis implícitos que se escribieron",
  "y nada más. La regla de seguridad vuelve a ser una garantía.",
  "",
  "Y una costumbre que ayuda incluso con parámetros: **pon los paréntesis a mano** cuando",
  "mezcles `AND` y `OR`. No por seguridad, por legibilidad: la mitad de los errores lógicos",
  "de una consulta larga son de prioridad, y los paréntesis los hacen imposibles.",
  "",
  "## Cómo se plantea",
  "",
  "Las dos primeras filas son el programa funcionando. Para las dos últimas, pon los",
  "paréntesis a mano según la prioridad y mira qué queda. En (4) fíjate en que la primera",
  "mitad no encuentra nada, así que lo que sale es solo lo que trae la segunda.",
)
