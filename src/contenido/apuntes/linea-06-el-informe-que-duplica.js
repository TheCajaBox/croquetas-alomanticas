/**
 * El apunte de Raoden para «linea-06-el-informe-que-duplica».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Una unión que no se usa. Aparece al copiar y pegar consultas, al arreglar una a medias, y",
  "al añadir una tabla «por si acaso» y quitar después la columna que la necesitaba.",
  "",
  "```sql",
  "JOIN puestos AS p  ON p.gremio_id = g.id",
  "JOIN ventas  AS v  ON v.puesto_id = p.id",
  "JOIN puestos AS p2 ON p2.gremio_id = g.id   -- p2 no sale en ninguna otra línea",
  "```",
  "",
  "No da error. Y no es inofensiva: **multiplica las filas**.",
  "",
  "## Qué pasa exactamente",
  "",
  "Cada fila del resultado intermedio se une con todas las filas de `p2` que cumplan la",
  "condición, que son **todos los puestos de su gremio**. Así que:",
  "",
  "```sql",
  "-- Escribas: 3 puestos, así que cada venta suya sale 3 veces.",
  "-- 305 monedas de verdad -> 915 en el informe.",
  "-- Canteros: 2 puestos -> sus 95 salen 190.",
  "-- Y los gremios de un solo puesto salen bien. Eso es lo que más despista.",
  "```",
  "",
  "Los gremios de un puesto salen correctos, así que la mitad del informe está bien. Y el",
  "número de filas es el que tenía que ser: cinco gremios con ventas, cinco filas.",
  "",
  "## Por qué la columna de puestos sale bien",
  "",
  "Y esto es lo cruel: `COUNT(DISTINCT p.id)` **se salva**. Las copias son idénticas, así que",
  "el `DISTINCT` las quita y el número sale correcto.",
  "",
  "Así que el informe tiene una columna correcta al lado de otra inflada, y eso lo hace",
  "parecer más de fiar. Si las dos estuvieran mal, alguien lo habría notado.",
  "",
  "De aquí sale una advertencia sobre el `DISTINCT` que vale la pena tener: **cuando un",
  "`DISTINCT` es lo único que hace que una columna salga bien, hay una duplicación por",
  "debajo**, y las columnas que no lo llevan están mal. El `DISTINCT` no arregla la",
  "consulta: la esconde.",
  "",
  "## El arreglo que no arregla",
  "",
  "La tentación al ver el total inflado es ponerle un `DISTINCT` también:",
  "",
  "```sql",
  "SUM(DISTINCT v.monedas)   -- el número cambia y sigue siendo falso",
  "```",
  "",
  "Eso suma **una vez cada cantidad distinta**, así que dos ventas de 35 monedas cuentan",
  "como una. El número baja, deja de ser el inflado, y no es el correcto. Es el arreglo que",
  "hace que el fallo deje de verse.",
  "",
  "Lo que hay que arreglar es la unión.",
  "",
  "## La comprobación que lo caza, y es la única",
  "",
  "**Que los totales de los montones sumen el total de la tabla.**",
  "",
  "El número de filas no lo delata -cinco gremios son cinco gremios- y las columnas contadas",
  "con `DISTINCT` tampoco. Lo único que lo delata es la suma, y hay que sabérsela de",
  "antemano: 1.275 el mercado, 165 sin gremio, 1.110 el informe.",
  "",
  "Es la tercera de las tres cuentas del reto anterior, y es la que caza el fallo que las",
  "otras dos no ven.",
  "",
  "## Cómo se plantea: leer las uniones",
  "",
  "Delante de una consulta con varias uniones, una revisión de treinta segundos:",
  "",
  "1. Lista los alias: `g`, `p`, `v`, `p2`.",
  "2. Para cada uno, **búscalo en el resto de la consulta**. En el `SELECT`, en el `WHERE`,",
  "   en el `GROUP BY`, en otro `ON`.",
  "3. El que no aparezca en ningún sitio, fuera.",
  "",
  "Tres pasos mecánicos y sin pensar. Es la clase de revisión que conviene hacer siempre,",
  "porque una unión de sobra no se nota leyendo: se nota contando.",
  "",
  "## Pruébalo tú",
  "",
  "Quita la línea de `p2` y suma los totales: 1.110. Ahora vuelve a ponerla y prueba el",
  "`SUM(DISTINCT ...)`. Tres números distintos para el mismo informe, y solo uno es el",
  "bueno.",
)
