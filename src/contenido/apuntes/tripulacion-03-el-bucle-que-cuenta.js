/**
 * El apunte de Kelsier para «tripulacion-03-el-bucle-que-cuenta».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Un bucle es lo que evita escribir la misma línea cien veces. Hay tres formas de escribirlo",
  "en PHP y las tres hacen lo mismo por dentro; lo que cambia es cuál se lee mejor según lo",
  "que estés haciendo.",
  "",
  "## `while`: mientras se cumpla",
  "",
  "```php",
  "$quedan = 3;",
  "while ($quedan > 0) {",
  "    echo $quedan;",
  "    $quedan--;",
  "}",
  "```",
  "",
  "Tres piezas y **hacen falta las tres**: algo que valga algo antes de empezar, una condición,",
  "y algo dentro que acerque la condición a dejar de cumplirse. Si te olvidas de lo tercero, el",
  "bucle no termina nunca. Aquí eso no es grave: el juego corta la ejecución a los tres",
  "segundos, así que puedes probarlo a propósito para ver qué se siente.",
  "",
  "## `for`: cuando cuentas",
  "",
  "```php",
  "for ($i = 1; $i <= 5; $i++) {",
  "    echo $i;",
  "}",
  "```",
  "",
  "Es un `while` con las tres piezas escritas juntas: **con qué empieza**, **hasta cuándo",
  "sigue**, **qué cambia cada vuelta**. Separadas por punto y coma, en ese orden. Cuando el",
  "bucle es «hazlo N veces» o «del 1 al N», el `for` lo dice en una línea y no hay que buscar",
  "el incremento entre el resto del código.",
  "",
  "`$i++` suma uno. `$i--` resta uno. `$i += 2` suma dos.",
  "",
  "## `foreach`: cuando recorres una lista",
  "",
  "```php",
  "foreach ($metales as $metal) {",
  "    echo $metal;",
  "}",
  "```",
  "",
  "Ya lo conoces. **Es el que hay que usar siempre que se pueda**: no hay contador que",
  "equivocarse, ni condición que ajustar, ni forma de pasarse del final. Recorrer un array con",
  "un `for` y `count` funciona y es una manera de buscarse problemas.",
  "",
  "## Cómo se plantea un bucle que acumula",
  "",
  "Siempre las mismas tres líneas y en este orden, **antes** de pensar en nada más:",
  "",
  "```php",
  "$total = 0;                    // 1. el acumulador, FUERA",
  "for ($i = 1; $i <= $n; $i++) { // 2. el bucle, vacío",
  "}",
  "return $total;                 // 3. devolverlo, DESPUÉS",
  "```",
  "",
  "Y luego se rellena el bucle. Escribiendo las tres primero, el error de declarar el",
  "acumulador dentro no llega a ocurrir; y ese error es el más común que hay, porque no",
  "revienta: simplemente devuelve el último valor en vez de la suma. Con una lista de un",
  "elemento incluso parece que funciona.",
  "",
  "## Construir una lista dentro de un bucle",
  "",
  "```php",
  "$numeros = [];",
  "while ($desde > 0) {",
  "    $numeros[] = $desde;",
  "    $desde--;",
  "}",
  "```",
  "",
  "Mismo patrón: el array vacío fuera, los corchetes vacíos para añadir al final.",
  "",
  "## Los casos límite",
  "",
  "El **cero vueltas**. `while (0 > 0)` no entra ni una vez, y eso casi siempre es lo",
  "correcto: una cuenta atrás desde 0 es una lista vacía, no un error. Pruébalo siempre,",
  "porque es el caso en el que el acumulador tiene que valer ya lo que va a devolver.",
  "",
  "Y el **uno**: es el caso que hace parecer correctos los bucles mal escritos. Si funciona",
  "con uno, prueba con tres antes de darlo por bueno.",
)
