/**
 * El apunte de Wax para «elendel-06-numeros-que-enganan».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Los números de JavaScript tienen unas cuantas rarezas que no son rarezas de JavaScript: son",
  "cómo funcionan los decimales en cualquier ordenador. Conviene conocerlas **antes** de",
  "escribir algo que maneje dinero, porque después se conocen igual pero más caras.",
  "",
  "## `0.1 + 0.2` no es `0.3`",
  "",
  "```js",
  "0.1 + 0.2           // 0.30000000000000004",
  "0.1 + 0.2 === 0.3   // false",
  "```",
  "",
  "No es un fallo. Los decimales se guardan en binario, y 0,1 en binario es periódico —igual",
  "que un tercio en decimal es 0,333… y no cabe—. Al guardarlo se corta, y al sumar dos números",
  "cortados el error asoma.",
  "",
  "Pasa igual en Python, en Java y en C. Lo que tiene JavaScript de particular es que solo hay",
  "**un** tipo de número, así que no puedes escaparte eligiendo otro.",
  "",
  "**Qué se hace en la práctica.** Para comparar, con un margen:",
  "",
  "```js",
  "Math.abs(a - b) < 0.000001",
  "```",
  "",
  "Y para dinero, la solución de verdad: **guardar céntimos enteros**. Un precio de 12,45 se",
  "guarda como `1245`, se hacen todas las cuentas con enteros y se divide entre 100 solo al",
  "enseñarlo. Es lo que hacen los bancos y no es casualidad.",
  "",
  "## `NaN` no es igual a sí mismo",
  "",
  "```js",
  "NaN === NaN     // false",
  "```",
  "",
  "Es el único valor del lenguaje que no es igual a sí mismo, y viene del estándar de coma",
  "flotante: `NaN` significa «esto no es un número», y dos cosas que no son números no tienen",
  "por qué ser la misma.",
  "",
  "La consecuencia práctica es que **comprobarlo con `===` no funciona nunca**:",
  "",
  "```js",
  "Number.isNaN(x)     // así",
  "isNaN(x)            // así no: convierte antes, y isNaN('hola') es true",
  "```",
  "",
  "`Number.isNaN` pregunta si **es** `NaN`. El `isNaN` antiguo pregunta si «no se puede",
  "convertir a número», que es otra cosa y da falsos positivos.",
  "",
  "De dónde sale un `NaN`: de `0/0`, de `Number('hola')`, de `undefined + 1`, de una fecha",
  "inválida. Y es contagioso — en cuanto entra uno en una cuenta, todo lo que toca se vuelve",
  "`NaN` y se pasea por el programa hasta salir en pantalla.",
  "",
  "## `parseInt` y `Number` no hacen lo mismo",
  "",
  "```js",
  "parseInt('12px')    // 12    ← lee mientras entiende y se para",
  "Number('12px')      // NaN   ← exige que TODO sea número",
  "Number('')          // 0     ← el vacío es cero, sorpresa",
  "parseInt('')        // NaN",
  "```",
  "",
  "**Para validar lo que escribe alguien, `Number`**, que no se traga medias tintas. `parseInt`",
  "es para cuando de verdad quieres extraer el número de delante, como en `'20px'`.",
  "",
  "Y `parseInt` tiene otra trampa histórica: acepta una base como segundo argumento, y no",
  "ponerla ha dado disgustos. Hoy ya no interpreta el `0` inicial como octal, pero la costumbre",
  "de escribir `parseInt(x, 10)` sigue siendo buena.",
  "",
  "## Los enteros también tienen techo",
  "",
  "```js",
  "Number.MAX_SAFE_INTEGER          // 9007199254740991",
  "9007199254740992 + 1 === 9007199254740992    // true",
  "```",
  "",
  "A partir de ahí los enteros empiezan a saltarse valores, porque no caben en la precisión",
  "disponible. Suena lejano hasta que te llega un identificador de una base de datos, que",
  "perfectamente puede pasar de eso.",
  "",
  "Por eso los identificadores grandes viajan como **texto** en los JSON serios, y por eso",
  "existe `BigInt`:",
  "",
  "```js",
  "9007199254740993n + 1n     // 9007199254740994n",
  "```",
  "",
  "Los `BigInt` no se mezclan con los números normales: `1n + 1` lanza un `TypeError`.",
  "",
  "## Redondear tiene más de una forma",
  "",
  "```js",
  "Math.round(2.5)     // 3",
  "Math.round(-2.5)    // -2    ← hacia arriba también en negativo",
  "Math.round(-0.5)    // -0",
  "Math.trunc(-2.7)    // -2    ← quita los decimales",
  "Math.floor(-2.7)    // -3    ← hacia abajo de verdad",
  "```",
  "",
  "`Math.round` redondea siempre hacia arriba en los empates, y «arriba» en negativo significa",
  "hacia el cero. Si esperabas que se alejara del cero, esa es otra función.",
  "",
  "## `toFixed` devuelve texto",
  "",
  "```js",
  "(1.5).toFixed(2)          // '1.50'  ← texto",
  "(1.5).toFixed(2) + 1      // '1.501'",
  "(1.005).toFixed(2)        // '1.00'  ← y no '1.01'",
  "```",
  "",
  "Dos problemas de golpe. Es texto, así que sumarle algo lo pega. Y redondea sobre el valor",
  "binario real, que para 1,005 es un pelo menor que 1,005, así que baja.",
  "",
  "**Sirve para enseñar, nunca para calcular.** Y para enseñar, `Intl.NumberFormat` es mejor,",
  "porque además pone el separador de miles y el símbolo de la moneda del sitio:",
  "",
  "```js",
  "new Intl.NumberFormat('es-ES', { style: 'currency', currency: 'EUR' }).format(1234.5)",
  "// '1234,50 €'",
  "```",
  "",
  "## Lo que despista",
  "",
  "**`+` con un texto pega en vez de sumar.** `'3' + 1` es `'31'`. Los demás operadores sí",
  "convierten: `'3' - 1` es `2`. Es una inconsistencia y hay que conocerla.",
  "",
  "**`0.1 + 0.2` se ve bien si lo enseñas mal.** `console.log(0.1 + 0.2)` en algunos sitios",
  "sale como `0.3` porque el formateo lo redondea. La comparación sigue siendo falsa.",
  "",
  "**Hay dos ceros.** `-0` y `0` son `===` entre sí, pero `1/0` es `Infinity` y `1/-0` es",
  "`-Infinity`. Casi nunca importa, y cuando importa vuelve loco a cualquiera.",
  "",
  "## Pruébalo tú",
  "",
  "¿Qué sale?",
  "",
  "```js",
  "console.log(0.1 + 0.2 === 0.3)",
  "console.log(Number('') === 0)",
  "console.log([1, 2, 3].map(parseInt))",
  "```",
  "",
  "Sale `false`, `true` y **`[1, NaN, NaN]`**.",
  "",
  "La tercera es célebre. `map` le pasa a cada elemento **tres** argumentos —valor, índice y",
  "lista—, y `parseInt` coge los dos primeros como número y base. Así que se llama",
  "`parseInt(1, 0)`, `parseInt(2, 1)` y `parseInt(3, 2)`: base 0 se ignora y da 1, base 1 no",
  "existe y da `NaN`, y en base 2 no hay ningún dígito 3.",
  "",
  "La lección no es sobre `parseInt`: es que **pasar una función directamente a `map` es",
  "peligroso si acepta más de un argumento**. Con `map((n) => parseInt(n, 10))` no pasa nada.",
  "",
  "## Cómo se plantea",
  "",
  "1. **¿Esto es dinero?** Entonces enteros, en la unidad más pequeña. No hay atajo.",
  "2. **¿Viene de fuera?** `Number` para validar, y comprueba `Number.isNaN` antes de seguir.",
  "3. **¿Voy a comparar decimales?** Con margen, nunca con `===`.",
  "4. **¿Es para enseñar o para calcular?** `toFixed` e `Intl` solo para lo primero.",
  "5. **¿Puede ser enorme?** Los identificadores, como texto.",
)
