/**
 * El apunte de Wax para «vue2-02b-la-linea-culpable».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Hay un fallo de Vue 2 que comete todo el mundo una vez y que cuesta encontrar porque **el",
  "componente se pinta perfectamente**. Solo falla al pulsar, y el aviso habla de un dato que",
  "está clarísimamente ahí.",
  "",
  "Es el de las funciones flecha en las opciones del componente.",
  "",
  "## El síntoma",
  "",
  "```js",
  "methods: {",
  "  disparar: () => {",
  "    this.balas = this.balas - 1",
  "  },",
  "}",
  "```",
  "",
  "```",
  "[Vue warn]: Error in v-on handler: \"TypeError: Cannot read properties",
  "of undefined (reading 'balas')\"",
  "```",
  "",
  "Y uno mira `data`, ve `balas: 6` ahí puesto, y no entiende nada. La clave está en lo que",
  "el aviso **no** dice: no dice que `balas` no exista. Dice que lo de delante del punto es",
  "`undefined`. Y lo de delante del punto es `this`.",
  "",
  "## Qué es `this` en un método de Vue",
  "",
  "Vue reúne props, data, computadas y métodos, y los cuelga todos de un mismo objeto: la",
  "instancia del componente. Cuando llama a un método, se lo pasa como `this`. Por eso",
  "`this.balas` funciona.",
  "",
  "Pero para poder pasárselo, la función tiene que **admitir** un `this`. Y una función flecha",
  "no admite: no tiene `this` propio, usa el de donde se escribió. Ahí fuera no hay ningún",
  "componente, así que sale `undefined`.",
  "",
  "```js",
  "methods: {",
  "  disparar() {           // función normal: Vue le da el componente",
  "    this.balas -= 1",
  "  },",
  "}",
  "```",
  "",
  "La misma línea, escrita de la otra manera, funciona.",
  "",
  "## La regla, sin excepciones",
  "",
  "**Nunca una flecha en las opciones de un componente.** Ni en `methods`, ni en `computed`,",
  "ni en `watch`, ni en los hooks del ciclo de vida, ni en `data`.",
  "",
  "```js",
  "data: () => ({ balas: 6 }),   // funciona de casualidad, y no lo hagas",
  "created: () => { ... },       // this roto",
  "computed: { x: () => ... },   // this roto",
  "```",
  "",
  "`data` con flecha funciona porque no usa `this` casi nunca — hasta el día que lo necesita.",
  "",
  "**Dónde sí van las flechas:** dentro del cuerpo de un método. Ahí son perfectas, y de hecho",
  "son la forma de arreglar el problema contrario:",
  "",
  "```js",
  "methods: {",
  "  disparar() {",
  "    setTimeout(() => {",
  "      this.balas -= 1     // la flecha hereda el this del método. Justo lo que hace falta",
  "    }, 100)",
  "  },",
  "}",
  "```",
  "",
  "Con una función normal dentro del `setTimeout`, `this` se volvería a perder. La regla no es",
  "«las flechas son malas»: es **las flechas heredan `this`**, y eso a veces es lo que quieres",
  "y a veces es lo contrario.",
  "",
  "## Por qué el componente sí se pinta",
  "",
  "Esto es lo que despista de verdad. El fallo está en `methods`, y `methods` no se toca al",
  "pintar: solo se guarda. El `{{ balas }}` de la plantilla funciona, se ve el 6, todo",
  "parece bien.",
  "",
  "El error solo aparece cuando alguien pulsa, que puede ser mucho después de mirar la",
  "pantalla y darla por buena.",
  "",
  "Es un buen recordatorio de una cosa general: **que se pinte no quiere decir que funcione**.",
  "Un componente tiene dos vidas, la de pintarse y la de responder, y las pruebas que solo",
  "miran la primera dejan pasar la mitad de los fallos.",
  "",
  "## Por qué es un aviso y no un error",
  "",
  "Fíjate en que pone `[Vue warn]` y que el programa **sigue vivo** después. Vue envuelve los",
  "manejadores de eventos para que un fallo en uno no se lleve por delante la aplicación",
  "entera.",
  "",
  "Está bien pensado y tiene un precio: los fallos en manejadores son fáciles de no ver, sobre",
  "todo si nadie mira la consola. Cuando algo «no hace nada» al pulsar, lo primero es abrirla.",
  "",
  "## Lo que despista",
  "",
  "**`this` dentro de un `forEach` o un `map`.** Mismo problema, misma solución: flecha.",
  "",
  "**`this` en un `then`.** Igual. Los `.then(function () { ... })` antiguos pierden el `this`",
  "y por eso hay tanto código viejo con `const self = this` arriba. Con flechas eso sobra.",
  "",
  "**En Vue 3 con `setup` no existe este problema**, porque ahí no hay `this`: las variables",
  "son variables normales y se usan por su nombre. Es una de las cosas que la Composition API",
  "simplifica de verdad, no solo de forma.",
  "",
  "## Pruébalo tú",
  "",
  "¿Cuáles de estos cuatro funcionan?",
  "",
  "```js",
  "const componente = {",
  "  data() { return { n: 0 } },",
  "  methods: {",
  "    a() { this.n += 1 },",
  "    b: () => { this.n += 1 },",
  "    c() { setTimeout(() => { this.n += 1 }, 10) },",
  "    d() { setTimeout(function () { this.n += 1 }, 10) },",
  "  },",
  "}",
  "```",
  "",
  "Funcionan **`a`** y **`c`**.",
  "",
  "`b` falla porque la flecha está en la opción. `d` falla porque la función normal dentro del",
  "`setTimeout` recibe su propio `this`, que no es el componente. `c` es la combinación buena:",
  "método normal por fuera, flecha por dentro.",
  "",
  "Si te has fijado, `b` y `d` son el mismo error con las piezas cambiadas de sitio. Por eso",
  "no basta con memorizar «flechas no»: hay que entender que **la flecha hereda y la función",
  "normal recibe**, y decidir cuál quieres en cada sitio.",
  "",
  "## Cómo se plantea",
  "",
  "Cuando algo dé `Cannot read properties of undefined` y lo que falta sea claramente un dato",
  "del componente:",
  "",
  "1. **Mira `this`, no el dato.** El dato está bien; lo que está mal es desde dónde se mira.",
  "2. **Busca la función que lo contiene.** ¿Es flecha o normal?",
  "3. **¿Dónde está escrita?** Si es una opción del componente, tiene que ser normal. Si está",
  "   dentro de otra función del componente, tiene que ser flecha.",
  "4. **Si sigue sin cuadrar**, escribe `console.log(this)` en la primera línea. Te dirá en un",
  "   segundo si es el componente, `undefined` o alguna otra cosa.",
  "",
  "## La otra mitad: dónde SÍ van las flechas",
  "",
  "La regla de este reto es «nunca una flecha en las opciones». La mitad que falta es que dentro",
  "del cuerpo de un método son justo lo que hace falta:",
  "",
  "```js",
  "methods: {",
  "  disparar() {",
  "    setTimeout(() => {",
  "      this.balas -= 1        // la flecha hereda el this del método",
  "    }, 100)",
  "  },",
  "}",
  "```",
  "",
  "Con una función normal ahí dentro, `this` se volvería a perder, y ese era el problema que",
  "tenía todo el código de hace diez años — de ahí el `const self = this` que aparece en",
  "cualquier proyecto viejo.",
  "",
  "Lo mismo dentro de un `forEach`, de un `map` o de un `.then`:",
  "",
  "```js",
  "methods: {",
  "  contar() {",
  "    this.gatos.forEach((gato) => {",
  "      this.total += gato.croquetas     // funciona",
  "    })",
  "  },",
  "}",
  "```",
  "",
  "**La regla completa, entonces, no es «flechas no».** Es: **la flecha hereda `this` y la",
  "función normal lo recibe de quien la llama.** En las opciones del componente quieres que lo",
  "reciba, porque quien llama es Vue y te da el componente. Dentro de un método quieres que lo",
  "herede, porque el `this` bueno es el que ya tienes.",
  "",
  "Sabiendo eso, se decide sola en cualquier sitio, incluidos los que no aparecen en este reto.",
)
