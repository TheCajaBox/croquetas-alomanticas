/**
 * El apunte de Kelsier para «ceniza-05-pegar-textos».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Pegar dos textos es lo que más se hace en PHP, y se hace con un **punto**.",
  "",
  "```php",
  "echo 'Los Pozos' . ' de Hathsin';   // Los Pozos de Hathsin",
  "```",
  "",
  "## El `+` no pega",
  "",
  "En muchos lenguajes `'a' + 'b'` da `'ab'`. En PHP, no: el `+` es para sumar",
  "números, y si le das textos intentará convertirlos a números primero. Con textos",
  "que no son números, error.",
  "",
  "```php",
  "echo 'Kelsier' + 'Vin';   // revienta: no son números",
  "```",
  "",
  "Que sean dos símbolos distintos es una ventaja, aunque no lo parezca: cuando lees",
  "código sabes si esa línea suma o junta sin tener que adivinar los tipos.",
  "",
  "## `.=` para ir añadiendo",
  "",
  "`$texto .= 'más'` es la forma corta de `$texto = $texto . 'más'`. Se usa cuando el",
  "texto se construye por trozos:",
  "",
  "```php",
  "$parte = '';",
  "$parte .= 'Trece ';",
  "$parte .= 'cuadrillas';",
  "echo $parte;              // Trece cuadrillas",
  "```",
  "",
  "Para que eso funcione, `$parte` tiene que **existir antes**, aunque sea vacía. Si",
  "empiezas con `.=` sobre una variable que no existe, PHP avisa.",
  "",
  "## Cómo se plantea: juntar una lista sin coma de más",
  "",
  "El problema clásico. Recorres una lista pegando `'nombre, '` y al final te sobra",
  "una coma:",
  "",
  "```",
  "Vin, Kelsier, Brisa,        <- esa última coma es el fallo",
  "```",
  "",
  "La solución que siempre funciona: **la coma va delante, no detrás**. Delante de",
  "cada elemento menos del primero. Y sabes que estás en el primero porque lo que",
  "llevas construido todavía está vacío:",
  "",
  "```php",
  "$texto = '';",
  "foreach ($nombres as $nombre) {",
  "    if ($texto !== '') {",
  "        $texto .= ', ';",
  "    }",
  "    $texto .= $nombre;",
  "}",
  "```",
  "",
  "Lee las cinco líneas de dentro del bucle en orden y comprueba qué pasa en la",
  "primera vuelta: `$texto` está vacío, así que no pone coma, y añade el nombre. En",
  "la segunda ya no está vacío, así que pone coma y luego el nombre. Sale bien y no",
  "hay que quitar nada al final.",
  "",
  "## Los casos límite",
  "",
  "La lista vacía: el bucle no da ni una vuelta y devuelves `''`, que es exactamente",
  "lo correcto. Un solo elemento: una vuelta, sin coma. Los dos casos salen gratis",
  "con este planteamiento, y con el otro —quitar la coma final— hay que acordarse",
  "de comprobar que había algo que quitar.",
  "",
  "## Pruébalo tú",
  "",
  "Hazlo primero **mal**: pega `$nombre . ', '` en cada vuelta y mira la coma que",
  "sobra. Ver el fallo con tus ojos hace que la solución se entienda en vez de",
  "memorizarse.",
)
