/**
 * El apunte de Raoden para «linea-07-el-orden-completo».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Las siete cláusulas, la lista completa, y por última vez:",
  "",
  "```sql",
  "SELECT   -- qué columnas quiero",
  "FROM     -- de dónde, y con qué se une",
  "WHERE    -- qué filas me quedo",
  "GROUP BY -- en qué montones se parten",
  "HAVING   -- qué montones salen",
  "ORDER BY -- en qué orden",
  "LIMIT    -- cuántas como mucho",
  "```",
  "",
  "Ese orden al escribir, y no se negocia: una cláusula fuera de sitio no es un estilo",
  "distinto, es una consulta que la base no entiende.",
  "",
  "## Las uniones van pegadas al FROM",
  "",
  "Un detalle que a veces despista: `JOIN` y `ON` no son una cláusula aparte de la lista.",
  "Forman parte del `FROM`, porque son parte de decir **de dónde salen las filas**.",
  "",
  "Por eso la unión pasa antes que todo lo demás, y por eso una condición en el `ON` filtra",
  "antes que una en el `WHERE`. No es una excepción: es que están en el mismo paso.",
  "",
  "## El orden en que trabaja, una vez más",
  "",
  "```sql",
  "FROM (y las uniones) · WHERE · GROUP BY · HAVING · SELECT · ORDER BY · LIMIT",
  "```",
  "",
  "Y de esa lista sale todo lo que has aprendido a base de tropezar:",
  "",
  "- El `WHERE` no puede hablar de una suma: cuando trabaja, no hay sumas.",
  "- El `HAVING` sí, y no debería filtrar filas: para eso está el `WHERE`.",
  "- El `SELECT` trabaja **después** del `HAVING`, así que el `HAVING` no debería usar sus",
  "  alias -SQLite lo permite, otras bases no-.",
  "- El `ORDER BY` sí puede usarlos, en todas.",
  "- Una función de ventana se calcula en el `SELECT`, así que no se puede filtrar por ella",
  "  en el `WHERE` de su propia consulta: hace falta un paso más.",
  "- Y `LIMIT` corta lo último, así que sin `ORDER BY` corta a ciegas.",
  "",
  "Seis reglas y una sola lista. Merece la pena sabérsela de memoria, porque es la que",
  "contesta a las preguntas que se van a ir presentando.",
  "",
  "## Una cláusula por línea",
  "",
  "```sql",
  "SELECT g.nombre AS gremio, COUNT(v.id) AS ventas",
  "FROM ventas AS v",
  "JOIN puestos AS p ON v.puesto_id = p.id",
  "JOIN gremios AS g ON p.gremio_id = g.id",
  "WHERE v.dia <> 'miércoles'",
  "GROUP BY g.id",
  "HAVING COUNT(v.id) >= 2",
  "ORDER BY ventas DESC",
  "LIMIT 4;",
  "```",
  "",
  "Cuesta lo mismo que escribirlo en un párrafo y cambia tres cosas: se ve de un vistazo qué",
  "piezas tiene y qué le falta; cuando la base se queja de la línea seis, la línea seis es",
  "una cláusula entera; y añadir un `ORDER BY` es añadir una línea, no reescribir la de",
  "antes.",
  "",
  "## Cómo se plantea",
  "",
  "Y por última vez, la manera de montar cualquier consulta:",
  "",
  "1. `SELECT` y `FROM` con dos columnas cualesquiera. **Cuenta.**",
  "2. Las uniones. **Cuenta**: tienes que poder predecir el número.",
  "3. El `WHERE`. **Cuenta**: menos.",
  "4. El `GROUP BY`. **Cuenta**: tantas como montones.",
  "5. Los agregados, y **comprueba que suman el total**.",
  "6. El `HAVING`, el orden y el corte.",
  "7. Y al final el `SELECT` de verdad, con sus alias.",
  "",
  "Siete ejecuciones para una consulta de ocho líneas. Parece mucho hasta que se cuenta el",
  "tiempo que se pierde depurando una consulta escrita de golpe.",
  "",
  "## Pruébalo tú",
  "",
  "Cambia el `HAVING` de sitio, ponlo antes del `GROUP BY`, y lee el error. Después mueve el",
  "`WHERE` detrás del `GROUP BY` y lee ese otro. Los dos mensajes son de los que se",
  "reconocen al vuelo cuando ya los has visto una vez.",
)
