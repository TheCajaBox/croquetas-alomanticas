/**
 * El apunte de Wax para «elendel-04b-todos-los-que-casan».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Ya sabes escribir un patrón y sacar un dato de un texto. Lo que viene ahora es el paso que",
  "separa el ejemplo de clase del trabajo de verdad: los textos de verdad no traen un dato,",
  "traen **todos** los datos, y hay que sacarlos todos.",
  "",
  "Y con eso llega la sorpresa: durante veinte años, sacar todas las coincidencias **con sus",
  "grupos** no se podía hacer de una manera limpia en JavaScript. Ahora sí. Este apunte es esa",
  "manera.",
  "",
  "## El problema que resuelve",
  "",
  "Con un texto delante hay dos preguntas distintas, y `match` solo responde una:",
  "",
  "```js",
  "const texto = 'oro: 12, peltre: 30, acero: 8'",
  "",
  "texto.match(/(\\w+): (\\d+)/)     // ['oro: 12', 'oro', '12']",
  "texto.match(/(\\w+): (\\d+)/g)    // ['oro: 12', 'peltre: 30', 'acero: 8']",
  "```",
  "",
  "La primera trae **una** coincidencia y viene despiezada: el trozo entero y luego cada",
  "grupo. La segunda trae **todas** y ninguna despiezada: solo los trozos completos.",
  "",
  "Y lo que quieres, siempre, es lo tercero: todas y despiezadas. Con `match` no hay manera;",
  "hay que volver a partir cada trozo, y entonces has escrito el patrón dos veces.",
  "",
  "## La idea: `matchAll`",
  "",
  "```js",
  "const todas = [...texto.matchAll(/(\\w+): (\\d+)/g)]",
  "",
  "todas.length      // 3",
  "todas[0][1]       // 'oro'",
  "todas[1][0]       // 'peltre: 30'",
  "todas[1].index    // 9",
  "```",
  "",
  "Cada elemento de `todas` es una coincidencia completa, con la misma forma que la que",
  "devolvía `match` sin la `g`: el trozo entero en la posición 0, cada grupo detrás, y de",
  "propina `index`, que dice en qué carácter empezaba.",
  "",
  "Los tres puntitos son necesarios: `matchAll` no devuelve una lista, devuelve un **iterador**",
  "-algo que se puede recorrer una vez-. Con `[...]` se convierte en lista y ya se le puede",
  "hacer `.map`, `.filter` y contar cuántos hay.",
  "",
  "## Los grupos con nombre",
  "",
  "`todas[0][1]` funciona y es ilegible. Un grupo se puede bautizar poniéndole `?<nombre>`",
  "justo después del paréntesis que abre:",
  "",
  "```js",
  "const patron = /(?<metal>\\w+): (?<precio>\\d+)/g",
  "const todas = [...texto.matchAll(patron)]",
  "",
  "todas[0].groups            // { metal: 'oro', precio: '12' }",
  "todas.map((cada) => cada.groups.metal)   // ['oro', 'peltre', 'acero']",
  "```",
  "",
  "Los números siguen ahí; lo que gana es quien lo lea dentro de seis meses. Con `[2]` hay que",
  "contar paréntesis de izquierda a derecha; con `groups.precio` no hay nada que contar. Y si",
  "mañana alguien añade un grupo por delante, los números se mueven todos y los nombres no.",
  "",
  "También funcionan en el reemplazo, con la misma sintaxis:",
  "",
  "```js",
  "texto.replace(/(?<metal>\\w+): (\\d+)/g, (todo, metal, precio) => metal.toUpperCase() + '=' + precio)",
  "// 'ORO=12, PELTRE=30, ACERO=8'",
  "```",
  "",
  "## El modelo mental",
  "",
  "`match` pregunta «¿está esto ahí?». `matchAll` pregunta «¿cuántas veces, y qué había en cada",
  "una?». La primera es una comprobación; la segunda es una **extracción**.",
  "",
  "Y el resultado de una extracción es siempre lo mismo: una lista de objetos con nombres. Ese",
  "es el objetivo, y conviene tenerlo claro antes de escribir el patrón, porque los nombres de",
  "los grupos son los nombres de los campos del objeto. Escribe primero cómo quieres que se",
  "vea el dato, y el patrón sale casi solo.",
  "",
  "## Lo que despista",
  "",
  "**Sin la `g`, `matchAll` no funciona, y encima lo dice raro:**",
  "",
  "```",
  "TypeError: String.prototype.matchAll called with a non-global RegExp argument",
  "```",
  "",
  "Es de las pocas veces que JavaScript se pone estricto, y hay que agradecérselo: sin la `g`",
  "tendría que decidir si darte una coincidencia o entrar en un bucle infinito.",
  "",
  "**Un patrón con `g` guardado en una variable tiene memoria.** Esto sorprende a todo el",
  "mundo:",
  "",
  "```js",
  "const suelto = /(\\d+)/g",
  "suelto.test('a1')   // true",
  "suelto.test('a1')   // false  ← el mismo texto, otra respuesta",
  "```",
  "",
  "Un patrón global recuerda por dónde iba, y esa memoria se queda pegada a la variable. Con",
  "`matchAll` no molesta -recorre y termina-, pero con `test` y `exec` sí, y es un fallo que",
  "cuesta horas. La costumbre segura: **escribe el patrón donde se usa**, o si lo guardas, no",
  "lo compartas entre llamadas.",
  "",
  "**Los grupos siempre devuelven texto.** `groups.precio` es `'12'`, no `12`. Un patrón",
  "encuentra dígitos; convertirlos es tu trabajo, y si te lo saltas acabas sumando `'12'` con",
  "`'30'` y sacando `'1230'`.",
  "",
  "**Un grupo que no casó no es vacío: es `undefined`.** Con partes opcionales en el patrón,",
  "los grupos que se quedaron sin nada vienen así, y ahí es donde toca el `??`.",
  "",
  "## Pruébalo tú",
  "",
  "```js",
  "const fecha = '2015-03-14'",
  "const partes = fecha.match(/(?<anio>\\d{4})-(?<mes>\\d{2})-(?<dia>\\d{2})/)",
  "",
  "console.log(partes.groups)",
  "console.log(typeof partes.groups.anio)",
  "```",
  "",
  "Sale `{ anio: '2015', mes: '03', dia: '14' }` y luego `string`.",
  "",
  "Dos cosas que llevarse. La primera: los grupos con nombre funcionan igual con `match` a",
  "secas, no son cosa de `matchAll`; lo que hace falta ahí es que **no** haya `g`, porque con",
  "`g` los grupos desaparecen.",
  "",
  "La segunda: el mes es `'03'` con el cero delante y es texto. Si lo metes en `Date.UTC` tal",
  "cual, ni es número ni el 3 es marzo. Extraer y convertir son dos pasos, y los dos son",
  "tuyos.",
  "",
  "## Cómo se plantea",
  "",
  "Ante un texto del que hay que sacar varias cosas:",
  "",
  "1. **Escribe primero el objeto que quieres.** `{ metal, precio }`. Eso te da los nombres de",
  "   los grupos antes de haber escrito una barra.",
  "2. **Haz el patrón para una sola coincidencia** y pruébalo con `match` a secas, sin `g`.",
  "   Hasta que una no salga, todas no van a salir.",
  "3. **Añade la `g` y pásate a `matchAll`.** Y los tres puntitos, para tener una lista.",
  "4. **Convierte los tipos en el `.map`**, en el mismo sitio donde construyes el objeto. Ese",
  "   es el borde: de ahí para adentro, los números son números.",
  "5. **Y prueba con el texto vacío y con uno sin coincidencias.** Tienen que dar lista vacía,",
  "   no reventar. Es el caso que llega un martes de agosto.",
)
