/**
 * El apunte de Gaotona para «alma-04-la-costura».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Las cinco defensas están, todas bien puestas, y una está en el sitio equivocado. Es el",
  "reto que resume el mundo.",
  "",
  "## La línea",
  "",
  "```js",
  "const texto = normalizar(peticion.texto)",
  "if (!TEXTO_PERMITIDO.test(texto)) return { estado: 400 }",
  "base.ejecutar('UPDATE … SET nota = :nota …', {",
  "  nota: peticion.texto,          // ← el original, no el validado",
  "  id: documento.id,",
  "})",
  "```",
  "",
  "Se validó `texto` y se guarda `peticion.texto`. La consulta está parametrizada, así que **no",
  "hay inyección**; lo que hay es un dato que nadie ha aprobado metiéndose en la base. Y de",
  "ahí sale a una página, a un correo o a un informe algún día.",
  "",
  "## Por qué esto es tan difícil de ver",
  "",
  "Tres razones, y todas cuentan:",
  "",
  "**Las dos variables se llaman parecido.** `texto` y `peticion.texto`. En una revisión de",
  "cincuenta líneas eso no salta.",
  "",
  "**El código de al lado hace lo correcto.** La línea siguiente usa `documento.id` y no",
  "`peticion.id`, que es exactamente el mismo cuidado bien aplicado. Quien lo escribió sabía",
  "lo que hacía, y por eso nadie duda del resto.",
  "",
  "**No falla nunca.** Con datos normales, el original y el normalizado son iguales o casi. El",
  "agujero solo aparece cuando alguien manda algo que la normalización cambia, y eso no lo hace",
  "ningún usuario.",
  "",
  "## La forma que lo hace imposible",
  "",
  "Es la misma que en La inspección y conviene repetirla, porque es el arreglo de fondo de",
  "toda esta familia de fallos: **que la validación devuelva el valor**.",
  "",
  "```js",
  "// Con esto, el original no vuelve a estar a mano por accidente.",
  "const texto = textoAprobado(peticion.texto)   // el valor bueno, o null",
  "if (texto === null) return { estado: 400 }",
  "base.ejecutar('…', { nota: texto, id: documento.id })",
  "```",
  "",
  "Y hay una variante más fuerte todavía, que se usa en sistemas grandes: que el valor validado",
  "tenga un **tipo distinto**. Una función que solo acepta un `TextoAprobado` no se puede",
  "llamar con un texto crudo, y el compilador lo dice. Eso no se puede hacer en JavaScript sin",
  "ceremonia; la idea —hacer que el error no se pueda escribir— vale igual.",
  "",
  "## La costura, en una frase",
  "",
  "**Cualquier sitio donde un dato exista dos veces —crudo y tratado— es una costura.** Y las",
  "costuras se cierran de una sola manera: haciendo que solo exista una versión.",
  "",
  "Eso vale para el texto validado, para la ruta normalizada, para el usuario de la sesión",
  "frente al de la petición, y para el dueño del documento frente al declarado. Los cuatro",
  "retos silenciosos de este camino son el mismo reto.",
  "",
  "## Cómo se plantea",
  "",
  "Busca dos variables que se parezcan y que no sean la misma. Y compara la línea que valida",
  "con la que escribe, y luego con la de al lado, que hace lo correcto.",
)
