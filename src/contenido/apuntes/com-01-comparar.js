/**
 * El apunte de Wax para «com-01-comparar».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Antes de que un programa pueda decidir nada, tiene que saber comparar. Y comparar",
  "siempre da lo mismo: `true` o `false`. Nada más. A eso se le llama un **booleano**,",
  "y es el tipo más pequeño que existe.",
  "",
  "```js",
  "5 > 3        // true",
  "5 < 3        // false",
  "5 >= 5       // true    mayor O IGUAL",
  "5 <= 4       // false",
  "```",
  "",
  "Cada una de esas líneas produce un valor, igual que `2 + 2` produce un 4. Eso es",
  "lo primero que hay que interiorizar: una comparación **es un valor**, se puede",
  "guardar en una variable, pasar a una función y devolver.",
  "",
  "```js",
  "const esCaro = recompensa > 500      // esCaro vale true o false",
  "```",
  "",
  "## Los dos iguales y los tres iguales",
  "",
  "Para preguntar «¿es lo mismo?» hay dos operadores, y aquí es donde se pierde",
  "todo el mundo alguna vez.",
  "",
  "```js",
  "5 === 5      // true",
  "5 === '5'    // false   el número cinco no es el texto cinco",
  "5 == '5'     // true    ...pero con dos iguales, sí",
  "```",
  "",
  "`===` compara **tal cual**: si los tipos no coinciden, ya es que no.",
  "",
  "`==` **convierte antes de comparar**. Ve un número y un texto, convierte el texto",
  "a número, y entonces compara. Suena cómodo y es una fuente inagotable de",
  "disgustos, porque las reglas de conversión son largas, nadie se las sabe enteras,",
  "y producen cosas como estas:",
  "",
  "```js",
  "0 == ''          // true",
  "0 == '0'         // true",
  "'' == '0'        // false   <- y aquí se rompe la lógica",
  "null == undefined // true",
  "```",
  "",
  "Fíjate en las tres primeras juntas: cero es igual a texto vacío, y cero es igual",
  "a «0», pero texto vacío no es igual a «0». Si `a` es igual a `b` y `a` es igual a",
  "`c`, uno esperaría que `b` fuera igual a `c`. Con `==` no se cumple ni eso.",
  "",
  "**La norma es corta: usa siempre `===`, y `!==` para lo contrario.** Los de dos",
  "iguales déjalos donde están. No es una preferencia de estilo: es que uno de los",
  "dos tiene reglas que puedes deducir y el otro no.",
  "",
  "(El único caso donde `==` se usa a propósito es `x == null`, que da true tanto",
  "para `null` como para `undefined` y a veces es justo lo que quieres. Cuando lo",
  "veas, es eso.)",
  "",
  "## Verdadero-ish: cuando algo no es booleano y hace de booleano",
  "",
  "Cuando JavaScript necesita un `true` o un `false` y le das otra cosa, se lo",
  "inventa. Estos seis valores cuentan como falsos, y hay que sabérselos:",
  "",
  "```js",
  "false    0    ''    null    undefined    NaN",
  "```",
  "",
  "Y **todo lo demás cuenta como verdadero**. Todo. Incluidos estos tres, que son",
  "los que sorprenden:",
  "",
  "```js",
  "Boolean('0')     // true   es un texto con un carácter dentro",
  "Boolean([])      // true   la lista vacía es verdadera",
  "Boolean({})      // true   el objeto vacío también",
  "```",
  "",
  "Que la lista vacía sea verdadera es de las cosas que hay que saberse y ya está.",
  "Si quieres preguntar si una lista tiene algo, se pregunta por su longitud:",
  "",
  "```js",
  "if (buscados.length > 0) { ... }     // así",
  "if (buscados) { ... }                // esto es true siempre, incluso vacía",
  "```",
  "",
  "## Y, o, no",
  "",
  "Tres operadores para combinar condiciones. Los dos primeros se leen tal cual:",
  "",
  "```js",
  "a && b    // Y:  verdadero solo si los dos lo son",
  "a || b    // O:  verdadero si al menos uno lo es",
  "!a        // NO: le da la vuelta",
  "```",
  "",
  "```js",
  "const puedePasar = tieneCita || edad >= 18",
  "const esUrgente = recompensa > 500 && !capturado",
  "```",
  "",
  "Y una cosa que casi nunca se cuenta al principio y evita muchos errores: **paran",
  "en cuanto saben la respuesta**. En `a && b`, si `a` es falso, `b` ni se mira: ya",
  "da igual. En `a || b`, si `a` es verdadero, tampoco.",
  "",
  "Eso permite escribir esto, que verás por todas partes:",
  "",
  "```js",
  "if (ficha && ficha.nombre) { ... }",
  "```",
  "",
  "Si `ficha` no existe, la segunda parte no se evalúa y no revienta. Sin esa",
  "garantía, `ficha.nombre` sobre algo inexistente daría error.",
  "",
  "## Devolver la comparación, sin adornos",
  "",
  "Al empezar, todo el mundo escribe esto:",
  "",
  "```js",
  "if (edad >= 18) {",
  "  return true",
  "} else {",
  "  return false",
  "}",
  "```",
  "",
  "Funciona. Pero mira lo que hace: pregunta si algo es verdad, y si lo es, devuelve",
  "verdad. Sobra la mitad.",
  "",
  "```js",
  "return edad >= 18",
  "```",
  "",
  "`edad >= 18` **ya es** `true` o `false`. Envolverlo en un `if` para devolver lo",
  "mismo que ya tenías es dar una vuelta de más. Es la primera simplificación que",
  "aprende todo el mundo, y en una revisión de código es lo primero que te van a",
  "señalar.",
  "",
  "## Cómo nombrar un booleano",
  "",
  "Un booleano se nombra de forma que la pregunta se lea sola. Con una forma verbal",
  "delante: `es`, `tiene`, `puede`, `hay`, `esta`.",
  "",
  "```js",
  "const esPeligroso = ...",
  "const tieneCoartada = ...",
  "const puedeEntrar = ...",
  "```",
  "",
  "Así, cuando aparezca en un `if`, la línea se leerá como una frase en castellano:",
  "`if (puedeEntrar)`. Compáralo con `if (entrada)`, que no dice si es un booleano,",
  "una puerta o una anotación.",
  "",
  "Y **evita nombrarlos en negativo**. `noEstaCerrado` parece inofensivo hasta que",
  "alguien escribe `!noEstaCerrado`, que son dos negaciones y ya nadie sabe qué",
  "significa. Nómbralo en positivo y niega en el sitio donde haga falta.",
)
