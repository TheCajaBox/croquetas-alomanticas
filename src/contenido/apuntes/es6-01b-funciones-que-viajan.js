/**
 * El apunte de Wax para «es6-01b-funciones-que-viajan».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Aquí ya sabes lo que es una función. Ahora la idea que le da la vuelta a todo:",
  "**una función es un valor**. Se puede guardar en una variable, meter en una",
  "lista, pasarla a otra función y devolverla.",
  "",
  "```js",
  "function saludar(nombre) {",
  "  return `Buenas, ${nombre}.`",
  "}",
  "",
  "const otroNombre = saludar        // sin paréntesis: la función, no su resultado",
  "otroNombre('Wayne')               // 'Buenas, Wayne.'",
  "```",
  "",
  "Los paréntesis son los que **llaman**. Sin ellos tienes la función misma, como",
  "tener el papel con las instrucciones en vez de haberlas seguido.",
  "",
  "Esa distinción es la puerta de entrada a media biblioteca de JavaScript, y",
  "también el origen de un fallo muy común:",
  "",
  "```js",
  "setTimeout(avisar, 1000)     // bien: le paso la función, la llamará dentro de 1s",
  "setTimeout(avisar(), 1000)   // mal: la llamo YA y le paso lo que devuelve",
  "```",
  "",
  "## Pasarle una función a otra función",
  "",
  "Si una función es un valor, se puede pasar como argumento:",
  "",
  "```js",
  "function hacerDosVeces(tarea) {",
  "  tarea()",
  "  tarea()",
  "}",
  "",
  "hacerDosVeces(() => console.log('bang'))",
  "```",
  "",
  "A la función que recibe otra se le llama **función de orden superior**, y a la que",
  "se le pasa, *callback* o «la que se llama después». Los nombres importan poco; lo",
  "que importa es reconocer la forma, porque está en todas partes:",
  "",
  "```js",
  "metales.map(metal => metal.nombre)",
  "metales.filter(metal => metal.precio > 30)",
  "boton.addEventListener('click', avisar)",
  "```",
  "",
  "En los tres casos estás diciendo **qué hacer con cada cosa**, y dejando que otro",
  "se ocupe del cuándo y del cómo recorrer.",
  "",
  "## Por qué esto es un salto, y no un truco",
  "",
  "Hasta ahora, para cambiar el comportamiento de un trozo de código tenías que",
  "tocar ese trozo. Con funciones como valores puedes escribir algo una vez y que",
  "cada quien le pase el comportamiento que necesite.",
  "",
  "Compara. Sin ello, una función por cada criterio:",
  "",
  "```js",
  "function caros(metales) {",
  "  const salida = []",
  "  for (const m of metales) if (m.precio > 30) salida.push(m)",
  "  return salida",
  "}",
  "function baratos(metales) { /* lo mismo con < 30 */ }",
  "function deAcero(metales) { /* lo mismo con otra condición */ }",
  "```",
  "",
  "Con ello, una sola función y el criterio de fuera:",
  "",
  "```js",
  "function filtrar(metales, cumple) {",
  "  const salida = []",
  "  for (const m of metales) if (cumple(m)) salida.push(m)",
  "  return salida",
  "}",
  "",
  "filtrar(metales, m => m.precio > 30)",
  "filtrar(metales, m => m.nombre === 'acero')",
  "```",
  "",
  "Esa segunda versión es, exactamente, `filter`. Cuando lo uses dentro de un par de",
  "retos ya sabrás lo que hace por dentro, que es la diferencia entre usar una",
  "herramienta y entenderla.",
  "",
  "## Devolver una función",
  "",
  "Y como son valores, también se pueden devolver:",
  "",
  "```js",
  "function multiplicadorPor(n) {",
  "  return (x) => x * n",
  "}",
  "",
  "const doble = multiplicadorPor(2)",
  "doble(5)      // 10",
  "```",
  "",
  "`multiplicadorPor(2)` no devuelve un número: devuelve **una función nueva** que",
  "recuerda que su `n` era 2. Eso se llama un cierre, tiene un mundo entero dedicado",
  "en El taller, y de momento basta con que no te sorprenda verlo.",
  "",
  "## Los tres tropiezos de siempre",
  "",
  "**Llamar cuando querías pasar**, que ya has visto: `tarea()` en vez de `tarea`.",
  "",
  "**Pasar cuando querías llamar**, que es el contrario y produce cosas raras:",
  "",
  "```js",
  "console.log(saludar)     // imprime el código de la función, no el saludo",
  "```",
  "",
  "**Y perder el `this`**, que es el gordo y tiene su propio reto justo después.",
  "Cuando arrancas un método de su objeto y lo pasas suelto, deja de saber a quién",
  "pertenecía:",
  "",
  "```js",
  "const timbre = { veces: 0, sonar() { this.veces += 1 } }",
  "setTimeout(timbre.sonar, 100)     // dentro, this ya no es timbre",
  "```",
  "",
  "Guárdate el síntoma: si un método funciona al llamarlo normal y falla al pasarlo",
  "a otro sitio, es esto.",
  "",
  "## Cómo se lee una función que recibe funciones",
  "",
  "Cuando veas algo como esto y se te haga bola:",
  "",
  "```js",
  "const nombres = metales.filter(m => m.precio > 30).map(m => m.nombre)",
  "```",
  "",
  "Léelo en dos pasos y en castellano: «de metales, quédate con los que cumplen que",
  "el precio pasa de 30; y de esos, coge el nombre de cada uno».",
  "",
  "La flecha se lee como «da»: `m => m.nombre` es «dado un metal, da su nombre».",
  "Con esa traducción mental, la mitad del JavaScript moderno se vuelve legible de",
  "golpe.",
)
