/**
 * El apunte de Kelsier para «pozo-09-cuando-falta-el-dato».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Los cuatro huecos de este reto llevan lo mismo, y eso es media lección: `??` sirve igual",
  "para una clave que no está, para un array que es `null` y para una variable que nunca se",
  "declaró. Todos esos casos son «aquí no hay nada» y todos se resuelven de la misma manera.",
  "",
  "## Los tres casos, y son el mismo",
  "",
  "```php",
  "$inventario = ['acero' => 4];",
  "$nadie = null;",
  "",
  "$inventario['acero'] ?? 0;    // 4    -> está",
  "$inventario['oro'] ?? 0;      // 0    -> la clave no está",
  "$nadie['nombre'] ?? '?';      // '?'  -> el array entero es null",
  "$sinDeclarar ?? '?';          // '?'  -> la variable no existe",
  "```",
  "",
  "Las cuatro líneas se leen igual y ninguna avisa. Es lo que hace `??` cómodo: no hay que",
  "distinguir de antemano cuál de las tres cosas ha fallado, porque la respuesta es la misma.",
  "",
  "## Por qué `??` y no lo otro",
  "",
  "Sin `??` esto se escribe así, y merece verlo para saber de qué te libras:",
  "",
  "```php",
  "if (isset($inventario['oro'])) {",
  "    $cuantos = $inventario['oro'];",
  "} else {",
  "    $cuantos = 0;",
  "}",
  "```",
  "",
  "Seis líneas donde caben tres erratas -escribir la clave dos veces es una de ellas- para",
  "decir lo que cabe en una. Y la de una línea no puede tener la clave mal escrita en un solo",
  "sitio, porque solo está escrita una vez.",
  "",
  "## Los paréntesis, que no son opcionales",
  "",
  "```php",
  "echo 'hay ' . $inventario['oro'] ?? 0;      // mal",
  "echo 'hay ' . ($inventario['oro'] ?? 0);    // bien",
  "```",
  "",
  "`??` se evalúa **después** del punto de concatenar, así que en la primera línea PHP entiende",
  "«(`'hay ' . $inventario['oro']`) o si no, 0», y como el texto pegado ya cuenta como algo,",
  "el `?? 0` nunca entra. El resultado es `hay ` con un hueco, y sigue avisando de la clave.",
  "",
  "La costumbre: **`??` siempre entre paréntesis** cuando hay algo más en la línea. Cuesta dos",
  "teclas y quita una clase entera de sorpresas.",
  "",
  "## Cuándo `??` es la respuesta equivocada",
  "",
  "`??` está para lo que **puede** faltar y tiene una respuesta razonable: un stock que no",
  "está es cero, un nombre que no está es «desconocido».",
  "",
  "No está para lo que **no debería** faltar. Si el precio de un producto no aparece, poner",
  "cero es regalarlo: ahí lo que hay que hacer es quejarse en voz alta, y de eso va el reto",
  "once. La pregunta es siempre: «si esto falta, ¿puedo seguir teniendo razón?».",
  "",
  "## Los casos límite",
  "",
  "- **`??` se encadena**: `$a ?? $b ?? 'nada'` coge el primero que exista, en orden.",
  "- **`??=` asigna solo si falta**: `$opciones['color'] ??= 'gris'` deja el que ya hubiera.",
  "- **No silencia otros errores.** Si `$inventario` no es un array sino un número, `??` no te",
  "  salva: eso es un error de tipo y sale igual. Solo cubre «no hay nada ahí».",
  "- **En una función que declara `: int`**, devolver `$x ?? 0` te asegura el tipo; devolver",
  "  `$x` a secas revienta el día que `$x` sea `null`, y eso es bueno: te enteras.",
  "",
  "## Pruébalo tú",
  "",
  "```php",
  "<?php",
  "$inventario = ['acero' => 0];",
  "",
  "echo 'hay ' . $inventario['oro'] ?? 0;        // mira lo que sale, y el aviso",
  "echo PHP_EOL;",
  "echo 'hay ' . ($inventario['oro'] ?? 0);",
  "echo PHP_EOL;",
  "echo 'acero: ' . ($inventario['acero'] ?? 99);   // 0, no 99",
  "```",
)
