/**
 * El apunte de Wax para «vue3-01b-poner-nombre».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Compara el componente de este reto con el de la mansión Ladrian. Hacen exactamente lo",
  "mismo: reciben un dueño, guardan unas balas, calculan un resumen y tienen un botón para",
  "recargar.",
  "",
  "Lo único que cambia es **dónde vive cada cosa**. Y merece la pena verlo así, en paralelo,",
  "porque la Composition API no es Vue nuevo: es Vue reordenado.",
  "",
  "## Los mismos conceptos, otro sitio",
  "",
  "| En la mansión (Vue 2) | En la ciudad nueva (Vue 3) |",
  "|---|---|",
  "| `data() { return { balas: 6 } }` | `const balas = ref(6)` |",
  "| `computed: { resumen() {...} }` | `const resumen = computed(() => ...)` |",
  "| `methods: { recargar() {...} }` | `function recargar() { ... }` |",
  "| `mounted() { ... }` | `onMounted(() => { ... })` |",
  "| Vue decide los cajones | Tú decides el orden |",
  "| Todo cuelga de `this` | Variables normales, por su nombre |",
  "",
  "Esa última fila es la que más cambia el día a día. En `setup` no hay `this`: hay variables",
  "de JavaScript de toda la vida, y se usan escribiendo su nombre.",
  "",
  "## `setup` es una función normal",
  "",
  "Eso es lo que hay que interiorizar. Dentro de `setup` puedes hacer lo que harías en",
  "cualquier función: declarar variables, escribir funciones, poner un `if`, sacar cosas a",
  "otro fichero. No hay reglas especiales.",
  "",
  "```js",
  "setup(props) {",
  "  const balas = ref(6)",
  "",
  "  const resumen = computed(() => `${props.dueno}: ${balas.value}`)",
  "",
  "  function recargar() {",
  "    balas.value = 6",
  "  }",
  "",
  "  onMounted(() => console.log('ya está en pantalla'))",
  "",
  "  return { balas, resumen, recargar }",
  "}",
  "```",
  "",
  "Se ejecuta **una vez** por componente, antes del primer pintado. Recibe las props como",
  "primer argumento, y por eso no hace falta `this.dueno`: es `props.dueno`.",
  "",
  "## El `return` no es decoración",
  "",
  "La última línea decide qué ve la plantilla. Lo que no devuelvas, no existe para ella:",
  "",
  "```js",
  "setup() {",
  "  const balas = ref(6)",
  "  return {}          // la plantilla no verá balas",
  "}",
  "```",
  "",
  "Y el síntoma es cruel: no da error. Sale un hueco vacío donde debería estar el número, sin",
  "ningún aviso. Es el olvido más común al empezar con la Composition API.",
  "",
  "(Con `<script setup>`, que es lo que se usa hoy en día, esto desaparece: todo lo que",
  "declares arriba queda visible sin devolver nada. Pero conviene haber visto la forma larga",
  "una vez para entender qué está haciendo el atajo por ti.)",
  "",
  "## `.value` dentro, nada fuera",
  "",
  "Una `ref` es un objeto con una propiedad `value`. Ahí vive el dato:",
  "",
  "```js",
  "const balas = ref(6)",
  "balas          // el objeto envoltorio",
  "balas.value    // 6",
  "```",
  "",
  "Y entonces la asimetría que despista a todo el mundo:",
  "",
  "```js",
  "// Dentro de setup: con .value",
  "balas.value = 5",
  "",
  "// En la plantilla: sin .value",
  "{{ balas }}",
  "```",
  "",
  "La plantilla desenvuelve sola las refs que le devuelvas, para no llenarla de `.value`. En",
  "el JavaScript no hay magia: si te dejas el `.value`, estás usando el objeto envoltorio.",
  "",
  "```js",
  "console.log(balas + 1)         // '[object Object]1'",
  "console.log(balas.value + 1)   // 7",
  "```",
  "",
  "Ese `[object Object]` es la señal. Cuando lo veas, te has dejado un `.value`.",
  "",
  "## Por qué esta forma existe",
  "",
  "La queja contra los cajones no era estética. En un componente grande, una funcionalidad",
  "—digamos la búsqueda— tiene su dato en `data`, su resultado en `computed`, su función en",
  "`methods` y su limpieza en `beforeDestroy`. Cuatro sitios distintos, y en medio el código",
  "de las otras tres funcionalidades.",
  "",
  "Con `setup`, todo lo de la búsqueda va junto en diez líneas seguidas. Y como son diez",
  "líneas seguidas de JavaScript normal, se pueden **cortar y pegar en otro fichero**:",
  "",
  "```js",
  "function usarBusqueda(lista) {",
  "  const texto = ref('')",
  "  const resultados = computed(() => lista.filter((x) => x.includes(texto.value)))",
  "  return { texto, resultados }",
  "}",
  "```",
  "",
  "Eso es un composable, y no es un concepto nuevo que haya que aprender: es esto mismo movido",
  "de sitio. Lo que en Vue 2 requería mixins —con sus choques de nombres y su «¿de dónde sale",
  "esta variable?»— aquí es una función que devuelve un objeto.",
  "",
  "## Lo que despista",
  "",
  "**`props` no se desestructura.** `const { dueno } = props` corta la reactividad: te quedas",
  "con el valor de ese momento. Si lo necesitas suelto, `toRefs(props)`.",
  "",
  "**Los hooks se registran, no se llaman.** `onMounted(() => ...)` le dice a Vue «cuando",
  "montes, ejecuta esto». Y tiene que llamarse durante `setup`, no dentro de un `setTimeout` ni",
  "después de un `await`: Vue necesita saber a qué componente pertenece, y solo lo sabe",
  "mientras `setup` está corriendo.",
  "",
  "**No hay `this`, y es bueno.** Se acabaron los problemas con las flechas: aquí todas las",
  "funciones pueden ser flechas sin que se rompa nada.",
  "",
  "**Las dos APIs conviven.** Vue 3 admite las dos, y Vue 2.7 admite la Composition API. No es",
  "una migración obligatoria.",
  "",
  "## Pruébalo tú",
  "",
  "¿Qué falla en este `setup`?",
  "",
  "```js",
  "setup() {",
  "  const balas = ref(6)",
  "  const dobles = computed(() => balas * 2)",
  "",
  "  function recargar() {",
  "    balas = 6",
  "  }",
  "",
  "  return { balas, dobles }",
  "}",
  "```",
  "",
  "Tres cosas:",
  "",
  "1. `balas * 2` sin `.value` — multiplica el objeto envoltorio y da `NaN`.",
  "2. `balas = 6` reasigna la constante — `TypeError`. Lo que hay que cambiar es `balas.value`.",
  "3. `recargar` no se devuelve, así que la plantilla no puede llamarla. Sin error ninguno: el",
  "   botón simplemente no hace nada.",
  "",
  "Las tres son el mismo malentendido de fondo: **la ref es la caja, no el contenido**. La caja",
  "se declara una vez y no se reasigna nunca; lo que cambia es lo que hay dentro.",
  "",
  "## Cómo se plantea",
  "",
  "Para escribir un `setup` desde cero:",
  "",
  "1. **¿Qué datos cambian?** Una `ref` para cada uno.",
  "2. **¿Qué se deduce de ellos?** `computed`, y agrúpalo justo debajo de sus datos.",
  "3. **¿Qué acciones hay?** Funciones normales, al lado de lo que tocan.",
  "4. **¿Hay que arrancar o parar algo?** `onMounted` / `onUnmounted`, juntos y al lado de lo",
  "   suyo.",
  "5. **Devuelve lo que use la plantilla.** Ni más ni menos.",
  "",
  "Y al terminar, una pregunta: **¿alguno de estos bloques podría irse a su propio fichero?**",
  "Si la respuesta es sí, ahí tienes tu primer composable.",
)
