/**
 * El apunte de Raoden para «dor-06-cada-subconsulta-lo-suyo».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Una subconsulta puede ir en cuatro sitios y en cada uno hace un papel. Reconocerlos es",
  "la mitad de entender una consulta ajena.",
  "",
  "## Escalar: un valor donde iría un número",
  "",
  "```sql",
  "WHERE monedas > (SELECT AVG(monedas) FROM ventas)",
  "```",
  "",
  "Una fila y una columna. Si devuelve más, error -y eso es una suerte-. Se calcula una",
  "vez.",
  "",
  "## Lista: para IN",
  "",
  "```sql",
  "WHERE id IN (SELECT puesto_id FROM ventas)",
  "```",
  "",
  "Una columna, muchas filas. Y **con un nulo dentro, la versión negada no devuelve",
  "nada**: es la trampa del reto ocho y por eso `NOT IN` con subconsulta no se usa.",
  "",
  "## Existencia: EXISTS",
  "",
  "```sql",
  "WHERE EXISTS (SELECT 1 FROM ventas v WHERE v.puesto_id = p.id)",
  "```",
  "",
  "Solo pregunta si hay **al menos una fila**, sin mirar qué hay dentro -de ahí el",
  "`SELECT 1`, que es la costumbre: da igual qué se pida-. Se para en la primera que",
  "encuentra, y no se rompe con los nulos.",
  "",
  "Es la forma correcta de escribir «los que tienen» y «los que no tienen»:",
  "",
  "```sql",
  "WHERE NOT EXISTS (SELECT 1 FROM ventas v WHERE v.puesto_id = p.id)",
  "```",
  "",
  "## Tabla derivada: en el FROM",
  "",
  "```sql",
  "FROM (SELECT puesto_id, SUM(monedas) AS t FROM ventas GROUP BY puesto_id) AS x",
  "```",
  "",
  "Aporta filas en vez de filtrar. Es un `WITH` escrito de dentro hacia fuera, y con dos",
  "de estos anidados la consulta ya no se lee.",
  "",
  "## Correlacionada: la que mira hacia arriba",
  "",
  "```sql",
  "SELECT p.nombre,",
  "       (SELECT COUNT(*) FROM ventas v WHERE v.puesto_id = p.id) AS ventas",
  "FROM puestos AS p;",
  "```",
  "",
  "Se reconoce a la vista: **dentro del paréntesis hay un nombre que viene de fuera**",
  "(`p.id`). Y eso cambia cuándo se ejecuta: una vez **por cada fila** de la consulta de",
  "arriba, en vez de una sola.",
  "",
  "Con nueve puestos da igual. Con nueve millones no, y entonces conviene saber que casi",
  "siempre hay una versión con `LEFT JOIN` y `GROUP BY` que hace lo mismo mirando la tabla",
  "una vez.",
  "",
  "A cambio tiene una ventaja de verdad: con `LEFT JOIN` y `GROUP BY`, un puesto sin",
  "ventas obliga a acordarse de `COUNT(v.id)` y de `COALESCE`; con la correlacionada, el",
  "`COUNT(*)` de dentro da cero solo, porque su montón está vacío de verdad. Cada una",
  "tiene su clase de descuido.",
  "",
  "## Cómo se plantea: elegir entre EXISTS y JOIN",
  "",
  "Para «los que tienen al menos uno» hay tres maneras y conviene tener una preferida:",
  "",
  "- `EXISTS` — la que dice lo que quiere decir. Es la que yo escribiría.",
  "- `IN` con subconsulta — vale, y con la negación es un campo de minas.",
  "- `JOIN` + `DISTINCT` — funciona y obliga a acordarse del `DISTINCT`, porque un puesto",
  "  con tres ventas sale tres veces.",
  "",
  "Y para «los que no tienen ninguno», solo una: `NOT EXISTS`.",
  "",
  "## Pruébalo tú",
  "",
  "Escribe la misma pregunta -«los puestos que han vendido algo»- de las tres maneras y",
  "comprueba que las tres dan ocho filas. Después mira cuál de las tres se entiende sin",
  "pensar. Esa es la que hay que escribir.",
)
