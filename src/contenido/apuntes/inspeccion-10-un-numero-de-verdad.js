/**
 * El apunte de Gaotona para «inspeccion-10-un-numero-de-verdad».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Lo que llega en una petición es texto. Siempre. Convertirlo es fácil; lo difícil es que",
  "lo que salga sea un número que **tenga sentido**.",
  "",
  "## Las dos funciones de convertir, y por qué ninguna vale sola",
  "",
  "```js",
  "parseInt('12abc')   // 12      ← se queda con el principio y tira el resto",
  "parseInt('1e3')     // 1       ← y aquí el resto era importante",
  "parseInt('0x10')    // 16      ← el hexadecimal sí lo entiende",
  "parseInt('')        // NaN",
  "",
  "Number(' 12 ')      // 12      ← se traga los espacios",
  "Number('1e3')       // 1000    ← y la notación científica",
  "Number('0x10')      // 16",
  "Number('')          // 0       ← el vacío es cero, que es lo peor de todo",
  "Number('١٢')        // 12      ← dígitos árabe-orientales, y hay más alfabetos",
  "Number('Infinity')  // Infinity",
  "```",
  "",
  "`parseInt` es peor porque **no avisa**: `'12abc'` le parece bien. `Number` avisa con `NaN`",
  "en más casos, pero acepta media docena de formas de escribir un número que casi nunca",
  "quieres admitir.",
  "",
  "Y `Number('') === 0` es el que más caro sale: un campo vacío se convierte en un cero",
  "válido, y de ahí salen descuentos del cien por cien y cantidades a cero.",
  "",
  "## El orden correcto: forma, luego valor",
  "",
  "```js",
  "const SOLO_DIGITOS = /^[1-9][0-9]{0,2}$/",
  "",
  "if (!SOLO_DIGITOS.test(texto)) return null   // 1. ¿tiene la forma?",
  "const numero = Number(texto)                 // 2. ahora convertir es seguro",
  "if (numero < 1 || numero > 500) return null  // 3. ¿está en el rango?",
  "```",
  "",
  "Primero se comprueba **la forma del texto** con una lista de permitidos, y solo después se",
  "convierte. Así no hay que saberse las rarezas de `Number`: nada que no sean dígitos llega",
  "a la conversión.",
  "",
  "Fíjate en el `[1-9]` del principio: rechaza `007`. No es tiquismiquis, es",
  "**canonicalizar**: un número tiene una sola forma de escribirse, y admitir dos abre la",
  "puerta a que dos textos distintos signifiquen lo mismo. Eso importa en cuanto ese texto se",
  "use como clave de una caché, se compare con otro, o se meta en una URL.",
  "",
  "Y el rango se comprueba **igual**, aunque el patrón limite a tres dígitos: `999` tiene",
  "tres dígitos y no es una página válida. La forma y el rango son dos comprobaciones y",
  "ninguna sustituye a la otra.",
  "",
  "## Lo que pasa cuando esto falta",
  "",
  "- **Un número negativo donde va una cantidad.** Transferir `-100` es cobrar 100.",
  "- **Un número enorme en un tamaño.** `pagina = 99999999` con veinte filas por página son",
  "  dos mil millones de filas que la base intenta saltarse.",
  "- **Un decimal donde va un entero.** `pagina = 1.5` con aritmética de por medio da",
  "  desplazamientos que nadie ha probado.",
  "- **`NaN` colándose en una comparación.** `NaN > 0` es falso y `NaN < 0` también, así que",
  "  un `if (numero < 0) rechazar` deja pasar `NaN` tranquilamente.",
  "",
  "Ese último merece una línea aparte: con `NaN`, **todas** las comparaciones son falsas.",
  "Validar por lo que se rechaza —«si es menor que cero, fuera»— falla justo con el valor",
  "más raro. Validar por lo que se admite —«tiene que estar entre 1 y 500»— no.",
  "",
  "## Cómo se plantea",
  "",
  "Dos comprobaciones en este orden: la forma del texto y el rango del número. La forma, con",
  "una expresión regular anclada por los dos lados; el rango, con dos comparaciones.",
  "",
  "## Pruébalo tú",
  "",
  "Cambia el `[1-9][0-9]{0,2}` por `[0-9]+` y mira qué tests se caen. Son tres, y los tres",
  "dicen cosas distintas: el cero, los ceros delante y el número gigante.",
)
