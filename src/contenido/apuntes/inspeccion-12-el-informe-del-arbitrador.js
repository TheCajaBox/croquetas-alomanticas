/**
 * El apunte de Gaotona para «inspeccion-12-el-informe-del-arbitrador».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "El jefe junta los tres tipos de comprobación del mundo —una lista cerrada, un número con",
  "rango, un texto libre— y los dos destinos donde se pinta. Y añade una cosa que en los",
  "retos sueltos no se veía: **el orden en que se responden los errores es parte del",
  "contrato**.",
  "",
  "## El código de partida, y sus cinco agujeros",
  "",
  "```js",
  "if (!ESTADOS.includes(peticion.estado)) …          // 1 y 2",
  "const pagina = parseInt(peticion.pagina)           // 3",
  "if (!(pagina > 0)) …                               // 4",
  "if (peticion.comentario.length > 200) …            // 5",
  "'<article data-estado=' + ficha.estado + …         // 6",
  "```",
  "",
  "1. **No normaliza el estado**, así que `' Aprobado '` se rechaza aunque sea lo que el",
  "   usuario quería decir. Este no es un agujero de seguridad: es un formulario que no",
  "   funciona, y de esos también hay que responder.",
  "2. **No comprueba el tipo.** Si `estado` llega como lista, `includes` devuelve `false` y",
  "   se rechaza por casualidad. Funciona, y funciona por el motivo equivocado, que es peor",
  "   que fallar.",
  "3. **`parseInt`**, con todo lo del reto diez detrás.",
  "4. **`!(pagina > 0)`** en vez de un rango: no hay límite por arriba, y está escrito con",
  "   una negación de una comparación para atrapar el `NaN`. Funciona y no se entiende, que",
  "   en una comprobación de seguridad es un defecto.",
  "5. **`.length` sobre algo que puede no ser texto.** Con `comentario: 7` esto revienta; con",
  "   `comentario: ['a','b']` cuenta dos elementos y pasa.",
  "6. **El atributo sin comillas**, el reto ocho.",
  "",
  "## El contrato de los motivos",
  "",
  "```js",
  "revisar({ estado: 'no', pagina: 'no', comentario: 7 }).motivo   // 'estado'",
  "```",
  "",
  "Cuando fallan tres cosas se responde la primera, y «la primera» tiene que estar definida.",
  "Si no lo está, el mensaje que ve el usuario cambia según cómo esté escrito el código, y un",
  "test que lo compruebe se rompe en cuanto alguien reordena dos `if`.",
  "",
  "Fíjate en que responder **solo el primero** es una decisión discutible: para un formulario",
  "es mejor devolver todos los errores a la vez, que el usuario no tenga que enviar cinco",
  "veces. Aquí se pide uno porque el reto va de la comprobación y no de la interfaz, pero en",
  "un sistema real la firma buena sería `{ ok, errores: [] }`.",
  "",
  "## Lo que la ficha hace y no parece",
  "",
  "```js",
  "return { ok: true, ficha: { estado, pagina, comentario } }",
  "```",
  "",
  "La ficha **no es la petición**. Es un objeto nuevo, con los tres campos que interesan y",
  "con los tipos ya buenos: `estado` normalizado, `pagina` como número, `comentario` como",
  "texto. De ahí en adelante nadie tiene que volver a preguntarse si están validados.",
  "",
  "Eso es lo que hace que la validación sirva de algo: que **exista un valor** que solo",
  "puede haber salido de la validación. Devolver `true` y seguir usando `peticion` deja la",
  "puerta abierta a la confusión del reto cuatro, y además obliga a validar otra vez en cada",
  "sitio, o a confiar en que alguien lo hizo antes.",
  "",
  "Y hay una razón más para construir un objeto nuevo en vez de copiar la petición: la",
  "petición puede traer campos que nadie pidió. `{ estado, pagina, comentario, esAdmin: true }`",
  "pasa las tres comprobaciones sin problema, y si el código de después hace",
  "`Object.assign(usuario, peticion)` acabas de regalar el permiso. Se llama asignación",
  "masiva, y la defensa es exactamente esta: enumerar los campos que se copian.",
  "",
  "## Cómo se plantea",
  "",
  "Reto por reto, en orden. El estado: tipo, normalizar, lista. La página: forma con una",
  "expresión anclada, convertir, rango. El comentario: tipo y longitud. Y la tarjeta:",
  "comillas en el atributo y escapar en los dos sitios. Nada nuevo; lo nuevo es que todo",
  "tiene que estar a la vez.",
)
