/**
 * El apunte de Raoden para «trazos-06-seis-frases-sobre-escribir».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Lo que hay que saberse antes de tocar una base de verdad.",
  "",
  "## Sin WHERE se aplica a todo, y no da error",
  "",
  "`UPDATE` y `DELETE` sin condición tocan la tabla entera. Es una orden válida y la base",
  "no puede saber que te has olvidado media línea.",
  "",
  "## La transacción es la única red",
  "",
  "```sql",
  "BEGIN;",
  "DELETE FROM ventas WHERE puesto_id = 3;",
  "-- mira las filas cambiadas",
  "ROLLBACK;   -- o COMMIT, según lo que hayas visto",
  "```",
  "",
  "Entre `BEGIN` y `COMMIT`, o pasan todas las órdenes o no pasa ninguna. Fuera de una",
  "transacción, cada orden se confirma sola en cuanto termina.",
  "",
  "Y ojo a la lección incómoda: **la transacción no evita el fallo, da tiempo a verlo**.",
  "Sirve si alguien mira el número de filas antes de confirmar. Con el `COMMIT` escrito de",
  "antemano, no sirve de nada.",
  "",
  "## Una columna puede usarse en su propio cálculo",
  "",
  "```sql",
  "UPDATE ventas SET monedas = monedas + 10 WHERE puesto_id = 1;",
  "```",
  "",
  "No hay que leerla primero, y es importante que no lo hagas: entre un `SELECT` y un",
  "`UPDATE` separados, otro programa puede haber cambiado el dato y entonces sumas sobre",
  "un número viejo. Con una sola orden, la base garantiza que eso no pasa.",
  "",
  "## Una regla que salta tira la orden entera",
  "",
  "Un `INSERT` que choca con un `UNIQUE` no mete la fila avisando: **falla**, y la tabla se",
  "queda como estaba. Eso es lo que se quiere de una restricción, y es la razón de ponerlas",
  "en el esquema.",
  "",
  "## Un índice no acelera «todo»",
  "",
  "Acelera las **lecturas** que puedan usarlo y encarece **todas** las escrituras, porque",
  "hay que mantenerlo al día en cada `INSERT`, `UPDATE` y `DELETE`. Y ocupa espacio.",
  "",
  "Se ponen mirando el plan de las consultas que tardan, no por si acaso. Una tabla con",
  "ocho índices que nadie usa es una tabla en la que escribir cuesta ocho veces más de lo",
  "necesario.",
  "",
  "## Las claves ajenas impiden dejar los datos a medias",
  "",
  "No se puede borrar un gremio que tenga puestos. La orden falla, y eso es lo bueno: sin",
  "la clave ajena se podría, y la tabla se quedaría con filas apuntando al vacío -datos",
  "que no se pueden arreglar sin adivinar-.",
  "",
  "Se puede pedir otro comportamiento -`ON DELETE CASCADE` borra los hijos, `ON DELETE SET",
  "NULL` los deja huérfanos a propósito- y las dos cosas hay que decidirlas, no heredarlas.",
  "El `CASCADE` sobre todo: borra filas que no has nombrado.",
  "",
  "## Cómo se plantea: el orden de un cambio con riesgo",
  "",
  "Cuando haya que tocar datos que importan:",
  "",
  "1. `BEGIN`.",
  "2. La orden.",
  "3. **Mirar** el número de filas cambiadas y, si hace falta, un `SELECT` para ver el",
  "   estado.",
  "4. `COMMIT` si cuadra, `ROLLBACK` si no.",
  "",
  "Cuatro pasos, y el tercero es el único que no se puede automatizar. Ahí está el oficio.",
  "",
  "## Pruébalo tú",
  "",
  "Haz un `BEGIN`, borra todas las ventas, mira el número, y haz `ROLLBACK`. Después",
  "cuenta las filas: siguen las seis. Eso es la red, y merece la pena haberla usado una vez",
  "en un sitio donde no pasa nada.",
)
