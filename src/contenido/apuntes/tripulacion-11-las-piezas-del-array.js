/**
 * El apunte de Kelsier para «tripulacion-11-las-piezas-del-array».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Antes de recorrer un array casi siempre hay que preguntarle algo. PHP trae una función por",
  "pregunta, y usar la de al lado es un clásico.",
  "",
  "## Cuántos hay",
  "",
  "```php",
  "count($inventario);        // 2",
  "count($inventario) === 0;  // «está vacío», dicho de forma que se entiende",
  "```",
  "",
  "## ¿Está esta clave?",
  "",
  "```php",
  "isset($inventario['oro']);                 // la clave está Y no vale null",
  "array_key_exists('oro', $inventario);      // la clave está, valga lo que valga",
  "```",
  "",
  "Se diferencian en una sola cosa, y es justo la que importa cuando `null` significa algo:",
  "",
  "```php",
  "$notas = ['vin' => null];",
  "isset($notas['vin']);                // false",
  "array_key_exists('vin', $notas);     // true",
  "```",
  "",
  "Fíjate además en el orden de los argumentos: `isset` recibe la casilla entera y",
  "`array_key_exists` recibe la clave primero y el array después. Es una incoherencia del",
  "lenguaje y hay que sabérsela.",
  "",
  "## ¿Está este valor?",
  "",
  "```php",
  "in_array('acero', ['acero', 'peltre']);        // true  <- lista de valores",
  "in_array('acero', ['acero' => 4]);             // FALSE <- 'acero' es la clave",
  "```",
  "",
  "**`in_array` busca valores, nunca claves.** Es el error más repetido de los seis: con un",
  "inventario, lo que buscas es una clave, así que la función es `array_key_exists`.",
  "",
  "Y un detalle: `in_array` compara de forma laxa por defecto, con todo lo que eso trae. Con un",
  "tercer argumento a `true` compara estricto, que casi siempre es lo que quieres:",
  "`in_array('0', $lista, true)`.",
  "",
  "## Las dos mitades de un array",
  "",
  "```php",
  "$inventario = ['acero' => 4, 'peltre' => 2];",
  "",
  "array_keys($inventario);      // ['acero', 'peltre']",
  "array_values($inventario);    // [4, 2]",
  "```",
  "",
  "Las dos devuelven una **lista nueva por posición**. `array_values` es la forma de tirar las",
  "claves cuando ya no hacen falta; `array_keys` sirve para recorrer los nombres, o para",
  "contarlos, o para comprobar en un test que están los que tienen que estar y en su orden.",
  "",
  "## Cómo se elige",
  "",
  "Traduce la frase antes de escribir la función:",
  "",
  "```",
  "«¿hay algo?»                 -> count(...) > 0",
  "«¿tengo este metal?»         -> array_key_exists / isset",
  "«¿está este nombre?»         -> in_array   (si es una lista de nombres)",
  "«dame los nombres»           -> array_keys",
  "```",
  "",
  "Cuando la frase en castellano dice «clave» y estás escribiendo `in_array`, algo va mal.",
  "",
  "## Pruébalo tú",
  "",
  "Coge un array asociativo y pásalo por las seis. Ver `in_array` devolver `false` con una",
  "clave que está clarísimamente ahí es la manera de que no vuelva a pasarte.",
)
