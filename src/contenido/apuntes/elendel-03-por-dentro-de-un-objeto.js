/**
 * El apunte de Wax para «elendel-03-por-dentro-de-un-objeto».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Un objeto no se recorre con `map` ni con `filter`. Esas son de las listas, y un objeto no es",
  "una lista.",
  "",
  "Pero se puede **convertir en** lista, recorrerla con todo lo que ya sabes, y volver a",
  "convertirla en objeto. Esa ida y vuelta es de las cosas que más se usan a diario y que casi",
  "nunca se explican.",
  "",
  "## Las cuatro de la familia",
  "",
  "```js",
  "const precios = { acero: 12, peltre: 30, oro: 8 }",
  "",
  "Object.keys(precios)      // ['acero', 'peltre', 'oro']",
  "Object.values(precios)    // [12, 30, 8]",
  "Object.entries(precios)   // [['acero', 12], ['peltre', 30], ['oro', 8]]",
  "```",
  "",
  "Y la que cierra el círculo:",
  "",
  "```js",
  "Object.fromEntries([['acero', 12]])    // { acero: 12 }",
  "```",
  "",
  "`entries` va del objeto a la lista de pares; `fromEntries` vuelve. Con esas dos y lo que ya",
  "sabes de listas, se puede hacer cualquier cosa con un objeto.",
  "",
  "## El patrón, que es siempre el mismo",
  "",
  "**Transformar todos los valores:**",
  "",
  "```js",
  "const subidos = Object.fromEntries(",
  "  Object.entries(precios).map(([metal, precio]) => [metal, precio * 1.5]),",
  ")",
  "// { acero: 18, peltre: 45, oro: 12 }",
  "```",
  "",
  "**Quedarse con algunas claves:**",
  "",
  "```js",
  "const caros = Object.fromEntries(",
  "  Object.entries(precios).filter(([, precio]) => precio > 10),",
  ")",
  "// { acero: 12, peltre: 30 }",
  "```",
  "",
  "Fíjate en ese `[, precio]`: la coma sola se salta el primer elemento del par. Es la forma de",
  "decir «la clave no me hace falta» sin inventarle un nombre.",
  "",
  "**Sumar todos los valores:**",
  "",
  "```js",
  "Object.values(precios).reduce((suma, precio) => suma + precio, 0)   // 50",
  "```",
  "",
  "Para esto no hace falta `entries`: si solo te importan los valores, `values` es más directo",
  "y se lee mejor.",
  "",
  "## Desestructurar el par es la mitad de la comodidad",
  "",
  "La flecha recibe un par, `['acero', 12]`, y desestructurarlo en la propia cabecera es lo que",
  "hace que se lea:",
  "",
  "```js",
  ".map((par) => [par[0], par[1] * 1.5])              // funciona, y no se entiende",
  ".map(([metal, precio]) => [metal, precio * 1.5])   // lo mismo, legible",
  "```",
  "",
  "Y hay que **devolver otro par**. El fallo típico es devolver solo el valor:",
  "",
  "```js",
  ".map(([metal, precio]) => precio * 1.5)   // ahora es [18, 45, 12], ya no hay claves",
  "```",
  "",
  "Después `fromEntries` recibe una lista de números en vez de pares y devuelve un objeto raro",
  "o vacío, sin ningún error.",
  "",
  "## Recorrer sin convertir",
  "",
  "Si solo quieres mirar, `for...in` recorre las claves directamente:",
  "",
  "```js",
  "for (const metal in precios) {",
  "  console.log(metal, precios[metal])",
  "}",
  "```",
  "",
  "Funciona, y tiene una trampa que `Object.keys` no tiene: `for...in` recorre también lo",
  "heredado del prototipo. Con objetos normales da igual; con objetos que vienen de librerías",
  "antiguas, no. Por eso la costumbre es `for (const [k, v] of Object.entries(x))`, que además",
  "te da clave y valor de una vez.",
  "",
  "## Lo que despista",
  "",
  "**Las claves son siempre texto.** Aunque escribas números:",
  "",
  "```js",
  "const porAnio = { 2015: 'Elendel', 2016: 'Bilming' }",
  "Object.keys(porAnio)     // ['2015', '2016'], textos",
  "```",
  "",
  "Y hay una sorpresa más: las claves que parecen números enteros **se ordenan solas y van",
  "primero**, por delante de las demás. Si el orden te importa, un objeto no es el sitio; usa",
  "un `Map` o una lista.",
  "",
  "**El orden de las demás claves sí se respeta**, y es el de inserción. Es fiable, y aun así",
  "apoyarse en él para lógica importante es frágil.",
  "",
  "**`Object.entries` hace una copia.** Modificar lo que sale no toca el objeto original, lo",
  "cual está bien, y significa que para objetos enormes hay un coste.",
  "",
  "**Un objeto vacío no es falsy.** `if (Object.keys(x).length === 0)` es la forma de",
  "comprobarlo; `if (!x)` no sirve para eso.",
  "",
  "## Pruébalo tú",
  "",
  "¿Qué sale?",
  "",
  "```js",
  "const casos = { Wax: 4, Wayne: 1, Marasi: 7 }",
  "",
  "const activos = Object.fromEntries(",
  "  Object.entries(casos).filter(([, cuantos]) => cuantos > 2),",
  ")",
  "",
  "console.log(activos)",
  "console.log(Object.keys(casos).length)",
  "console.log(Object.values(casos).reduce((a, b) => a + b, 0))",
  "```",
  "",
  "Sale `{ Wax: 4, Marasi: 7 }`, luego `3`, luego `12`.",
  "",
  "Fíjate en la segunda: `casos` sigue teniendo tres. Filtrar construyó un objeto nuevo y no",
  "tocó el de partida, igual que `filter` en una lista.",
  "",
  "## Cómo se plantea",
  "",
  "Ante un objeto que hay que recorrer:",
  "",
  "1. **¿Qué necesito, las claves, los valores o los dos?** Elige `keys`, `values` o `entries`.",
  "   Pedir `entries` para usar solo los valores es ruido.",
  "2. **¿El resultado es un objeto o es otra cosa?** Si es un número o una lista, no hace falta",
  "   `fromEntries`: acabas en el `reduce` o en el `map` y ya está.",
  "3. **Si vuelve a ser objeto, devuelve pares.** El error de devolver solo el valor no da",
  "   error, y esos son los peores.",
  "4. **¿El orden importa?** Entonces no es un objeto. Un `Map` o una lista de pares.",
)
