/**
 * El apunte de Wax para «es6-04b-metodos-de-lista».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Todos los bucles que has escrito en La comisaría hacían una de estas tres cosas:",
  "transformar cada elemento, quedarse con algunos, o reducirlo todo a un valor.",
  "Como son las tres de siempre, las listas traen un método para cada una.",
  "",
  "No es que el bucle esté mal. Es que estas tres **dicen qué estás haciendo** en el",
  "nombre, y un bucle obliga a leerlo entero para averiguarlo.",
  "",
  "## map: la misma cantidad, transformados",
  "",
  "```js",
  "const nombres = metales.map((metal) => metal.nombre)",
  "```",
  "",
  "De una lista de N sale otra de N. Uno a uno, ninguno se pierde y ninguno aparece.",
  "Cuando veas un `map`, ya sabes que la lista de salida tiene el mismo tamaño.",
  "",
  "## filter: los que cumplen, sin tocarlos",
  "",
  "```js",
  "const caros = metales.filter((metal) => metal.precio >= 30)",
  "```",
  "",
  "De una lista de N sale otra de 0 a N, con los elementos **tal cual**. `filter` no",
  "transforma nada: solo decide quién pasa.",
  "",
  "La función que le pasas tiene que devolver algo verdadero o falso. Si devuelve",
  "otra cosa, se aplica la regla de los seis valores falsos, con las sorpresas de",
  "siempre.",
  "",
  "## reduce: todo a un valor",
  "",
  "```js",
  "const total = metales.reduce((suma, metal) => suma + metal.precio, 0)",
  "```",
  "",
  "Es el que más cuesta, y es el bucle acumulador que ya sabes escribir, empaquetado.",
  "Tiene dos partes:",
  "",
  "- La función recibe **el acumulado hasta ahora** y **el elemento actual**, y",
  "  devuelve el acumulado nuevo.",
  "- El segundo argumento, el `0` del final, es **el valor inicial**. El mismo que",
  "  ya sabes que es una decisión y no un cero por costumbre.",
  "",
  "Lado a lado con lo que ya conoces:",
  "",
  "```js",
  "let suma = 0",
  "for (const metal of metales) {",
  "  suma = suma + metal.precio",
  "}",
  "```",
  "",
  "Es exactamente lo mismo. `reduce` no hace nada que no supieras hacer; lo que hace",
  "es quitarte la variable de fuera y el bucle de en medio.",
  "",
  "**Pon siempre el valor inicial.** Sin él, `reduce` coge el primer elemento como",
  "acumulado de partida, lo que cambia el tipo del resultado y revienta con la lista",
  "vacía (`TypeError: Reduce of empty array with no initial value`).",
  "",
  "Y no fuerces `reduce` donde no toca. Si lo que sale es una lista, casi siempre",
  "querías `map` o `filter`. `reduce` es para cuando el resultado es **una sola",
  "cosa**: un número, un texto, un objeto de totales.",
  "",
  "## Se encadenan, y ahí está la gracia",
  "",
  "```js",
  "const nombresCaros = metales",
  "  .filter((metal) => metal.precio >= 30)",
  "  .map((metal) => metal.nombre)",
  "```",
  "",
  "Se lee de arriba abajo como una frase: de los metales, quédate con los caros, y de",
  "esos coge el nombre. Cada paso hace una cosa y se puede leer solo.",
  "",
  "Un detalle de eficiencia que conviene conocer: **cada método recorre la lista",
  "entera**. Ese ejemplo la recorre dos veces. Con veinte metales es irrelevante; con",
  "un millón, no. Cuando importe de verdad, se vuelve a un bucle, y no antes: la",
  "legibilidad se paga con optimizaciones concretas, no con desconfianza general.",
  "",
  "Y el orden importa: filtrar primero y transformar después es más barato que al",
  "revés, porque transformas menos elementos.",
  "",
  "## Ninguno toca tu lista",
  "",
  "`map`, `filter` y `reduce` **devuelven algo nuevo** y dejan la original intacta.",
  "Eso es la mitad de por qué son mejores que un bucle con `push`: no hay forma de",
  "estropear sin querer un dato que alguien más estaba usando.",
  "",
  "Lo que sí ocurre: los objetos de dentro son los mismos.",
  "",
  "```js",
  "const copia = metales.map((m) => m)",
  "copia[0].precio = 999      // metales[0].precio también es 999",
  "```",
  "",
  "La lista es nueva, los objetos no. Para cambiarlos sin tocar los originales hay",
  "que crear objetos nuevos también:",
  "",
  "```js",
  "const subidos = metales.map((m) => ({ ...m, precio: m.precio * 2 }))",
  "```",
  "",
  "## Los otros cuatro que hacen falta",
  "",
  "```js",
  "metales.find((m) => m.nombre === 'acero')    // el primero que cumpla, o undefined",
  "metales.some((m) => m.precio > 100)          // ¿alguno?  true/false",
  "metales.every((m) => m.precio > 0)           // ¿todos?   true/false",
  "metales.forEach((m) => console.log(m))       // recorre y no devuelve nada",
  "```",
  "",
  "`find`, `some` y `every` **paran en cuanto saben la respuesta**, así que son más",
  "eficientes que filtrar y mirar el tamaño.",
  "",
  "Sobre `forEach`: existe, y casi nunca es lo que quieres. Si vas a transformar,",
  "`map`; si vas a filtrar, `filter`; si solo vas a recorrer para provocar un efecto,",
  "un `for...of` se lee igual de bien y admite `break`. `forEach` no se puede cortar.",
  "",
  "## Cómo elegir, en tres preguntas",
  "",
  "Cuando estés delante de una lista y no sepas qué usar:",
  "",
  "1. **¿La salida tiene el mismo tamaño?** → `map`.",
  "2. **¿Es un subconjunto sin tocar?** → `filter`.",
  "3. **¿Es una sola cosa?** → `reduce`, o `find`/`some`/`every` si esa cosa es un",
  "   elemento o un sí/no.",
  "",
  "Y si la respuesta es «depende», o hay que salirse a mitad, o hay que ir tocando",
  "dos cosas a la vez: un bucle, sin complejos. Escribir un `reduce` retorcido para",
  "no usar un `for` es de las cosas que más cuestan de leer en código ajeno.",
  "",
  "## Lo que despista",
  "",
  "**`map` no descarta y `filter` no transforma.** Cada uno hace una cosa. Si te ves poniendo",
  "`if` dentro de un `map` para no devolver nada en algunos casos, querías `filter` antes.",
  "",
  "**Todos devuelven una lista nueva y no tocan la tuya.** Salvo `sort` y `reverse`, que son las",
  "dos excepciones y por eso confunden tanto.",
  "",
  "**`forEach` no devuelve nada.** Es para hacer, no para producir. `const x = lista.forEach(...)`",
  "deja `x` en `undefined`.",
  "",
  "**Encadenar recorre la lista una vez por eslabón.** Con listas normales da igual; conviene",
  "saberlo antes de encadenar seis sobre cien mil elementos.",
  "",
  "**`reduce` puede hacer lo de todos los demás**, y por eso mismo casi nunca es la respuesta más",
  "legible. Se usa cuando el resultado tiene otra forma que la lista de partida.",
  "",
  "## Pruébalo tú",
  "",
  "Empareja cada frase con su método antes de mirar:",
  "",
  "1. «Quiero los nombres de todos los metales.»",
  "2. «Quiero solo los que valen más de 10.»",
  "3. «Quiero saber si hay alguno gratis.»",
  "4. «Quiero el primero que valga más de 10.»",
  "5. «Quiero cuánto valen todos juntos.»",
  "6. «Quiero escribir cada uno por consola.»",
  "",
  "Son `map`, `filter`, `some`, `find`, `reduce` y `forEach`.",
  "",
  "Y la prueba de que lo has entendido no es acertar los seis: es notar que **las frases ya lo",
  "dicen**. «Todos los…» es `map`. «Solo los que…» es `filter`. «Si hay alguno» es `some`. «El",
  "primero que» es `find`. «Todos juntos» es `reduce`.",
  "",
  "Cuando sepas decir en una frase qué quieres, el método sale solo. Cuando no sepas cuál usar,",
  "casi siempre es que la frase todavía no está clara.",
  "",
  "## Cómo se plantea",
  "",
  "1. **Di en una frase qué quieres que salga.** El método está en la frase.",
  "2. **¿Salen tantos como entran?** `map`. ¿Menos? `filter`. ¿Uno? `find`. ¿Un sí o un no?",
  "   `some`/`every`. ¿Otra cosa? `reduce`.",
  "3. **Filtra antes de transformar.** Así transformas menos.",
  "4. **Recoge el resultado.** Ninguno cambia tu lista, así que si no lo guardas no ha pasado",
  "   nada.",
  "5. **Y si la cadena no se lee mejor que un bucle, escribe el bucle.** El objetivo es que se",
  "   entienda, no usar métodos.",
)
