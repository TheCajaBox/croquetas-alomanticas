/**
 * El apunte de Wax para «ferro-02b-lo-que-cae-al-de-dentro».
 *
 * Vive fuera del reto y se carga cuando se abre, no al arrancar el juego.
 * Los apuntes son lo más largo que hay aquí -y tienen que serlo, porque son
 * la lección- así que si viajaran en el paquete inicial cada lección nueva
 * haría más lento el arranque para todo el mundo.
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Hay una cosa que Vue hace por ti todo el rato y que casi nadie sabe que existe hasta el día",
  "que le estorba: **reparte los atributos que sobran**.",
  "",
  "Cuando le pasas algo a un componente y ese algo no es una prop que él haya declarado, Vue",
  "no lo tira. Lo coge y lo pega en el elemento raíz de lo que el componente pinte. Con",
  "componentes normales eso es exactamente lo que quieres y por eso no te enteras. Con",
  "componentes que **envuelven** a otro es lo contrario de lo que quieres, y ahí empieza esta",
  "lección.",
  "",
  "## El problema que resuelve",
  "",
  "Escribes un campo de formulario reutilizable: una etiqueta y un `<input>` dentro.",
  "",
  "```js",
  "const Campo = {",
  "  props: ['etiqueta'],",
  "  template: `<label class=\"campo\"><input class=\"dato\" /></label>`,",
  "}",
  "```",
  "",
  "Lo usas y le pasas un `placeholder`, como harías con un `<input>` de toda la vida:",
  "",
  "```html",
  "<Campo etiqueta=\"Croquetas\" placeholder=\"cuántas\" class=\"gordo\" />",
  "```",
  "",
  "Y lo que sale pintado es esto:",
  "",
  "```html",
  "<label class=\"campo gordo\" placeholder=\"cuántas\"><input class=\"dato\"></label>",
  "```",
  "",
  "El `placeholder` está en el `<label>`, donde no significa nada, y el `<input>` -que es",
  "quien lo necesitaba- se ha quedado sin él. No hay error, no hay aviso: hay un campo de",
  "texto que no enseña la pista que le pusiste.",
  "",
  "Fíjate además en la clase: `class=\"campo gordo\"`. Las clases y los estilos **no**",
  "sustituyen, se **suman**. Eso es a propósito y es una de las cosas más cómodas de Vue.",
  "",
  "## La idea: dos decisiones, no una",
  "",
  "Se arregla en dos pasos, y conviene verlos como dos decisiones separadas porque lo son.",
  "",
  "**Uno: dile que no reparta.** `inheritAttrs: false` apaga el reparto automático. A partir de",
  "ahí, lo que sobra no se pega en ningún sitio.",
  "",
  "**Dos: dile dónde va.** Todo lo que sobra vive junto en un objeto que la plantilla ve por su",
  "nombre, `$attrs`, y `v-bind` sabe aplicar un objeto entero de atributos de golpe:",
  "",
  "```js",
  "const Campo = {",
  "  props: ['etiqueta'],",
  "  inheritAttrs: false,",
  "  template: `",
  "    <label class=\"campo\">",
  "      <span class=\"titulo\">{{ etiqueta }}</span>",
  "      <input class=\"dato\" v-bind=\"$attrs\" />",
  "    </label>",
  "  `,",
  "}",
  "```",
  "",
  "Y ya está. Ahora el `placeholder`, el `type`, el `maxlength`, el `autocomplete` y los",
  "treinta atributos que tiene un `<input>` funcionan sin que hayas declarado ni uno.",
  "",
  "## Los escuchadores también van ahí",
  "",
  "Esto es lo que casi nadie espera y lo que hace que la solución sea tan corta: **en Vue 3,",
  "los eventos que no declaras viajan en `$attrs` como una propiedad más.** Un `@input` de",
  "fuera llega como `onInput`, un `@focus` como `onFocus`. Míralo:",
  "",
  "```js",
  "const Mira = {",
  "  inheritAttrs: false,",
  "  setup() {",
  "    return { atributos: Vue.useAttrs() }",
  "  },",
  "  template: `<p>{{ Object.keys(atributos).join(',') }}</p>`,",
  "}",
  "",
  "// Usado con placeholder=\"p\" @click=\"algo\" data-y=\"2\" pinta:",
  "// placeholder,onClick,data-y",
  "```",
  "",
  "Así que ese `v-bind=\"$attrs\"` no reparte solo atributos: reparte también los escuchadores.",
  "Un envoltorio hecho así se comporta como el elemento que envuelve **sin escribir una sola",
  "línea por evento**.",
  "",
  "Y de paso ahí sale `useAttrs()`, que es la forma de leerlos desde `setup` cuando hace falta",
  "decidir con ellos y no solo repartirlos.",
  "",
  "## El modelo mental",
  "",
  "Un componente tiene dos entradas y no se parecen en nada.",
  "",
  "**Las props son el contrato.** Están declaradas, tienen nombre, son las que el componente",
  "entiende y sobre las que decide.",
  "",
  "**Los atributos que sobran son el paquete que va de paso.** El componente no los entiende",
  "ni tiene por qué; su trabajo es dejarlos en el sitio bueno. Un envoltorio es una oficina de",
  "reparto, no el destinatario.",
  "",
  "Con esa idea, la pregunta al escribir un componente que envuelve es siempre la misma: **¿de",
  "todo lo que me pueden pasar, cuál es el elemento que de verdad lo necesita?** Ese es el que",
  "lleva el `v-bind`.",
  "",
  "## Lo que despista",
  "",
  "**Con dos raíces, el reparto no funciona y sí avisa.** Si el componente pinta dos elementos",
  "hermanos, Vue no sabe a cuál pegar las cosas y lo dice por consola:",
  "",
  "```",
  "[Vue warn]: Extraneous non-props attributes (data-x) were passed to component",
  "but could not be automatically inherited because component renders fragment…",
  "```",
  "",
  "Es uno de los pocos avisos de Vue que hay que leer entero: significa «me has pasado algo y",
  "lo he tirado». La solución es la misma de siempre, decir a mano dónde va.",
  "",
  "**Una prop declarada deja de ser un atributo.** En cuanto pones `etiqueta` en `props`, deja",
  "de aparecer en `$attrs` y no se pinta en el HTML. Eso está bien, y explica por qué un dato",
  "que antes salía en el elemento desaparece el día que alguien lo declara.",
  "",
  "**`inheritAttrs: false` no cambia nada por sí solo.** Solo apaga; si te olvidas del",
  "`v-bind`, los atributos no van a ninguna parte y el componente empieza a ignorar todo lo",
  "que le pasen. Los dos pasos van juntos siempre.",
  "",
  "**`class` y `style` se suman, y eso también se apaga.** Con el reparto apagado, la clase de",
  "fuera tampoco se pega en la raíz: acaba donde pongas el `v-bind`. A veces es justo lo que",
  "quieres y a veces no, y entonces hay que repartir a mano.",
  "",
  "## Pruébalo tú",
  "",
  "Con el `Campo` bien escrito, ¿qué pinta esto?",
  "",
  "```html",
  "<Campo etiqueta=\"Nota\" maxlength=\"4\" data-linea=\"7\" />",
  "```",
  "",
  "Pinta un `<label class=\"campo\">` con su `<span>` dentro y un `<input class=\"dato\"",
  "maxlength=\"4\" data-linea=\"7\">`.",
  "",
  "Lo interesante es lo que **no** sale: `etiqueta` no aparece como atributo en ningún sitio,",
  "porque está declarada como prop y se ha usado para pintar el texto. Y `data-linea` sí sale,",
  "aunque el componente no sepa qué es: eso es lo que significa ir de paso.",
  "",
  "## Cómo se plantea",
  "",
  "Cuando escribas un componente que envuelve a otro:",
  "",
  "1. **Decide cuál es el elemento importante.** El `<input>`, el `<button>`, el `<a>`. Es el",
  "   que va a llevar el `v-bind`.",
  "2. **Declara como props solo lo que el componente usa para decidir.** Todo lo demás no es",
  "   asunto suyo y viaja de paso.",
  "3. **Apaga el reparto y ponlo tú.** Las dos líneas, siempre juntas.",
  "4. **Comprueba una raíz.** Si tu plantilla tiene dos elementos hermanos arriba, envuélvelos",
  "   o reparte a mano, y mira la consola.",
  "5. **Y pruébalo sin pasarle nada.** Un envoltorio tiene que funcionar también cuando no",
  "   recibe ningún atributo extra, que es como se va a usar la mitad de las veces.",
)
