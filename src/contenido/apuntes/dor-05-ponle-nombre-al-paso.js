/**
 * El apunte de Raoden para «dor-05-ponle-nombre-al-paso».
 */
import { codigo } from '../retos/comun.js'

export default codigo(
  "Las subconsultas se leen de dentro hacia fuera, y a la tercera se hace insoportable.",
  "`WITH` le pone nombre a un paso intermedio y deja escribir la consulta **de arriba",
  "abajo**, en el orden en que se piensa:",
  "",
  "```sql",
  "WITH grandes AS (",
  "  SELECT puesto_id, SUM(monedas) AS total",
  "  FROM ventas",
  "  GROUP BY puesto_id",
  ")",
  "SELECT * FROM grandes WHERE total > 150;",
  "```",
  "",
  "Lo que sale del `WITH` se usa **como si fuera una tabla**: se puede unir, agrupar,",
  "filtrar y volver a nombrar. Existe solo durante esa consulta.",
  "",
  "## Es lo mismo que una tabla derivada, y se lee mejor",
  "",
  "Esto hace exactamente lo mismo:",
  "",
  "```sql",
  "SELECT * FROM (",
  "  SELECT puesto_id, SUM(monedas) AS total FROM ventas GROUP BY puesto_id",
  ") AS grandes WHERE total > 150;",
  "```",
  "",
  "Y con dos pasos anidados se vuelve ilegible, mientras que con `WITH` se encadenan",
  "separados por una coma y cada uno tiene su nombre:",
  "",
  "```sql",
  "WITH totales AS (...),",
  "     numerados AS (SELECT ... FROM totales ...)",
  "SELECT ... FROM numerados ...;",
  "```",
  "",
  "Fíjate en que el segundo paso puede usar el primero. Eso es lo que convierte una",
  "consulta imposible de leer en una lista de pasos.",
  "",
  "## Lo que gana no es velocidad",
  "",
  "Conviene decirlo, porque se cree lo contrario: en SQLite un `WITH` normal no se",
  "calcula «una vez y se guarda» necesariamente; la base decide, y muchas veces lo trata",
  "igual que si lo hubieras escrito dentro. En algunas bases se puede pedir lo uno o lo",
  "otro (`MATERIALIZED`).",
  "",
  "Lo que gana es que **se pueda leer y se pueda tocar**. Una consulta de cuatro pasos",
  "con nombre se corrige cambiando un paso; la misma anidada se corrige contando",
  "paréntesis.",
  "",
  "## Lo que gana de verdad: cambia lo que es una fila",
  "",
  "Y esto es lo importante del reto. En el paso `totales`, **cada fila es un puesto**. No",
  "una venta: un puesto. Así que en la consulta de fuera:",
  "",
  "```sql",
  "COUNT(*) AS puestos      -- cuenta puestos, sin ningún DISTINCT",
  "SUM(t.total) AS total    -- suma totales de puesto",
  "```",
  "",
  "Sin el paso nombrado habría que contar `COUNT(DISTINCT p.id)`, porque cada puesto",
  "aparecería una vez por venta. Nombrar el paso no es solo comodidad: **cambia la unidad",
  "de la fila**, y con ella los agregados que se pueden escribir sin trampas.",
  "",
  "Cuando una consulta agrupada empiece a necesitar `DISTINCT` por todas partes, casi",
  "siempre es que falta un paso intermedio.",
  "",
  "## Cómo se plantea",
  "",
  "Escribe la consulta como una lista de pasos, en español, antes de tocar el teclado:",
  "",
  "1. «El total de cada puesto.» → un `WITH`.",
  "2. «Sumado por gremios, y solo los que pasen de 150.» → la consulta de fuera.",
  "",
  "Después ejecuta **cada paso por separado** -un `SELECT * FROM el_paso` y a mirar- y",
  "cuenta las filas. Un paso mal hecho se ve en un segundo; el mismo error dentro de una",
  "consulta de veinte líneas, no.",
  "",
  "## Pruébalo tú",
  "",
  "Sustituye la consulta de fuera por `SELECT * FROM totales` y ejecútala. Ocho filas, un",
  "puesto en cada una. Ese es el paso que has nombrado, y poder mirarlo es la mitad de",
  "para qué sirve.",
)
